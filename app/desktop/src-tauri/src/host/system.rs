// Host system module: close-to-tray and tray plumbing live in host::window.
// Updater commands (check_for_update, install_update) are re-exported
// directly from crate::updater in lib.rs, so this module currently holds no
// additional public surface.
