// Workbench — the main command center.
// Re-exports App.tsx which provides the full WebLayout shell with
// ChatView + AgentList sidebar + PromptInput + RunDetail panel.
export { default } from '@/App';
