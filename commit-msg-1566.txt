refactor(edge): 迁移 orchestrator 到叶子包并加依赖方向门禁 (#1566)

A-V1 Step 2：把 13 个 orchestrator*.go + plan_approval.go 从根
internal/adapters god package 迁入叶子包 internal/adapters/orchestrator。

- 合同 SSOT 扩大：AgentAdapter/EventEmitter/AdapterMetadata/AgentCapabilities/
  SubAgentTask/SiblingInfo/SubAgentSpawner/RunProcessContext/CtxBudgetKey/
  BusEvent* 迁入 internal/orchestration，根包保留 alias（外部调用点零改动）
- 叶子包仅依赖合同与窄 ports（AgentExecutor / AdapterRegistry），
  composition root（cmd/agenthub-edge、httpserver、api）注入具体实现
- 依赖方向单向且机器门禁：scripts/verify/verify-orchestrator-deps.ps1
  （叶子不 import 根、orchestration 不 import adapters、根不 import 叶子）
  + 负向自测 scripts/verify/tests/verify-orchestrator-deps.Tests.ps1
- lifecycle 零改动；外部 API/JSON/事件语义不变；未动 workflows

验证：go build/vet 全过，go test ./... -short 全过，
go list -deps 三向依赖证明通过，负向 fixture 真实 FAIL。
