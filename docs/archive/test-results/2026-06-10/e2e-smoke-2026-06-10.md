# AgentHub E2E Smoke Test Results

**Date:** 2026-06-10
**Environment:** Local dev (Hub on :8080, Edge on :3210)
**Hub version:** dev (uptime 7h+)
**Edge version:** dev

---

## 1. Health Checks

| Check | Endpoint | Result | Details |
|-------|----------|--------|---------|
| Hub health | `GET /health` | PASS | `status: ok`, live/ready, DB ok, Redis ok, 49 migrations |
| Edge health | `GET /health` | PASS | `status: ok`, bus ok, store ok |

## 2. cc-switch Status

| Check | Endpoint | Result | Details |
|-------|----------|--------|---------|
| cc-switch installed | `GET /v1/ccswitch/status` | PASS | installed, routing active, proxy :15721, appTypes: claude/codex/gemini |
| cc-switch providers | `GET /v1/ccswitch/providers` | WARN | 0 providers configured -- SDK runs will get 401 from upstream |

## 3. Edge Runners

| Check | Endpoint | Result | Details |
|-------|----------|--------|---------|
| Runners | `GET /v1/runners` | PASS | 1 runner: `runner_local_1` (online, claude-code + streaming + tool_calls + ...) |
| Projects | `GET /v1/projects` | PASS | 2 projects: `proj_demo` (AgentHub Demo), `proj_local` (Local Project) |
| Threads | `GET /v1/threads` | PASS | 11 threads across projects |

## 4. SDK Adapter (Anthropic SDK via cc-switch)

| Check | Result | Details |
|-------|--------|---------|
| Create run | PASS | `POST /v1/runs` returned runId `run_bb1850bfe5c16a52`, status=queued |
| Run completion | PASS | Status progressed: queued -> started -> finished (~2s) |
| Run output | WARN | Agent received 401 from upstream API -- cc-switch has 0 providers configured |

## 5. Memory Endpoint

| Check | Endpoint | Result | Details |
|-------|----------|--------|---------|
| Memory | `GET /v1/memory` | FAIL | 404 -- route does not exist in Edge server. No `/v1/memory` handler registered. |

## 6. Hub API (Authenticated)

| Check | Endpoint | Result | Details |
|-------|----------|--------|---------|
| JWT generation | -- | PASS | Correct claims: `user_id`, `device_type`, `device_id`, `iss`, `aud` |
| Auth/me | `GET /client/auth/me` | PASS | Returned user: `marketplace-system` / `E2E Test User` |
| Devices | `GET /web/devices` | PASS | Empty list (no registered devices) |
| Custom agents | `GET /web/custom-agents` | PASS | Empty list |
| Agent profiles | `GET /web/agent-profiles` | PASS | Empty, paginated response |
| Sessions | `GET /client/sessions` | PASS | 2 sessions |
| Projects | `GET /web/projects` | PASS | 1 project: `E2E Test Project` |
| Skills | `GET /web/skills` | PASS | 1 skill: `PPTX Generator` (public) |
| Execution targets | `GET /web/execution-targets` | PASS | 1 target: `E2E Test Target` (offline, local_edge) |
| Public stats | `GET /api/public/stats` | PASS | Returned stats payload |

---

## Summary

| Category | Pass | Fail | Warn |
|----------|------|------|------|
| Health | 2 | 0 | 0 |
| cc-switch | 1 | 0 | 1 |
| Edge runners | 3 | 0 | 0 |
| SDK adapter | 2 | 0 | 1 |
| Memory | 0 | 1 | 0 |
| Hub API | 10 | 0 | 0 |
| **Total** | **18** | **1** | **2** |

### Issues Found

1. **FAIL -- `/v1/memory` route missing:** The Edge server has no `/v1/memory` handler. This route is not implemented in `internal/api/handlers.go` route registration. Either the test plan references a not-yet-implemented feature, or this route was removed.

2. **WARN -- cc-switch providers empty (0):** No providers configured for `appType=claude`. SDK adapter runs will fail with 401 from upstream. This is a configuration issue, not a code bug.

3. **WARN -- SDK run 401:** The test run completed (status=finished) but the agent message shows `openai-sdk: API returned status 401: Incorrect API key`. Root cause: cc-switch has no providers to route to. Expected behavior given the providers state.
