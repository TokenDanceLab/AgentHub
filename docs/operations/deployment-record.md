# AgentHub Hub Server — 生产部署记录

最后更新：2026-05-27

## 部署拓扑

```
Cloudflare DNS (api.hub.vectorcontrol.tech A → 38.76.183.116, DNS-only)
  → hk2 nginx:80
    → 127.0.0.1:8090 (Docker agenthub-hub)
      → Docker 网络 agenthub-net
        ├── agenthub-postgres:5432 (独立实例)
        └── agenthub-redis:6379 (独立实例)
```

## 服务器信息

| 项 | 值 |
|:--|:--|
| **主机** | hk2（核云 VPS，Ubuntu） |
| **IP** | 38.76.183.116 |
| **仓库路径** | `/opt/agenthub-hub/` |
| **分支** | `dev/delicious233` |
| **Docker 版本** | 29.4.3 |
| **Compose 版本** | v5.1.3 |

## Docker 服务

| 容器 | 镜像 | 端口 | 持久卷 |
|:--|:--|:--|:--|
| `agenthub-postgres` | postgres:16-alpine | 5432（仅容器网络） | `agenthub_pg_data` |
| `agenthub-redis` | redis:7-alpine (AOF) | 6379（仅容器网络） | `agenthub_redis_data` |
| `agenthub-hub` | 预构建镜像（服务器只 `docker load`，不编译） | 127.0.0.1:8090:8080 | `agenthub_uploads` |

## nginx 配置

- 配置文件：`/etc/nginx/sites-enabled/agenthub-api.conf`
- 反代：`api.hub.vectorcontrol.tech:80` → `127.0.0.1:8090`
- WebSocket 支持（IM）：Upgrade + Connection headers
- Body 大小限制：10MB
- 安全头：X-Frame-Options, X-Content-Type-Options, Referrer-Policy

## 密钥管理

- `.env.production` 路径：`hub-server/deployments/.env.production`（gitignored）
- 生成命令：`cd hub-server && bash scripts/generate-secrets.sh`
- 权限：`chmod 600`
- 不进仓库，需安全备份

## 日常运维

```bash
ssh hk2
cd /opt/agenthub-hub

# 拉取最新代码
git pull origin dev/delicious233

# 部署：先在开发机/CI 构建镜像并传到服务器，服务器只加载镜像，不运行 build
cd hub-server
docker load -i /tmp/agenthub-hub-latest.tar
docker compose --env-file deployments/.env.production -f deployments/docker-compose.prod.yml up -d --no-build --no-deps --force-recreate hub-server

# 查看日志
docker compose --env-file deployments/.env.production -f deployments/docker-compose.prod.yml logs -f hub-server

# 健康检查
curl http://localhost:8090/health
curl http://api.hub.vectorcontrol.tech/health

# 公开统计
curl http://api.hub.vectorcontrol.tech/api/public/stats
```

## 最近部署

| 时间 (UTC) | 镜像 | 变更 | 验证 |
|:--|:--|:--|:--|
| 2026-05-27 03:09 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:982c1df71753...` | 本机预构建镜像 tar（35,273,728 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `d16b657`：`HandleTaskAck` 对 queued offline-replayed task 使用任务当前状态作为 atomic oldStatus，真实数据库中 queued/dispatched 都能 CAS 到 running 并写入 `edge_run_id`，修复此前 sqlmock rows=1 掩盖真实 queued→running rows=0、ack 被误拒的问题；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270305-4fc437a` | 本地验证：SQLite 红绿测试、`hub-server && go test ./internal/service -run TestHandleTaskAck_QueuedOfflineReplayTransitionsToRunning -count=1`、`hub-server && go test ./internal/service -run "TestHandleTaskAck" -count=1`、`hub-server && go test ./... -short -count=1`、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:982c1df71753d88085bc9c484cb3309c82cb9ac67304cedbce76efc5d76c9e49`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270211-9755a80`，保留 latest + 3 个 rollback；观测 Hub 7.266 MiB/256 MiB、PG 35.16 MiB/512 MiB、Redis 6.555 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 02:49 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:0dfcd3947296...` | 本机预构建镜像 tar（35,273,728 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `9a61fce`：`HandleTaskAck` 对已 running 且空 `edge_run_id` 的补写会检查 `RowsAffected`；CAS 失败后重读任务，只允许同一 `edge_run_id` 已被并发写入的幂等情况继续，其它 run id 或仍为空的冲突返回 `BAD_REQUEST`。repository backfill 也收紧为仅 running + empty `edge_run_id` 可写；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270247-be1e8b9` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTaskAck_EdgeRunIDBackfill" -count=1`、`hub-server && go test ./internal/repository -run TestPendingTaskRepo_AtomicWithEdgeRunID -count=1`、`hub-server && go test ./internal/service -run "TestHandleTaskAck" -count=1`、`hub-server && go test ./... -short -count=1`、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:0dfcd39472969a5462f10f2167cf41c0c8895afa0dbd3b076b24ee030957a410`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270157-c4fdd76`，保留 latest + 3 个 rollback；观测 Hub 6.426 MiB/256 MiB、PG 35.16 MiB/512 MiB、Redis 6.547 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 02:30 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:81dacb7fb8e7...` | 本机预构建镜像 tar（35,273,728 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `04e4908`：`HandleTaskStream` 不再用无条件 status update 把 `dispatched` 改为 `running`，而是通过 atomic compare-and-swap 迁移；CAS 失败后重读任务状态，只允许已被并发 callback 推到 `running` 的路径继续，终态/冲突返回 `BAD_REQUEST` 且不写 `agent_run_events` / `messages` / `agent.stream`；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270229-072264e` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTask(Stream_DispatchedTransitionConflictDoesNotPersist\|Stream(PersistsTypedRunEventAndProjection\|RejectsOversizedInferredEventType\|RejectsOversizedPayload\|RejectsOversizedProjectedContent\|RejectsOversizedEdgeRunID))" -count=1`、`hub-server && go test ./... -short -count=1`、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:81dacb7fb8e7dc5a2f35258a23e784fb3d97aa66362d72fb6fa06637aadaf16a`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270136-424f999`，保留 latest + 3 个 rollback；观测 Hub 8.438 MiB/256 MiB、PG 35.35 MiB/512 MiB、Redis 6.543 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 02:12 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:27a1e89de9e9...` | 本机预构建镜像 tar（35,270,656 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `ee059c6`：`HandleTaskDone` 和 `HandleTaskFail` 现在检查 atomic status update 的 error 与 affected rows，CAS 冲突返回 `BAD_REQUEST` 且不发布 `agent.done` / `agent.failed`；`HandleTaskDone` 的 final message 插入和 task done 迁移放入同一 DB transaction；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270211-9755a80` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTask(Done_AtomicTransition\|Done_AtomicConflictDoesNotPublish\|Done_AcceptsDispatchedTask\|DoneRejectsOversizedFinalContent\|DoneRejectsOversizedEdgeRunID\|Fail_AtomicTransition\|Fail_AtomicConflictDoesNotPublish\|FailRejectsOversizedError\|FailRejectsOversizedEdgeRunID)" -count=1`、`hub-server && go test ./... -short -count=1`、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:27a1e89de9e952bff2ca28cc0c7cd973bbc823378454430db65318240322198b`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270122-c0bf423`，保留 latest + 3 个 rollback；观测 Hub 7.559 MiB/256 MiB、PG 35.34 MiB/512 MiB、Redis 6.551 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 01:58 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:4179df9af46a...` | 本机预构建镜像 tar（35,270,144 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `a386bf0`：`HandleTaskAck`、`HandleTaskStream`、`HandleTaskDone` 和 `HandleTaskFail` 会在查询/持久化/广播前拒绝超过 128 字符的 `run_id` / `edge_run_id`，对齐 `pending_agent_tasks.edge_run_id` 与 `agent_run_events.edge_run_id` 的 `varchar(128)` 上限；OpenAPI 已在四个 callback body 标注 `maxLength: 128`；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270157-c4fdd76` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTask(Stream(PersistsTypedRunEventAndProjection\|RejectsOversizedInferredEventType\|RejectsOversizedPayload\|RejectsOversizedProjectedContent\|RejectsOversizedEdgeRunID)\|DoneRejectsOversizedFinalContent\|DoneRejectsOversizedEdgeRunID\|FailRejectsOversizedError\|FailRejectsOversizedEdgeRunID\|AckRejectsOversizedEdgeRunID)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:4179df9af46ae89446194dd4743f0be68c870e87663afcda4c91f31acb9c01ca`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270109-61bdcfe`，保留 latest + 3 个 rollback；观测 Hub 9 MiB/256 MiB、PG 35.35 MiB/512 MiB、Redis 6.543 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 01:37 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:b36927d74a75...` | 本机预构建镜像 tar（35,270,656 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `9293ade`：`HandleTaskFail` 现在会在写入 `pending_agent_tasks.error_message` 和广播 `agent.failed` 前拒绝超过 1 MiB 的 `error`，补齐失败回调的 DB/WS 放大入口；OpenAPI 已标注 fail error callback payload budget；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270136-424f999` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTask(Stream(PersistsTypedRunEventAndProjection\|RejectsOversizedInferredEventType\|RejectsOversizedPayload\|RejectsOversizedProjectedContent)\|DoneRejectsOversizedFinalContent\|FailRejectsOversizedError)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:b36927d74a75fa85c0e418586da98c54732d0a908bfecc6694be1e181aaf2363`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270050-2c030c7`，保留 latest + 3 个 rollback；观测 Hub 6.512 MiB/256 MiB、PG 35.15 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 01:23 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:6ce16cab05a1...` | 本机预构建镜像 tar（35,270,144 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `02645f7`：`HandleTaskDone` 现在会在插入 final message 和 task 状态迁移前拒绝超过 1 MiB 的 `final_content`，补齐 stream 之外的 done callback 存储放大入口；OpenAPI 将 done body 对齐为真实 `final_content` 字段并标注 1 MiB callback payload budget；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270122-c0bf423` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTask(Stream(PersistsTypedRunEventAndProjection\|RejectsOversizedInferredEventType\|RejectsOversizedPayload\|RejectsOversizedProjectedContent)\|DoneRejectsOversizedFinalContent)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:6ce16cab05a16f9b01646f1492cfe6861fa81dfa32af904d87ac0e88f32fab89`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270034-7beb39b`，保留 latest + 3 个 rollback；观测 Hub 6.383 MiB/256 MiB、PG 35.14 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 01:10 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:93babdc53d66...` | 本机预构建镜像 tar（35,269,120 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `8533e77`：`HandleTaskStream` 现在会在入库和聊天投影前拒绝超过 1 MiB 的规范化 runtime payload 或 projected message content，和 Edge structured event budget 对齐，避免异常 Edge callback 把大 JSON/文本写入 `agent_run_events.payload` 或 `messages.content` 持续放大数据库与内存压力；OpenAPI 同步 stream payload 预算说明；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270109-61bdcfe` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTaskStream(PersistsTypedRunEventAndProjection\|RejectsOversizedInferredEventType\|RejectsOversizedPayload\|RejectsOversizedProjectedContent)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:93babdc53d6656df9a1fd04509428cf93c91a59e343b41695ee753c6dbf8ca0d`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270022-951a945`，保留 latest + 3 个 rollback；观测 Hub 6.516 MiB/256 MiB、PG 35.15 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 00:53 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:8d2894a34f81...` | 本机预构建镜像 tar（约 34 MB）传输到 hk2 后 `docker load`，发布 commit `6d6d9eb`：`HandleTaskStream` 归一化显式或 payload 推断的 `event_type` 后，会拒绝空值、超过 `agent_run_events.event_type` 96 字符上限或包含非常规事件名字符的值，避免 runtime callback 把不可入库事件类型推到 PostgreSQL；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270050-2c030c7` | 本地验证：service 红绿测试、`hub-server && go test ./internal/service -run "TestHandleTaskStream(PersistsTypedRunEventAndProjection|RejectsOversizedInferredEventType)" -count=1`、`hub-server && go test ./... -short -count=1`、生产 compose config、staged diff check 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:8d2894a34f81b530d4b4114c74dd8efccf19c9c13b87bb2eb73ba56aa52b07b4`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605270010-2ab292b`，保留 latest + 3 个 rollback；观测 Hub 7.266 MiB/256 MiB、PG 35.35 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 00:37 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:7f47fdeffacd...` | 本机预构建镜像 tar（约 34 MB）传输到 hk2 后 `docker load`，发布 commit `57e727e`：`POST /edge/agent-tasks/{id}/stream` 在 handler 层校验并规范化可选 `client_msg_id` UUID，非法值直接 400 且不会进入 service/DB 查询，避免 Edge callback 把非 UUID 值写入或触发 PostgreSQL uuid cast 错误；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270034-7beb39b` | 本地验证：handler 红绿测试、`hub-server && go test ./internal/handler -run TestAgentHandlerTaskStreamRejectsInvalidClientMsgID -count=1`、`hub-server && go test ./... -short -count=1`、生产 compose config、staged diff check 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:7f47fdeffacdc3eaf5b1465758163cba6fc5408d4326b73417aa840898d44f50`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605262355-d60f409`，保留 latest + 3 个 rollback；观测 Hub 7.086 MiB/256 MiB、PG 35.34 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 00:23 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:2ea3baa82e30...` | 本机预构建镜像 tar（约 34 MB）传输到 hk2 后 `docker load`，发布 commit `e86f6f7`：`POST /web/execution-targets` 显式拒绝非空 `health_state`，避免 Web create 入口静默丢弃 rogue 字段导致客户端误以为伪健康状态已设置；该字段仍只允许 Hub proof path 写入；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270022-951a945` | 本地验证：handler 红绿测试、`hub-server && go test ./internal/handler -run TestExecutionTargetHandlerCreateRejectsClientManagedHealthState -count=1`、`hub-server && go test ./... -short -count=1`、生产 compose config、staged diff check 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:2ea3baa82e30fe31ef8a6bddebb2a5d797f170eb5349eef61a067bee9f364fe5`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605262339-3105322`，保留 latest + 3 个 rollback；观测 Hub 7.27 MiB/256 MiB、PG 35.14 MiB/512 MiB、Redis 6.543 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-27 00:11 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:130cc228cddc...` | 本机预构建镜像 tar（约 34 MB）传输到 hk2 后 `docker load`，发布 commit `3de038b`：`ExecutionTargetService.Create/Update` 禁止客户端把 `health_state` 写成 `healthy` / `degraded` / `offline`，`PATCH /web/execution-targets/{id}` 不再在 OpenAPI 暴露 `health_state`；该字段只能由 Hub live proof 路径写入，避免普通 target 配置更新绕过 health proof；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270010-2ab292b` | 本地验证：focused service tests、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:130cc228cddc5b0c5b339f831d610f249ee5d917ba363633837d1de69ffb9490`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605262316-9bd1b8c`，保留 latest + 3 个 rollback；观测 Hub 6.484 MiB/256 MiB、PG 35.14 MiB/512 MiB、Redis 6.539 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-26 23:56 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:046c616a83b0...` | 本机预构建镜像 tar（35,264,512 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `dbcdba9`：`POST /web/execution-targets/{id}/ping` 只允许当前可证明的 `local_edge` 被手动标记 online，并同步写入 `health_state=healthy`；`remote_ssh` / `tailscale` / `cloud_edge` / `hub_relay` 在 live proof path 未实现前返回 `TARGET_NOT_ROUTABLE`，不会写 `is_online`、`last_seen_at` 或伪健康状态；OpenAPI 补 409 语义说明；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262355-d60f409` | 本地验证：TDD red/green focused tests、`hub-server && go test ./internal/service -run TestExecutionTargetPing -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、staged diff check 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:046c616a83b0127418d4962cc4e9bfe67c33d3ece3f1570578adf03cbae6c274`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605262303-c9b924f`，保留 latest + 3 个 rollback；观测 Hub 6.707 MiB/256 MiB、PG 35.14 MiB/512 MiB、Redis 6.535 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-26 23:39 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:db3f2fc21e96...` | 本机预构建镜像 tar（35,266,048 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `7651554`：`POST /web/relay/commands` 兼容 `target_edge_id` 与旧 `target_id` alias，明确要求目标、`command_type` 和非空 `payload`；OpenAPI 同步 relay command schema、200 OK response，并修正 ack path 为 `/web/relay/commands/{id}/ack`；无新 migration；服务器未执行 build；GitHub HTTPS 推送临时不可用时，hk2 代码通过 `git bundle` fast-forward 到同一 commit；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262339-3105322` | 本地验证：TDD red/green focused tests、`hub-server && go test ./internal/handler -run TestRelayHandlerCreateCommand -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、staged diff check 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:db3f2fc21e967fce6f3e5c9826901e92d12581592b4c7903c833da8dbe0e6e27`；清理远端/本地传输 tar/bundle，并删除最旧 rollback `rollback-202605262242-dc8f81b`，保留 latest + 3 个 rollback；观测 Hub 6.273 MiB/256 MiB、PG 35.15 MiB/512 MiB、Redis 6.531 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-26 23:16 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:41cbbb000e74...` | 本机预构建镜像 tar（35,264,512 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `599b9b9`：`GET /web/skills/{id}`、`GET /web/mcp-servers/{id}` 和 internal `ProviderBindingService.Get` 改为 owner-scoped，跨 owner 返回 `AUTH_DEVICE_MISMATCH`，避免用户私有 catalog/resource metadata 被 UUID 枚举读取；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262316-9bd1b8c` | 本地验证：TDD red/green focused tests、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:41cbbb000e74ff7daaa09c1aaa780cd55fac7e171af89e27b4d282742bb58225`；清理远端/本地传输 tar，并删除最旧 rollback `rollback-202605262227-34bde88`，保留 latest + 3 个 rollback；观测 Hub 13.82 MiB/256 MiB、PG 35.15 MiB/512 MiB、Redis 6.53 MiB/384 MiB，根盘 9.0 GiB 可用（69% 使用） |
| 2026-05-26 23:03 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:e80e311c4464...` | 本机预构建镜像 tar（35,262,976 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `acbe28e`：`GET /web/agent-profiles/{id}` 改为 Hub session owner-scoped 读取，跨 owner 返回 `AUTH_DEVICE_MISMATCH`；Marketplace detail 改走 public-only lookup，未发布 profile 返回 `AGENT_NOT_FOUND`；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262303-c9b924f` | 本地验证：TDD red/green focused tests、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、目标文件 `git diff --check` 和本机 Docker build 通过；部署态三容器 healthy；本地与公网 `/health` 返回 `status=ok`、`migrations=39`；运行容器 image `sha256:e80e311c446404bcacb1d32f21ee0c16c3095027b7dfe84a73db024cd697857c`；清理远端/本地传输 tar；观测 Hub 6.57 MiB/256 MiB、PG 35.14 MiB/512 MiB、Redis 6.54 MiB/384 MiB，根盘 9.1 GiB 可用（69% 使用） |
| 2026-05-26 22:42 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:79bf02938e2c...` | 本机预构建镜像 tar（SHA256 `d8eb2343976b500d32544aa09de6b8de562b787ee1f87b67bec6171340032ef3`，35,262,976 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `3fd0c55`：新增 migration 0039，先清理历史 cross-session `message_pins` 行，再将 `message_pins(session_id,message_id)` 约束为复合外键引用 `messages(session_id,id)`，把 pin/session 归属固化到 DB 层；预迁移备份 `/opt/agenthub-hub/hub-server/deployments/backups/agenthub-pg-pre-migration-0039-20260526T224231Z.sql.gz`（SHA256 `e36ded64ff0b771d7c66c9078f2a1ba6dc57617c347597e3d6651b06c17b3cfb`，7417 bytes）；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262242-dc8f81b` | 本地验证：临时 PostgreSQL migration smoke 确认 bad pin 清理到 0、复合外键存在且 cross-session insert 被拒绝；`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、diff check 和本机 Docker build 通过；部署态本地与公网 `/health` migrations=39；迁移前/后 bad pin 均为 0；`message_pins` 上存在 `fk_message_pins_message_session`；未登录访问 summary endpoint 返回 401；运行镜像 `sha256:79bf02938e2c55d34721e486ac392f834635e033771552cc555d53d2fa077bf7`；清理远端/本地传输 tar，并保留 latest + 最近两个 rollback；观测 Hub 7.75 MiB/256 MiB、PG 35.36 MiB/512 MiB、Redis 6.54 MiB/384 MiB，根盘 9.3 GiB 可用（69% 使用） |
| 2026-05-26 22:27 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:e04b5b910d34...` | 本机预构建镜像 tar（SHA256 `fc3e79708ab340407c479ebf3a0e7565d182453021dd3dbfea1449a4b56b4590`，35,262,976 bytes，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `4c17ae0`：`GET /web/agent-tasks/{id}/events` 增加 `event_type` / `after_seq` / `limit` owner-scoped 查询过滤，新增 `GET /web/agent-tasks/{id}/events/summary` 汇总 runtime event counts、tool/step/artifact/approval、token、output bytes 和 elapsed runtime 指标；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262227-34bde88` | 本地验证：runtime history service/handler focused tests、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、diff check 和本机 Docker build 通过；部署态三容器 healthy；本地 `/health` migrations=38；公网 `/health` 返回 `status=ok`；未登录访问 summary endpoint 返回 401；运行镜像 `sha256:e04b5b910d347881106e1274d70210bf86a358e819fac132a3ba3a05445888b8`；观测 Hub 6.2 MiB/256 MiB、PG 35.0 MiB/512 MiB、Redis 6.5 MiB/384 MiB；清理远端/本地传输 tar，并删除 5 个更旧 AgentHub rollback 镜像，保留 latest + 最近两个 rollback，根盘 9.2 GiB 可用（69% 使用） |
| 2026-05-26 22:02 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:f1c06fc3cf5a...` | 本机预构建镜像 tar（SHA256 `69d24816bc10c0de0099f772ffe37029045e5b4a932a548b364751096bcc07a4`，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `0c52cce`：新增 exact-device `agent.control` / `permission.decide` Hub 控制通道、user/device scoped Redis 离线控制队列、TeamRun approval decision 到 `pending_agent_tasks.edge_device_id` 的投递绑定；缺 `edge_device_id` 时拒绝决策，禁止 fallback 到其它 Desktop；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605270602-303a7de` | 本地验证：TDD red/green focused tests、`hub-server && go test ./internal/cache ./internal/ws ./internal/app -run "Test(PendingAgentControls\|PendingTargetTasks\|OnRouteSetReplays\|AllFrameTypes\|StartEventSubscriptionsPushesAgentStream)" -count=1`、`hub-server && go test ./internal/service ./internal/handler -run "TestAgentTeam(Service\|Handler)_(DecideApproval\|ResolveConflict\|GetTeamRunStateProjectsDependenciesAndBudget\|HandleRouteDecision\|ListTeam)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、diff check 和本机 Docker build 通过；部署态三容器 healthy；本地 `/health` migrations=38；未登录访问 approval decide endpoint 返回 401；运行镜像 `sha256:f1c06fc3cf5a9d86fc455d7053839a2a9bcad059e2976f75792e2f5621a3c8af`；观测 Hub 6.0 MiB/256 MiB、PG 35.0 MiB/512 MiB、Redis 6.8 MiB/384 MiB，根盘 8.7 GiB 可用（70% 使用）；本地/远端传输 tar 已清理 |
| 2026-05-26 21:37 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:e33d9e2b6468...` | 本机预构建镜像 tar（SHA256 `109540f2173582160d7caea51a527dc3114a1798d4e3ed2567520427bdfda913`，约 34 MB）传输到 hk2 后 `docker load`，发布 commit `86c7b36`：新增 `POST /web/agent-teams/{id}/runs/{run_id}/approvals/{approval_id}/decide`，校验 pending TeamRun approval 后写入 append-only `team.approval.decided` TeamEvent，并在 `TeamRunState` replay 中标记 decision、decided_by、decided_at、reason 和可投递给 Edge `/v1/permissions/decide` 的 `edge_control` payload；无新 migration；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-202605262137-903b725` | 本地验证：TDD red/green focused tests、`hub-server && go test ./internal/service ./internal/handler -run "TestAgentTeam(Service\|Handler)_(DecideApproval\|ResolveConflict\|GetTeamRunStateProjectsDependenciesAndBudget\|HandleRouteDecision\|ListTeam)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、staged diff check 和本机 Docker build 通过；部署态 Docker health `healthy`；本地 `/health` migrations=38；未登录访问 approval decide endpoint 返回 401；Docker 限制读回 Hub 256MiB/1 CPU/256 pids，PostgreSQL 512MiB/1 CPU/256 pids，Redis 384MiB/0.5 CPU/128 pids；观测 Hub ~6.5 MiB、PG ~35.0 MiB、Redis ~6.5 MiB，根盘约 8.9 GiB 可用（70% 使用）；本地/远端传输 tar 已清理 |
| 2026-05-26 21:15 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:61d9582d1005...` | 本机预构建镜像 tar（SHA256 `46174db23b34bc0ccaa044f8b5bbac685f2b6ca3dde778796dcdbf741c5f5974`，约 35 MB）传输到 hk2 后 `docker load`，发布 commit `b9d8734`：新增 migration 0038 `agent_team_artifacts`，TeamRunState projection 刷新 artifact DB index，持久化 team_run/team_task/assignment/member/agent_task/edge_run/source_event/tool/normalized_path/conflict_id 追溯字段；预迁移备份 `/opt/agenthub-hub/hub-server/deployments/backups/agenthub-pg-pre-migration-0038-20260526T211305Z.sql.gz`（SHA256 `c56742687e6b60e5d39d91a60773ba3da46caf0100879410fb28357ff5d91b49`，7018 bytes）；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-20260526211437-179814d` | 本地验证：`hub-server && go test ./internal/service -run "TestAgentTeamService_(GetTeamRunStateProjectsDependenciesAndBudget|ResolveConflict)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、staged diff check 和本机 Docker build 通过；部署态 Docker health `healthy`；本地/公网 `/health` migrations=38；PostgreSQL `to_regclass('public.agent_team_artifacts')` 返回 `agent_team_artifacts`；未登录访问 conflict resolve endpoint 返回 401；Docker 限制读回 Hub 256MiB/1 CPU/256 pids，PostgreSQL 512MiB/1 CPU/256 pids，Redis 384MiB/0.5 CPU/128 pids；观测 Hub ~7.3 MiB、PG ~34.9 MiB、Redis ~6.5 MiB，根盘约 9.0 GiB 可用（70% 使用）；本地/远端传输 tar 已清理 |
| 2026-05-26 21:01 | `ghcr.io/tokendancelab/agenthub-hub:latest` -> `sha256:e2a14107f0d5...` | 本机预构建镜像 tar（SHA256 `e9ee1a63b84313217a9bb6b73f9759b4f3609c13b212ca831e6b271f987c64b1`，约 35 MB）传输到 hk2 后 `docker load`，发布 commit `c951582`：新增 `POST /web/agent-teams/{id}/runs/{run_id}/conflicts/{conflict_id}/resolve`，人工 conflict resolution 写入 append-only `team.conflict.resolved` TeamEvent，TeamRunState replay 会标记 conflict `resolved` 并记录 resolution、resolved_by、resolved_at、reason、selected_agent_task_id；服务器未执行 build；旧 latest 已保留为 `ghcr.io/tokendancelab/agenthub-hub:rollback-20260526210032-c373291` | 本地验证：focused AgentTeam service/handler tests、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config、staged diff check 和本机 Docker build 通过；部署态 Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 conflict resolve endpoint 返回 401；Docker 限制读回 Hub 256MiB/1 CPU/256 pids，PostgreSQL 512MiB/1 CPU/256 pids，Redis 384MiB/0.5 CPU/128 pids；观测 Hub ~7.2 MiB、PG ~30.2 MiB、Redis ~6.5 MiB，根盘约 9.1 GiB 可用（69% 使用） |
| 2026-05-26 20:42 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:85a1b7bf76b1...` | 本机预构建镜像 tar（约 35 MB）传输到 hk2 后 `docker load`，发布 commit `8580066`：TeamRunState approval/file-change projection 增加 `team_task_id` / `assignment_id` / `member_id` 追溯字段，并从多个 member/task 对同一路径的 file-change 事件投影 `conflicts[]` 和 artifact `conflict_id`；生产 compose 同步应用 Hub/PostgreSQL/Redis `mem_limit`、`cpus`、`pids_limit` 与 Redis 连接池默认值 20/2；服务器未执行 build；保留 `ghcr.io/tokendancelab/agenthub-hub:rollback-20260526-8580066` 回滚镜像 | 本地验证：`hub-server && go test ./internal/service -run "TestAgentTeamService_(GetTeamRunStateProjectsDependenciesAndBudget|HandleRouteDecision|DispatchAssignment)" -count=1`、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析、生产 compose config 和 diff check 通过；部署态 Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Docker 限制读回 Hub 256MiB/1 CPU/256 pids，PostgreSQL 512MiB/1 CPU/256 pids，Redis 384MiB/0.5 CPU/128 pids；观测 Hub ~7.1 MiB、PG ~29.2 MiB、Redis ~6.0 MiB，根盘约 9.2 GiB 可用（69% 使用）；本地/远端传输 tar 已清理 |
| 2026-05-26 20:23 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:53b9c72c7dd8...` | 本机预构建镜像 tar（SHA256 `7b5b03a344263662ab7ad62c5e19dd2b7965498159da499c6a6b96ff2179aa65`）传输到 hk2 后 `docker load`，发布 commit `0cfc85f`：TeamRun supervisor dispatch 携带 structured output schema + TeamRun context，Edge Claude structured output 提升为 `run.agent.route_decision`，Desktop bridge 自动 POST route decision 到 Hub；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_202320.txt`；保留 `rollback-before-route-binding-20260526-2023z` 回滚镜像 | 本地验证：Hub/Edge `go test ./... -short -count=1`、Desktop `corepack.cmd pnpm typecheck`、`useHubIntegration` Vitest 30/30、OpenAPI YAML 解析和 staged diff check 通过；部署态 Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Hub ~7.6 MiB、PG ~21.8 MiB、Redis ~2.7 MiB；根盘约 9.3 GiB 可用（68% 使用）；本地/远端传输 tar 已清理，旧 budget-timeout rollback 镜像已删除 |
| 2026-05-26 19:42 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:501db86de843...` | 本机预构建镜像 tar（SHA256 `f2517ce94574bac4d09bfc455ca574dbacbba03d0415f7e59162e8bf231d09db`）传输到 hk2 后 `docker load`，发布 AT-3 Hub guardrail 补强：route decision 接受前拦截 active assignment 超时、TeamRun runtime budget/token limit/usage percent 超限并写 `team.route.rejected`；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_194250.txt`；保留 `rollback-before-budget-timeout-20260526-1940z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Hub ~7.8 MiB、PG ~21.7 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 approvals rollback 镜像已删除 |
| 2026-05-26 19:32 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:fc93676d794d...` | 本机预构建镜像 tar（SHA256 `ad758ef23f695ab57a0ff31c9e734da8a891fbb859e263b92db1c4d82fba13d8`）传输到 hk2 后 `docker load`，发布 TeamRunState approval/artifact summary projection：从 `run.agent.permission_requested` / `run.agent.permission_decided` 派生 approvals，并从 `run.agent.file_change` 派生 artifact/file-change 摘要；AT-2 后端 projection 闭环；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_193234.txt`；保留 `rollback-before-approvals-20260526-1930z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Hub ~7.6 MiB、PG ~21.3 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 statebudget rollback 镜像已删除 |
| 2026-05-26 19:23 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:4ab08a930759...` | 本机预构建镜像 tar（SHA256 `da88bf647e59b063716ed8c0c18a05fd14775e55ce67ba38804c2a0c6a1d450b`）传输到 hk2 后 `docker load`，发布 TeamRunState `dependencies` + `budget` projection：从 TeamTask `parent_task_id` 派生 dependency edges，并从 `run.agent.result` / context usage events 汇总 token budget；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_192322.txt`；保留 `rollback-before-statebudget-20260526-1921z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Hub ~8.1 MiB、PG ~21.3 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 runevents rollback 镜像已删除 |
| 2026-05-26 19:09 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:7fba1c34c427...` | 本机预构建镜像 tar（SHA256 `137186f9c9057e1404eb3a99858f936d0e6c84dff7560a74e2b9b3c5265d3851`）传输到 hk2 后 `docker load`，发布 TeamRunState `run_events` projection：按 TeamTask/Assignment 绑定的 Hub task id 合并 `agent_run_events` runtime 摘要；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_190918.txt`；保留 `rollback-before-runevents-20260526-1908z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 TeamRunState endpoint 返回 401；Hub ~8.0 MiB、PG ~21.3 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 guardrails rollback 镜像已删除 |
| 2026-05-26 18:54 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:00ab9aa27411...` | 本机预构建镜像 tar（SHA256 `5b8bf2e05345d565a282c3e49942c65bd9c7df7bd18f3ac2b1e459d40fab096b`）传输到 hk2 后 `docker load`，发布 AT-3 guardrails：`MAX_ACTIVE_SUBAGENTS_PER_RUN` 按 TeamRun 统计、`MAX_ROUTE_REPEATS` 重复 route 拦截、route rejection 审计原因；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_185451.txt`；保留 `rollback-before-guardrails-20260526-1852z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 route-decisions POST 返回 401；Hub ~8.2 MiB、PG ~21.3 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 dispatch-binding rollback 镜像已删除 |
| 2026-05-26 18:39 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:f136c606fea7...` | 本机预构建镜像 tar（SHA256 `7ff58790c1fd5e132cdbaafe51f7ef1fb2dd4a000fc893fa58bdba9eab8f9bcb`）传输到 hk2 后 `docker load`，发布 TeamRun supervisor trigger message 修复、Assignment dispatch → Hub pending task 真实绑定、TeamTask/Assignment `agent_task_id` / `edge_run_id` projection；无新 migration；服务器未执行 build；compose 状态备份 `/opt/agenthub-hub/hub-server/deployments/backups/ps_20260526_183936.txt`；保留 `rollback-before-dispatch-binding-20260526-1837z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 assignment dispatch POST 与 tasks endpoint 均返回 401；Hub ~7.8 MiB、PG ~21.3 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理，旧 TeamTask rollback 镜像已删除 |
| 2026-05-26 18:16 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:8fbf4ae18632...` | 本机预构建镜像 tar（SHA256 `64e1ea3dc0cda444ab934f96596dc91e1116d69878ca2a5f8600ec6d32779a74`）传输到 hk2 后 `docker load`，发布 migration 0037、`agent_team_tasks` model/API/projection、owner-scoped `GET /web/agent-teams/{id}/runs/{run_id}/tasks`，accepted route decision 会同步创建 TeamAssignment + TeamTask 并写 `team.task.created`；预迁移备份 `/opt/agenthub-hub/hub-server/deployments/backups/agenthub-pg-pre-migration-0037-20260526T1810Z.sql.gz`（SHA256 `8206cdc52de3a01509541317ced63b798e4ba44989f5efa5415e338636d0aafa`）；服务器未执行 build；保留 `rollback-before-teamtasks-20260526-1813z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=37；未登录访问 tasks endpoint 返回 401；Hub ~9.0 MiB、PG ~21.4 MiB、Redis ~2.7 MiB；根盘约 9.2 GiB 可用（69% 使用）；传输 tar 已清理 |
| 2026-05-26 17:51 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:c7204543986d...` | 本机预构建镜像传输到 hk2，发布 owner-scoped `GET /web/agent-teams/{id}/runs/{run_id}/events` TeamEvent 读取 API；服务器未执行 build；保留 `rollback-before-events-api-20260526-1753z` 回滚镜像 | Docker health `healthy`；本地/公网 `/health` migrations=36；未登录访问 events endpoint 返回 401；Hub ~8 MiB、PG ~19 MiB、Redis ~5 MiB；根盘约 9.3 GiB 可用 |
| 2026-05-26 17:27 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:300cc8e8ddad...` | 本机预构建镜像传输到 hk2，发布 `POST /web/agent-teams/{id}/runs/{run_id}/route-decisions`、typed route accepted/rejected TeamEvent 审计和 TeamRun 任务总数 guardrail；服务器未执行 build；该版本后续作为 `rollback-before-events-api-20260526-1753z` 保留 | Docker health `healthy`；本地/公网 `/health` migrations=36；未登录访问 route decision endpoint 返回 401；Hub ~8 MiB、PG ~19 MiB、Redis ~3 MiB |
| 2026-05-26 16:59 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:919673032e59...` | 本机预构建镜像传输到 hk2，发布 TeamEvent append/list 与 TeamRunState replay API；服务器未执行 build | Docker health `healthy`；`/health` migrations=36；公网 `/health` OK；未登录访问 state route 返回 401 |
| 2026-05-26 16:40 | `ghcr.io/tokendancelab/agenthub-hub:latest` → `sha256:55c372b7041b...` | 本机预构建镜像传输到 hk2，升级到 commit `b77591d` 并执行 migration 0036 | Docker health `healthy`；`/health` migrations=36；公网 `/api/public/stats` OK |

## 与 AIhub 隔离

| 资源 | AIhub（已有） | AgentHub（新增） |
|:--|:--|:--|
| PostgreSQL | 宿主机 :5432 | Docker agenthub-postgres（容器内 :5432，不对外暴露） |
| Redis | 宿主机 :6379 | Docker agenthub-redis（容器内 :6379，不对外暴露） |
| 端口 | 8080（NewAPI）+ 其他 | 127.0.0.1:8090（仅 nginx 访问） |
