# AgentHub 项目状态

最后更新：2026-05-30 UTC+8 | 分支：dev/delicious233 | 状态：Desktop UI 继续治理中；Mobile/Web 正在按 Desktop 设计系统收敛，Web search dialog、Web IM empty/locked cards、Web sidebar section headers、Web sidebar thread rows、Web RunDetail empty/source/tool-call rows 与 mobile overlay fallback、Mobile Chat message bubbles、Mobile Account readiness/surface registry、Run detail block/resource rows、Web route context 与 Mobile Chat context/activity cards、queue overview headers/header/list rows/triage cards、状态反馈已继续收敛到 shared UI 组件，Web/Mobile TokenDance brand mark 已收敛到 shared UI 组件，待 Hub workflow API/session contract 与 OIDC/secure store 实现

> 2026-05-30 Shared MessageBubble / Mobile Chat bubble cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `MessageBubble`，承接 author/time/actions/content 和 start/end alignment 的消息气泡结构；补 `MessageBubble.test.tsx` 与 Storybook story。Mobile `ChatView` 的普通消息和本地发送回显不再手写 `article.mobileMessageRow` / `div.mobileMessage` / meta / content DOM，改为 shared `MessageBubble` + 现有 `mobileMessage*` class overrides，保留移动端右侧用户气泡、左侧 Agent 文本、Copy 44px+ action、本地 sending/failed/sent 状态。截图证据由 `app/mobile && corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-chat-context-mocked-dark.png`、`mobile-design-chat-copy-feedback-mocked-dark.png`、`mobile-design-chat-send-pending-mocked-dark.png`、`mobile-design-chat-send-error-retry-mocked-dark.png`、`mobile-design-chat-send-success-mocked-dark.png`、`mobile-design-light-chat-send-error-retry-mocked.png` 等 390x844 Chat 场景；probe 输出确认 `scrollWidth=390`、Back 44x44、Copy 68x44/Copied 79x44、Send 85x52、Retry 80x44、bottom nav 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test -- MessageBubble.test.tsx ActivityCard.test.tsx`（29 files / 131 tests）、`app && corepack.cmd pnpm --filter agenthub-mobile typecheck`、`app && corepack.cmd pnpm --filter agenthub-mobile build`、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/shared app/web docs/handoff/STATE.md`。

> 2026-05-30 Web SearchDialog shell wiring / shared row cleanup：本轮继续只整理 `app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。Web `SearchDialog` 原先只在 registry 注册、组件内部监听 Ctrl+K，但 WebLayout 没有实际渲染该 slot；本轮把 `search-dialog` 挂到 Web shell modal 区，Ctrl+K 现在能打开真实搜索弹窗。搜索结果行不再手写 item/icon/body/timestamp DOM，改为 shared `SelectableRow` + SearchDialog CSS Modules overrides；无结果态改为 shared `EmptyState`，并补 `search.*` zh/en i18n，移除硬编码英文。首次截图发现 dialog 被 overlay flex stretch 拉成 60vh 空壳，不符合“卡片不能空、文字填充饱满”；已给 overlay 加 `align-items:flex-start`，弹窗按内容收缩。Web visual QA 新增 `web-design-search-dialog-desktop-1440x920` 场景，用 Ctrl+K 打开、输入 `Desktop glass`，断言搜索弹窗和 shared selectable result row 可见；截图证据：`app/web/screenshots/web-design-search-dialog-desktop-1440x920.png`。probe 确认 `searchDialog.width=560`、`visibleSearchResultButtonCount=1`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`rawI18nKeys=[]`、`scrollWidth=1440`、console 为空。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test -- SelectableRow.test.tsx EmptyState.test.tsx`（28 files / 129 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web exec vitest run src/utils/hubAdapters.test.ts`（10 tests）、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app && corepack.cmd pnpm --filter agenthub-web visual:qa`（13 scenes）、`app && corepack.cmd pnpm --filter agenthub-mobile typecheck`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web app/shared app/mobile docs/handoff/STATE.md`。

> 2026-05-30 Shared SelectableRow / Web ThreadPanel sidebar cleanup：本轮继续只整理 `app/shared/**`、`app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `SelectableRow`，用于“主行可选择 + 右侧 actions 不嵌套进主 button”的 sidebar/list row 模式；补 `SelectableRow.test.tsx` 与 Storybook story，默认把 decorative leading/icon `aria-hidden`，避免污染可访问名称。Web `ThreadPanel` 普通 thread row 不再手写整行 button/body/actions DOM，改为 shared `SelectableRow` + 现有 CSS Modules class overrides；同时修复 `.sidebar` 被 `display:none` 连同列表一起隐藏的真实可见缺陷，现在 Web 左侧 THREADS 区域能显示 Hub sessions。Web visual QA 新增 `web-design-thread-sidebar-desktop-1440x920` 场景，断言两个 visible selectable thread buttons、无 button nesting、无渐变/无左彩条/无横向溢出；截图证据：`app/web/screenshots/web-design-thread-sidebar-desktop-1440x920.png`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test -- SelectableRow.test.tsx ActionList.test.tsx`（28 files / 129 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web exec vitest run src/utils/hubAdapters.test.ts`（10 tests）、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app && corepack.cmd pnpm --filter agenthub-web visual:qa`（12 scenes）、`app && corepack.cmd pnpm --filter agenthub-mobile typecheck`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web app/shared app/mobile docs/handoff/STATE.md`。

> 2026-05-30 Shared DisclosureRow / Web RunDetail tool-call cleanup：本轮继续只整理 `app/shared/**`、`app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。`@agenthub/shared/ui` 的 `DisclosureRow` 新增 disabled 语义和 story/test 覆盖，用于不可展开的 pending/no-output 行；Web `RunDetail` 的 tool call row 不再手写 disclosure DOM/button/chevron/output wrapper，改为 shared `DisclosureRow` + 现有 CSS Modules class overrides，保持右侧 Run panel 的 Desktop-like dense glass、无渐变、无左彩条。Web visual QA mock Hub 消息新增 Bash tool call + tool result，并新增 `web-design-run-detail-tool-call-desktop-1440x920` 场景，自动展开 completed tool call 后截图；probe 确认 `gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`rawI18nKeys=[]`、`scrollWidth=1440`、console 为空。截图证据：`app/web/screenshots/web-design-run-detail-tool-call-desktop-1440x920.png`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test -- DisclosureRow.test.tsx ActivityCard.test.tsx`（27 files / 126 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web exec vitest run src/utils/hubAdapters.test.ts`（10 tests）、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app && corepack.cmd pnpm --filter agenthub-web visual:qa`（11 scenes）、`app && corepack.cmd pnpm --filter agenthub-mobile typecheck`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web app/shared app/mobile docs/handoff/STATE.md`。内置浏览器本轮尝试复核当前 `127.0.0.1:5175` tab 时连接 120s 超时，未作为验收证据。

> 2026-05-30 Shared EmptyState / Web AgentList empty runtime cleanup：本轮继续只整理 `app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。Web `AgentList` 的 no runtime / no search match 空态不再手写 `div`，改为 shared `EmptyState` + 侧栏专用 compact class overrides；空态补充 zh/en description，避免侧栏只剩一行瘦空文案，同时保持 Desktop-like dense sidebar、无渐变、无左彩条。Web visual QA 新增 `web-design-workspace-desktop-empty-agents-1440x920` 场景，通过 request header 让 mock Hub 返回空 `agent-profiles`，硬断言 `No runtimes available` 可见并生成截图。截图证据：`app/web/screenshots/web-design-workspace-desktop-empty-agents-1440x920.png`，probe 确认 `gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`rawI18nKeys=[]`、`scrollWidth=1440`。验证通过：`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app && corepack.cmd pnpm --filter agenthub-web visual:qa`（10 scenes）、`app/mobile && corepack.cmd pnpm typecheck`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果。

> 2026-05-30 Shared DisclosureRow / Web runtime status cleanup：本轮继续只整理 `app/shared/**`、`app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `DisclosureRow`，支持 controlled expanded state、leading/meta/body slots 和 consuming-app class overrides；补 `DisclosureRow.test.tsx` 与 `DisclosureRow.stories.tsx`。Web `ChatView` 的 session init / result runtime status rows 不再手写 disclosure row DOM，改为 shared `DisclosureRow` + 现有 `statusRow*` class，保持 Desktop-like dense glass message timeline、无渐变、无左彩条。`hubAdapters` 现在识别 Hub-delivered JSON envelope `{ event_type, payload }`，`run.agent.session_init` 能渲染为 `session_init` block；`WebLayout` 登录恢复后会 refetch threads/agents，并在 Workspace 无选中 thread 时自动选择第一条，避免登录 chrome 已恢复但主工作区空壳。Mobile/Web 触控回归同时修复：mobile empty-state suggestion chip、Web mobile message action button 和 status row header 均回到 44px+。截图证据：`app && corepack.cmd pnpm --filter agenthub-web visual:qa` 通过 9 scenes，新增 `app/web/screenshots/web-design-workspace-desktop-status-1440x920.png`，probe 确认 `Session initialized — gpt-5` / `Completed — 1420 in / 318 out tokens` 可见、`gradientCount=0`、`leftOnlyBorderCount=0`、`smallTargets=[]`；`app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 同步通过，390px viewport 下 bottom nav Account 仍为最右侧且关键按钮 44px+。验证通过：`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter agenthub-web exec vitest run src/api/hubAuth.test.ts src/utils/hubAdapters.test.ts`（2 files / 11 tests）、`app && corepack.cmd pnpm --filter @agenthub/shared test -- EmptyState.test.tsx DisclosureRow.test.tsx ActivityCard.test.tsx`（27 files / 125 tests）、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、Web visual QA、Mobile visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果。

> 2026-05-30 Shared ActivityCard / Web IM empty-state cleanup：本轮继续只整理 `app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。Web `IMView` 的 Hub locked state 与 no-selection state 不再手写 header/card DOM，改为 shared `SectionHeader` + `ActivityCard`，并通过现有 CSS Modules class override 保持 Desktop-like dense glass、图标信息卡、无渐变、无左彩条、移动视口内容填充饱满。截图证据：`app && corepack.cmd pnpm --filter agenthub-web visual:qa` 通过 8 scenes，人工查看 `app/web/screenshots/web-design-messages-mobile-390x844.png` 确认消息空态在 390x844 下无横向溢出且三张信息卡有有效内容；Mobile visual QA 同步通过，确认 shared UI 继续满足 390px scrollWidth、底部导航与关键按钮 44px+。验证通过：`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 122 tests）、`app && corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`app/mobile && corepack.cmd pnpm build`、Web visual QA、Mobile visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared SectionHeader / Web sidebar header cleanup：本轮继续只整理 `app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。Web shell 左侧 Agents / Threads section header 不再手写 `div/span`，改为 shared `SectionHeader` + 现有 CSS Modules class override；新增 `sectionHeaderTitle` 保持 26px sidebar 标题密度、小字号 uppercase、无额外 section padding，避免 shared H2 默认样式撑高侧栏。截图证据：`app && corepack.cmd pnpm --filter agenthub-web visual:qa` 通过 8 scenes，人工查看 `app/web/screenshots/web-design-workspace-desktop-1440x920.png` 确认左栏 Agents/Threads 仍为紧凑 Desktop-like sidebar header。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 122 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、Web visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ActivityCard / Web RunDetail density cleanup：本轮继续只整理 `app/shared/**`、`app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。扩展 `@agenthub/shared/ui` 的 `ActivityCard`，允许 compact icon+label row 不传 body copy，并补 test 覆盖无正文行。Web `RunDetail` 的 inactive/empty cards 与 changed file source rows 不再手写 row DOM，改为 shared `ActivityCard` + 现有 CSS Modules class，保留 Desktop-like dense glass right panel。visual QA 截图复核时发现 `web-design-run-overlay-mobile-390x844.png` 虽通过无溢出/无渐变断言，但移动 Web Run overlay 实际是空玻璃面板；本轮新增 run-detail lazy slot shared fallback，用三条 compact `ActivityCard` 显示 `No active run` / `No output yet` / `No sources yet`，并把 Web visual QA 改为等待 `Run Detail` region 和 `No active run` 文案，避免空壳 overlay 再次通过。截图证据：`app && corepack.cmd pnpm --filter agenthub-web visual:qa` 通过 8 scenes，`web-design-run-overlay-mobile-390x844.png` 显示密集 Run Detail fallback；Mobile visual QA 同步通过，确认 shared ActivityCard 变更未破坏 390x844 mobile touch targets。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 122 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、Web visual QA、Mobile visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ActivityCard / Mobile Account status-row cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。扩展 `@agenthub/shared/ui` 的 `ActivityCard`，新增 `data-has-icon` 网格分支，让无 icon 的 body + trailing state 行不再继承 34px 空列；补 test/story 覆盖 Account/surface registry trailing state 用法。Mobile `AccountView` 的 readiness rows 与 client surface registry rows 不再手写 row DOM，改为 shared `ActivityCard` + 现有 `mobileReadiness*`、`mobileSurfaceRegistry*` class，保持 Account 作为右下底栏身份入口、Desktop-like dense glass rows、无渐变、无左彩条、卡片内容饱满。visual QA 首次拦截了 shared specificity 变化导致 Run detail resource `Inspect` 按钮回退到 34x44 的回归；已把 `.mobileResourceRow[data-has-icon='true'][data-has-actions='true']` override 提到同等 specificity，最终 Mobile probe 输出确认 `Copy` 86x44、`Inspect` 101x44。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-light-settings-readiness-mocked.png`、`mobile-design-settings-compact-feedback-mocked-dark.png`、`mobile-design-run-summary-mocked-dark.png`、`mobile-design-light-approval-submit-error-mocked.png` 等 390x844 Account/Run 场景，`scrollWidth=390`、bottom nav 52px；Web visual QA 在 IPv4 `127.0.0.1:5175` dev server 下通过 8 scenes。内置浏览器本轮尝试连接当前 5174 tab 时插件初始化 120s 超时，未作为验收证据；已人工查看本轮生成的 `mobile-design-light-settings-readiness-mocked.png` 截图确认 Account rows 无横向溢出。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 121 tests）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、Mobile visual QA、Web visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ActivityCard / Mobile Run detail block-resource cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。扩展 `@agenthub/shared/ui` 的 `ActivityCard`，新增 leading、actions slot、leading/actions class overrides 和 shared 默认 grid columns；补 test/story 覆盖 Run block/resource 用法。Mobile `RunStatusView` 的 structured blocks 与 output resource rows 不再手写 `mobileRunBlock` / `mobileResourceRow` article DOM，改为 shared `ActivityCard` + 现有 `mobileRunBlock*`、`mobileResourceRow`、`mobileResourceActions` class，保持 Desktop-like dense glass rows、Copy/Inspect 44px+ 操作、无渐变、无左彩条。visual QA 首次拦截了 shared actions wrapper 导致 `mobile-design-run-summary-mocked-dark.png` 里两个 `Inspect` 按钮压到 34x44 的回归；已通过提高 Mobile resource row/grid override 修复，最终 probe 输出确认 Run detail resource actions 为 `Copy` 86x44、`Inspect` 101x44。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-run-summary-mocked-dark.png`、`mobile-design-run-blocks-mocked-dark.png`、`mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-approval-submit-retry-success-mocked.png` 等 390x844 Run detail 场景，`scrollWidth=390`、bottom nav 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 120 tests）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、Mobile visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ContextSummary / Web route context cleanup：本轮继续只整理 `app/shared/**`、`app/web/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。扩展 `@agenthub/shared/ui` 的 `ContextSummary`，新增 icon、description、actions slot 以及 Web shell class overrides，并补 test/story 覆盖 route context 用法。Web `WebLayout` 的 `routeContextPanel` 不再手写 header/copy/metric/action DOM，改为 shared `ContextSummary` + 现有 `routeContext*` / `routeMetric*` / `routeActionRow` CSS Modules class，保持 Desktop-like dense glass route context、移动 web 44px route actions、无渐变、无左彩条。截图证据：`app && corepack.cmd pnpm --filter agenthub-web visual:qa` 在 IPv4 `127.0.0.1:5175` dev server 下通过 8 scenes，截图写入 `app/web/screenshots/`；`app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 同步通过，覆盖 Mobile Chat/queue/run detail 390x844 场景，probe 输出确认 `scrollWidth=390`、关键触控目标 44px+、bottom nav 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 119 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、Web/Mobile visual QA、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ContextSummary / ActivityCard / Mobile Chat cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `ContextSummary` 与 `ActivityCard`，分别承接 thread context definition-list summary 和 typed thread activity card 结构；补 `ContextSummary.test.tsx`、`ContextSummary.stories.tsx`、`ActivityCard.test.tsx`、`ActivityCard.stories.tsx`。Mobile `ChatView` 不再手写 `mobileChatContextPanel` / `mobileActivityCard` DOM，只负责把 thread/item 数据、i18n label、time 和 lucide icon 映射给 shared 组件，并继续传入现有 `mobileChatContextPanel`、`mobileEyebrow`、`mobileActivityCard`、`mobileActivityIcon`、`mobileActivityBody`、`mobileActivityMeta` class，保持 Desktop-like dense glass chat context、typed activity 语义、无渐变、无左彩条。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-chat-context-mocked-dark.png`、`mobile-design-chat-activity-cards-mocked-dark.png`、`mobile-design-light-chat-recovery-mocked.png`、`mobile-design-light-chat-send-error-retry-mocked.png` 等 390x844 Chat 场景；probe 输出确认 `scrollWidth=390`、Back/Copy/Retry 等关键触控目标 44px+、bottom nav 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（26 files / 117 tests）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。内置浏览器本轮尝试连接当前 5174 tab 时插件初始化 120s 超时，未作为验收证据；以 visual QA 截图/探针为准。

> 2026-05-30 Shared SectionHeader / Mobile overview header cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `SectionHeader`，支持 eyebrow、title、action icon、action aria label、busy state 和 consuming-app class overrides；补 `SectionHeader.test.tsx` 与 `SectionHeader.stories.tsx`。Mobile Threads/Runs overview title row 不再各自手写 `mobileOverviewTitleRow` / refresh button DOM，统一消费 shared section header，并继续传入现有 `mobileOverviewTitleRow`、`mobileEyebrow`、`mobileIconButton` class，保持 Desktop-like dense glass overview、44px refresh target、无渐变、无左彩条。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-threads-refreshing-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png`、`mobile-design-light-threads-empty-filter-mocked.png`、`mobile-design-light-runs-empty-filter-mocked.png` 等 390x844 overview 场景；probe 输出确认 `scrollWidth=390`、Threads/Runs refresh buttons 44x44、bottom nav 52px、Continue handoff card 370x84、Threads/Runs rows 370x72/86。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（24 files / 113 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared TriageCard / Mobile queue shortcut cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `TriageCard`，支持 icon、eyebrow、title、meta、action icon、aria label 和 consuming-app class overrides；补 `TriageCard.test.tsx` 与 `TriageCard.stories.tsx`。Mobile Threads 的 Continue handoff 与 Runs 的 Next review 快捷卡不再各自手写 `mobileRunTriageCard` / icon / body / arrow DOM，统一消费 shared card，并继续传入现有 `mobileRunTriageCard`、`mobileRunTriageIcon`、`mobileRunTriageBody`、`mobileRunTriageAction` class，保持 Desktop-like dense glass shortcut、44px+ 触控、无渐变、无左彩条。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-threads-handoff-mocked-dark.png`、`mobile-design-threads-refreshing-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png` 等 390x844 triage 场景；probe 输出确认 Continue handoff card 370x84、Threads/Runs refresh buttons 44x44、bottom nav 52px、`scrollWidth=390`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（23 files / 110 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared ActionList / Mobile queue row cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `ActionList`，支持 icon、title、meta、trailing slot、aria label、item class 和 consuming-app class overrides；补 `ActionList.test.tsx` 与 `ActionList.stories.tsx`。Mobile Threads/Runs 队列行不再各自手写 `mobileListStack` / icon / title / meta / status badge DOM，统一消费 shared list row，并继续传入现有 `mobileThreadItem`、`mobileRunItem*`、`mobileThreadIcon`、`mobileRunIcon`、`mobileListItem*` class，保持 Desktop-like dense glass row、状态 tint、无渐变、无左彩条。visual QA 首次拦截了行 aria label 过窄导致 `/Review approval copy on mobile.*agenthub-mobile/` 定位失败的问题，已修为 Threads 行包含 title/project/time/status、Runs 行包含 run/time/thread/status。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-threads-filter-archived-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png`、`mobile-design-light-threads-empty-filter-mocked.png`、`mobile-design-light-runs-empty-filter-mocked.png` 等 390x844 queue row 场景；probe 输出确认 `scrollWidth=390`、Threads row 370x72、Runs row 370x72/86、bottom nav 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（22 files / 107 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared SurfaceHeader / Mobile queue header cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `SurfaceHeader`，支持 eyebrow、title、status dot/label、action slot、tone 与 consuming-app class overrides；补 `SurfaceHeader.test.tsx` 与 `SurfaceHeader.stories.tsx`。Mobile Threads/Runs 顶部 `mobileHeader` 不再各自手写 title/status badge DOM，统一消费 shared header，并继续传入现有 `mobileHeader` / `mobileEyebrow` / `mobileStatusBadge` / `mobileStatusDot` class，保持 Desktop-like glass 顶栏、小半径状态胶囊、无渐变、无左彩条。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-threads-refreshing-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png`、`mobile-design-light-threads-empty-filter-mocked.png`、`mobile-design-light-runs-empty-filter-mocked.png` 等 390x844 queue header 场景；probe 输出确认 `scrollWidth=390`、Threads/Runs refresh buttons 44x44、bottom nav 52px、列表行 370px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（21 files / 104 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared StatusNotice / Mobile status feedback cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `StatusNotice`，支持 icon、action slot、status/alert/note role、aria-live 和 consuming-app class overrides；补 `StatusNotice.test.tsx` 与 `StatusNotice.stories.tsx`。Mobile Threads/Runs signal row 与 refreshing row、Chat composer sending/error retry、Run detail decision notice/lock/approval sheet status、Account readiness/clear-session sheet status 不再各自手写 status DOM，统一消费 shared 组件并继续叠加现有 Mobile glass class，保持无渐变卡片、无左彩条、44px+ 操作。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-threads-refreshing-mocked-dark.png`、`mobile-design-chat-send-pending-mocked-dark.png`、`mobile-design-chat-send-error-mocked-dark.png`、`mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-settings-readiness-mocked.png` 等 390x844 status 场景；probe 输出确认 `scrollWidth=390`、bottom nav 52px、Chat retry 80x44、approval confirm/cancel 176x44、关键刷新/返回按钮 44x44。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（20 files / 102 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared MetricGrid / Mobile overview cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `MetricGrid`，支持只读 metric tile、可点击 shortcut tile、icon、aria label 和 consuming-app class overrides；补 `MetricGrid.test.tsx` 与 `MetricGrid.stories.tsx`。Mobile Threads overview、Runs overview 和 Run detail summary 三块统计区不再各自手写 `mobileMetricGrid` / `mobileMetricTile` DOM，统一消费 shared `MetricGrid` 并继续叠加原 mobile glass class；Run detail 的 Review/Blocks/Outputs summary 仍是 44px+ shortcut，可读 aria label 已由 shared 组件生成或显式传入。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png`、`mobile-design-light-approval-submit-error-mocked.png` 等 390x844 metric 场景，probe 输出确认 Threads/Runs overview controls 仍无横向溢出，Run detail summary tiles 为 `108x64`，底栏为 52px，`scrollWidth=390`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（19 files / 99 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（首次 Web build 在 `✓ built` 后遇到 Windows Node/uv 退出断言，重跑 exit 0；仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果。

> 2026-05-30 Shared BottomSheet / Mobile sheet cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `BottomSheet`，支持 scrim、handle、header、close action、description、body/footer slots、disabled close 和 consuming-app class overrides；补 `BottomSheet.test.tsx` 与 `BottomSheet.stories.tsx`。Mobile Account clear-session sheet、Run detail approval decision sheet、Run detail output resource sheet 不再各自手写 `mobileSheetLayer` / `mobileBottomSheet` / header / scrim 结构，改为 shared sheet + 现有 mobile glass class 覆盖，保持右下 Account/审批/resource 这些移动端原生底部交互一致。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-rejection-submit-error-mocked.png`、`mobile-design-light-approval-submit-retry-success-mocked.png`、`mobile-design-light-settings-readiness-mocked.png` 等 390x844 sheet 场景，probe 输出确认 sheet scrim/close targets、Cancel/Confirm/Copy/Close 等关键按钮均 44px+，`scrollWidth=390`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（18 files / 96 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared SegmentedControl / Mobile control cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `SegmentedControl`，支持 icon、meta/count、`div/nav` root、active/option class overrides 和可读 aria label；补 `SegmentedControl.test.tsx` 与 `SegmentedControl.stories.tsx`。Mobile Threads/Runs filter toolbar、Run detail section nav、Run detail logs filter 和 Account language picker 不再各自手写 segmented button 循环，统一消费 shared 控件并继续叠加 `mobileSegmentedToolbar` / `mobileSegmentButton` / `mobileSegmentButtonActive` 或 logs chip class，保持 Desktop-like glass density、无渐变、44px+ 触控。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png`、`mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-settings-readiness-mocked.png` 等 390x844 场景，probe 输出确认 Threads/Runs filter `87x44`、Run detail section nav `74x44`、log filter `62x44`、底栏 52px、`scrollWidth=390`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（17 files / 93 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared RecoveryPanel / Mobile recovery cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `RecoveryPanel` 组件，支持 icon、eyebrow、title、description、meta、primary/secondary action、busy label 和 consuming-app class overrides；补 `RecoveryPanel.test.tsx` 与 `RecoveryPanel.stories.tsx`，并在 barrel export 暴露类型。`app/mobile/src/components/MobileRecoveryPanel.tsx` 现在只负责 Mobile i18n/retry/action wiring，实际 DOM 和按钮状态由 shared `RecoveryPanel` 渲染，继续叠加 `mobileRecoveryPanel` / `mobileActionButton` 等玻璃触控类，避免恢复态继续散落成 Mobile-only 结构。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `mobile-design-chat-recovery-mocked-dark.png`、`mobile-design-light-chat-recovery-mocked.png` 等 390x844 recovery 场景，probe 输出确认 `scrollWidth=390`，Retry/Threads 分别为 `168x44`，底栏为 52px。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（16 files / 90 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Mobile queue empty-state shared cleanup：本轮继续只整理 `app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。在 shared `EmptyState` 已落地后，Mobile Threads/Runs 队列的无数据态和筛选无结果态不再手写 `mobileCenterState` DOM，改为直接消费 `@agenthub/shared/ui` `EmptyState`，并通过 `mobileCenterState` / `mobileEmptyIcon` / `mobileActionButton` class 覆盖保持 Desktop glass + 44px 移动触控表现；loading 状态仍保留本地 status block，因为它不是 empty-state 组件职责。截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，`mobile-design-light-threads-empty-filter-mocked.png` 与 `mobile-design-light-runs-empty-filter-mocked.png` 均为 `scrollWidth=390`，`Show all` 为 `114x44`，底栏为 52px。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`app && corepack.cmd pnpm --filter @agenthub/shared test`（15 files / 86 tests）、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared EmptyState / Web-Mobile empty surface cleanup：本轮继续只整理 `app/shared/**`、`app/web/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。新增 `@agenthub/shared/ui` 的 `EmptyState` 组件，支持 icon、primary action、suggestion chips、`titleLevel` 和 consuming-app class overrides；补 `EmptyState.test.tsx` 与 `EmptyState.stories.tsx`。Web 删除本地 `components/EmptyState.tsx` / `.module.css`，Chat empty prompt 和 ThreadPanel first-thread 空态改为消费 shared `EmptyState`；Mobile 删除 `MobileEmptyState.tsx`，Chat root 空态改为 shared `EmptyState` + `mobileEmptyView` / `mobileEmptyAction` 玻璃触控类，保留 44px Browse threads CTA。Mobile 截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，`mobile-design-chat-empty-cta-mocked-dark.png` / `mobile-design-chat-tab-root-mocked-dark.png` 等 390x844 场景确认 `scrollWidth=390`、Browse threads `144x44`、底栏 52px。Web 共享空态回归由 5175 preview 的 `corepack.cmd pnpm --filter agenthub-web visual:qa` 覆盖 8 个 Desktop/Web/Mobile shell 场景，截图写入 `app/web/screenshots/`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（15 files / 86 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`corepack.cmd pnpm --filter agenthub-web visual:qa`、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared StatusBadge / Mobile queue cleanup：本轮继续只整理 `app/shared/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。`@agenthub/shared/components` 的 `StatusBadge` 新增 consuming-app `label` 覆盖和稳定 `getStatusVariantClassName()`，补 `StatusBadge.test.tsx` 覆盖默认英文、本地化中文 label、`in progress` class suffix；Mobile 删除本地 `MobileStatusBadge.tsx`，Threads/Runs queue rows 改为直接消费 shared `StatusBadge`，但继续叠加 `mobileQueueStatusBadge-*` 玻璃状态类，避免回到彩条或渐变状态表达。Mobile 截图证据由 `app/mobile && $env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa` 生成，覆盖 `app/mobile/screenshots/mobile-design-bottom-nav-badges-mocked-dark.png`、`mobile-design-runs-after-rejection-return-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png` 等 390x844 dark/light 场景，probe 输出确认 `scrollWidth=390`、底栏 52px、列表行 370px、关键按钮 44px+。Web 共享组件回归由 5175 preview 的 `corepack.cmd pnpm --filter agenthub-web visual:qa` 覆盖 8 个 Desktop/Web/Mobile shell 场景，截图写入 `app/web/screenshots/`。验证通过：`app && corepack.cmd pnpm --filter @agenthub/shared test`（14 files / 82 tests）、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅既有 large chunk warning）、`corepack.cmd pnpm --filter agenthub-web visual:qa`、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile app/shared app/web docs/handoff/STATE.md`。

> 2026-05-30 Shared TokenDanceMark component cleanup：本轮继续只整理 `app/shared/**`、`app/web/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。在前序 shared SVG 资产基础上新增 `@agenthub/shared/ui` / `@shared/ui` 的 `TokenDanceMark` 组件，Web shell、Web Auth sheet/Login action 和 Mobile Account identity panel 不再各自手写 `<img src=...>`，而是复用同一个 shared UI 契约；组件支持默认 `alt="TokenDance"` 和 decorative `alt="" aria-hidden="true"` 两种使用方式。验证通过：`app/shared && corepack.cmd pnpm test`（13 files / 79 tests）、`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`、`corepack.cmd pnpm --filter agenthub-web visual:qa`、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web app/mobile app/shared docs/handoff/STATE.md`。

> 2026-05-30 Shared TokenDance brand asset cleanup：本轮只整理 `app/shared/**`、`app/web/**`、`app/mobile/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。Web shell、Web Auth sheet/Login action 和 Mobile Account identity panel 现在都引用 `app/shared/src/assets/tokendance-icon-rounded.svg`；删除 Web/Mobile app-local 重复 SVG，避免后续品牌图标靠复制维护。Web `visual:qa` 继续断言 shell TokenDance logo 存在且无 `AH` 文本 fallback，Mobile `visual:qa` 继续断言 Account identity panel 渲染 TokenDance logo 与三枚 identity chips。验证通过：`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`、`corepack.cmd pnpm --filter agenthub-web visual:qa`、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web app/mobile app/shared docs/handoff/STATE.md`。

> 2026-05-30 Web shell brand mark cleanup：本轮只整理 `app/web/**` 与本 handoff，不 stage Desktop/Mobile/Edge/roadmap/reference 改动。Web 真实入口 `src/layouts/WebLayout.tsx` 的顶栏品牌从旧 `AH` 文本方块改为共享 TokenDance rounded icon asset，样式保持 Desktop command-center 的 28px 小半径 glass mark，不新增渐变卡片或左 rail。`visual:qa` 已新增 shell brand 断言：所有覆盖场景必须存在 `header img[class*='brandMark']`，且 `header [class*='brandMark']` 不得再有文本 fallback；这会阻断 Web shell 回退到旧占位品牌。验证通过：`app && corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`、`corepack.cmd pnpm --filter agenthub-web visual:qa`、Web/Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web docs/handoff/STATE.md`。

> 2026-05-30 Mobile Account identity closeout：本轮只整理 `app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。Account 首屏从普通 Settings card 收敛为 Desktop/Web 风格的 TokenDance identity glass panel：新增 TokenDance rounded mark、`AgentHub Mobile` identity copy、TokenDance ID / Secure store / Native action 三枚紧凑 chip，并把 Sign in / Check session / Clear 保持为首屏 44px 行动按钮；Runtime readiness rows、语言、通知、client surfaces 和 clear-session sheet 继续保留在 Account 下。`visual:qa` 的 Account readiness 断言已扩展为校验 TokenDance logo、三枚 identity chips、`AgentHub Mobile` 与 `TokenDance ID` 文案，截图证据为 `app/mobile/screenshots/mobile-design-settings-readiness-mocked-dark.png` 与 `app/mobile/screenshots/mobile-design-light-settings-readiness-mocked.png`，人工复查确认 390px dark viewport 下三枚 chip 不裁切、首屏内容不空。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`node --check scripts/visual-qa.mjs`、`$env:MOBILE_QA_URL='http://127.0.0.1:5174/'; corepack.cmd pnpm visual:qa`、Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile docs/handoff/STATE.md`。

> 2026-05-30 Web visual QA gate closeout：本轮只整理 `app/web/**` 与本 handoff，不 stage Desktop/Edge/roadmap/reference 改动。Web 继续按 Desktop command-center glass shell 收敛，不新增独立 Web 装饰布局；新增 `app/web/scripts/visual-qa.mjs` 与 `agenthub-web` 的 `visual:qa` 命令，默认验收 `http://127.0.0.1:5175/`，context 级 mock Hub REST，阻断 service worker，覆盖 desktop Workbench、390px Workbench/Messages/Settings zh/Run overlay/Account sheet/Agent Square route bridge/Project route bridge light。脚本写入 `app/web/screenshots/web-design-*.png` 与 `.probe.json`（目录继续 gitignored），并断言 `scrollWidth<=innerWidth`、移动端可见 controls 44px+、Account 是移动底栏最右身份入口、390px 顶栏无重复 Account 按钮、Account sheet bottom-docked + glass blur、TokenDance brand logo/identity icon 渲染、无 raw i18n keys、无 gradients、无 left-only border rails、无 inset left rails、console warn/error 为空（本地 mock 下 Hub WebSocket 握手失败被视为预期网络噪声）。Web Auth surface 继续从旧 `AH` 占位和硬编码浅色按钮收敛到 Desktop TokenDance 图标 + dense glass identity button。验证通过：`app/web && node --check scripts/visual-qa.mjs`、`app && corepack.cmd pnpm --filter agenthub-web visual:qa`、`corepack.cmd pnpm --filter agenthub-web typecheck`、`corepack.cmd pnpm --filter agenthub-web build`（仅 Vite large chunk warning）、Web/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/web docs/handoff/STATE.md`。内置浏览器控制层本轮仍在连接阶段 120s timeout，因此视觉验收证据采用同一 5175 Web preview 的 Playwright 截图/probe。

> 2026-05-30 Desktop TeamRun communication graph cleanup：本轮只整理 Desktop 本地切片，不推 `master`，不 stage Mobile/Web/Edge/Tauri `gen/`/`.tmp`/`stats.html`。`SettingsPage` 的 AgentTeam Console 新增真实 TeamRun 通信图投影，数据只来自 Hub 返回的 `TeamRunState.members/tasks/assignments/route_log/run_events/artifacts/conflicts`，用于展示 supervisor/member、TeamTask、Edge run、artifact 和 conflict 之间的关系；没有新增假 remote/cloud 执行，也没有绕过现有 Hub/Edge contract。样式按 Desktop dense glass surface 收敛为紧凑节点 + 关系边，不使用厚卡片白描边，窄屏下单列无横向溢出。视觉证据使用 `localhost:5173` Playwright fallback（内置浏览器控制层本轮仍 120s timeout）：截图 `app/desktop/.tmp/desktop-agentteam-communication-graph-pw.png`，probe `app/desktop/.tmp/desktop-agentteam-communication-graph-probe.json` 断言 graph visible、`nodeCount=11`、`edgeCount=11`、delegate/runtime/artifact 均存在、`scrollWidth=innerWidth=924`、console warn/error 为空。验证通过：`app/desktop && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec vitest run src\__tests__\SettingsPage.test.tsx --reporter=dot`（26 passed，保留既有 React act warning）、`corepack.cmd pnpm exec playwright test agent-scheduling-visual.spec.ts --config=.tmp\playwright-agent-scheduling.config.ts --reporter=line`（1 passed）。

> 2026-05-30 Mobile visual QA / run detail closeout：本轮只整理 `app/mobile/**` 与本 handoff，不 stage Desktop/Web/Edge/roadmap/reference 改动。Mobile 继续按 Desktop glass command-center 收敛：Hub base URL 修正为 `https://hub.vectorcontrol.tech`（shared client 继续追加 `/v1`），BottomNav 的 active Threads / pending review Runs badge 接入真实 React Query 缓存，Chat 不再丢弃 approval/diff/code/file thread items，timeline sync 失败时 composer 暂停且保留 Retry/Threads 恢复路径，发送回复增加 pending/error/retry/success 本地状态和 44px Copy/Retry controls。Run detail 从轻量日志页扩展为 Mobile 审阅面：Review/Diff/Blocks/Outputs/Logs summary + sticky section nav、approval/rejection bottom sheet、decision lock、resources detail sheet、logs filter、审批成功后乐观更新 `runs` cache 并刷新 pending badge。`visual:qa` 现在由 `app/mobile/package.json` 入口运行，context 级 mock 当前 `hub.vectorcontrol.tech` 与旧 `api.hub.vectorcontrol.tech`，阻断 service worker，覆盖 dark/light、zh/en、Threads/Runs/Chat/Account recovery、approval/rejection pending/error/retry/success、post-decision queue return、resource copy/detail、logs、empty filters，并在最终指标中检查 `scrollWidth=390/innerWidth=390`、所有按钮 44px+、unexpected console output 为空。验证通过：`app/mobile && node --check scripts/visual-qa.mjs`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`$env:MOBILE_QA_URL='http://127.0.0.1:5184/'; corepack.cmd pnpm visual:qa`、Mobile/Shared 渐变/left-rail/particle 源扫描零结果、`git diff --check -- app/mobile docs/handoff/STATE.md`。截图证据仍保存在 `app/mobile/screenshots/mobile-design-*.png`，不强制入仓。

> 2026-05-30 Desktop workspace target cleanup：本轮只整理 Desktop 本地切片，不推 `master`，不 stage Mobile/Web/Edge/Tauri `gen/`/`.tmp`。`PromptInput` 的工作区和环境菜单改为真实本地闭环：手动目录、Tauri 目录选择和本地 target 都只写入 `/v1/runs` 的 `run.workDir`；最近工作区持久化到 `agenthub.prompt.recentWorkDirs`，最多 6 条，可一键清除；remote SSH / cloud / relay target 只作为禁用库存展示，明确当前不会发送 `targetId` 假调度。视觉证据使用 `localhost:5173` Playwright fallback（内置浏览器控制层前序仍出现 120s timeout）：截图 `app/desktop/.tmp/desktop-workspace-target-menu-recent.png`、`app/desktop/.tmp/desktop-workspace-target-after-select.png`；probe `app/desktop/.tmp/desktop-workspace-target-probe.json` 断言点击 Codex 后打开 composer、选择 `D:\Code\TokenDance\tokendance-gateway`、发送真实 `POST /v1/runs`，body 含 `workDir=D:\Code\TokenDance\tokendance-gateway` 且无 `targetId/target_id`，`scrollWidth=innerWidth=924`，console warn/error 为空。验证通过：`app/desktop && corepack.cmd pnpm exec playwright test workspace-target-menu.spec.ts --config=.tmp\playwright-agent-scheduling.config.ts --reporter=line`（1 passed）、`corepack.cmd pnpm exec vitest run src\__tests__\PromptInput.test.tsx --reporter=dot`（41 passed，保留既有 React act warning）、`corepack.cmd pnpm typecheck`。

> 2026-05-29 Desktop local orchestration commit cleanup：本轮只整理 Desktop 本地工作，不推 `master`，不碰 Mobile/Web/Edge 源码。修复 Home 的“本地编排”入口只选中 runtime、不进入可执行聊天的问题：`HomeDashboard` 的本地 Orchestrator action 现在通过 shell 回调切回 Agent 聊天、创建/恢复真实 Edge thread，并把编排草稿可靠填入 composer；同一 pending draft 机制替换 fork thread 里的固定 `setTimeout`，避免跨视图时草稿事件丢失。真实客户端 viewport 验收使用 `localhost:5173` 的 Playwright fallback（内置浏览器控制层仍 120s timeout）：截图 `app/desktop/.tmp/desktop-local-orchestration-home-before.png`、`app/desktop/.tmp/desktop-local-orchestration-draft-after.png`；probe `app/desktop/.tmp/desktop-local-orchestration-probe.json` 断言点击后 `POST /v1/threads` 创建 `Orchestrator` thread、Home action 不再可见、textarea 包含本地编排/subagent/Runtime 草稿、`scrollWidth=innerWidth=924`、console warn/error 为空。验证通过：`app/desktop && corepack.cmd pnpm exec vitest run src\__tests__\HomeDashboard.test.tsx --reporter=dot`（18 passed）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/local-orchestration-visual.spec.ts --config=.tmp/playwright-agent-scheduling.config.ts --reporter=line`（1 passed）。剩余风险：当前工作树仍保留其他 agent 的 Mobile/Web/Edge 改动和若干 Desktop 生成物；提交前只 stage Desktop 源码/测试/必要依赖与本条 handoff，不 stage `.tmp`、`stats.html` 或 Tauri `gen/` schema。
> 补充提交前验证：`corepack.cmd pnpm build` 通过（仅 Tauri dynamic import / large chunk warning），`app/desktop/src-tauri && cargo check` 通过（仅 `auto_restart` dead_code warning），分组 Vitest 通过 10 files / 87 tests、8 files / 130 tests、7 files / 114 tests；`git diff --cached --check` 通过，暂存路径 guard 确认没有 `app/mobile`、`app/web`、`edge-server`、`hub-server`、`.tmp`、`stats.html`、`src-tauri/gen`。未作为通过项声明：`corepack.cmd pnpm test` 当前仍受 `integration/edge-real.test.ts` 的 `/v1/runs` 409/404、WebSocket origin 与 Vitest worker OOM 影响失败；排除 edge-real 的 `test:ci` 在 4GB/8GB 下仍 worker OOM，未观察到断言失败但不能视为全量通过。

> 2026-05-29 Mobile Account IA source cleanup：本轮继续不写 `app/desktop/**`。Mobile 右下角底栏已经对外显示 Account，但内部 route/component 仍叫 `settings` / `SettingsView`，Threads/Runs recovery 二级动作也仍显示 Settings；现在把 `MobileView` 的第四个 tab 改为 `account`，`SettingsView.tsx` 重命名为 `AccountView.tsx`，BottomNav 第四项继续走 shared `mobile.account` surface metadata，Threads/Runs recovery 的 secondary action 改为 Account + user icon，README Source Layout 同步为 AccountView。Playwright 390x844 dark viewport 截图：`app/mobile/screenshots/mobile-account-ia-pw-390x844.png`、`app/mobile/screenshots/mobile-runs-recovery-account-pw-390x844.png`；probe：`app/mobile/screenshots/mobile-account-ia-pw-390x844.probe.json`，断言 Account/Runs recovery 两个状态均 `innerWidth=390`、`scrollWidth=390`、`navLabels=[Threads,Chat,Runs,Account]`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`。验证通过：`app/mobile && corepack.cmd pnpm exec playwright test .tmp-mobile-account-ia.spec.ts --reporter=line`（spec 已清理）和 `corepack.cmd pnpm typecheck`；build/diff check 见本轮提交前验证。

> 2026-05-29 Desktop weak-border polish / local commit boundary：本轮只改 Desktop UI 视觉层和 Desktop QA 文档，不碰 Mobile/Web/Edge。按用户 Codex 对照图继续收敛顶栏菜单、用户消息气泡、composer 和侧栏行项目：顶栏菜单 panel 去掉白色描边，暗色态为 `rgba(26,26,32,.96)` + `rgba(0,0,0,.16)` 发丝边 + 低黑影；用户消息气泡边界从明显黑框压到实际渲染约 `rgba(0,0,0,.067)`，阴影压到 `0 8px 20px rgba(0,0,0,.043)` + `0 1px 3px rgba(0,0,0,.055)`；composer 边界同样压到实际渲染约 `rgba(0,0,0,.067)`，保留轻微底部层级；Agent/Thread 行继续保持 32-34px 高、13px 文案、无厚卡片白描边。截图证据：`.tmp/desktop-sidebar-density-home.png`、`.tmp/desktop-top-menu-weak-border.png`、`.tmp/desktop-weak-border-composer-typed.png`、`.tmp/desktop-weak-border-chat-after-send.png`、`.tmp/desktop-weak-shadow-readonly.png`；probe：`.tmp/desktop-sidebar-density-probe.json`、`.tmp/desktop-top-menu-weak-border-probe.json`、`.tmp/desktop-weak-border-probe.json`、`.tmp/desktop-weak-shadow-readonly-probe.json`，断言无横向溢出、无 `No file chosen`、顶栏菜单无白描边、用户气泡/发送框边界与阴影均降到弱层级。内置浏览器控制层本轮仍有 120s timeout，因此截图验收用同一 `localhost:5173` 客户端的 Playwright viewport。验证待本地提交前运行：focused Vitest、typecheck、定向 diff check。

> 2026-05-29 Desktop Codex polish micro-adjustment：本轮只改 Desktop。对照用户给的 Codex composer 截图继续微调字体大小、颜色、字重、对齐和 icon 视觉比例：暗色 `--foreground` 从前序 `#c2c3ca` 再降到 `#b8bac2`，`--muted-foreground=#898b95`，弱文本 `#777984/#565963`；composer 输入文字降到 foreground/muted 68/32，胶囊背景 `rgba(45,45,54,.88)`、黑色边缘 `rgba(0,0,0,.46)`、高度约 92px；权限、工作区、模型触发器统一为 32px 高低对比圆胶囊，字重 430，icon stroke 约 1.85；模型下拉压到 30px 行高、12px/13.5px 文案、黑色边缘，去掉厚重卡片感；Home stat/AgentTeam 卡片统一 12px radius、冷灰底和更低字重，Chat 思考/Token/AgentTeam block 中 650-760 的粗字重降到 520/560。截图证据：`.tmp/desktop-codex-polish-home-live.png`、`.tmp/desktop-codex-polish-composer-live.png`、`.tmp/desktop-codex-polish-model-popover-live.png`、`.tmp/desktop-codex-polish-light-home-live.png`；probe：`.tmp/desktop-codex-polish-probe.json`，关键值 `body color=rgb(184,186,194)`、textarea `15.25px/400`、composer `rgba(45,45,54,0.88)/26px/92px`、model popover `rgba(29,30,37,0.97)/12px/292px`、默认权限/本地 Edge/model trigger 均 `32px` 高、`13px/430`、light stat card `rgb(255,255,255)` + `rgba(0,0,0,.06)`。验证通过：`app/desktop && corepack.cmd pnpm exec playwright test desktop-codex-polish-live.spec.ts --config .tmp\playwright-agent-scheduling.config.ts --reporter=line`（1 passed）、`desktop-microtune-live.spec.ts`（1 passed）、`corepack.cmd pnpm exec vitest run src\__tests__\ChatView.test.tsx --reporter=dot`（21 passed）、`corepack.cmd pnpm exec vitest run src\__tests__\PromptInput.test.tsx --reporter=dot`（39 passed，既有 React act warning）、`corepack.cmd pnpm typecheck`。

> 2026-05-29 Desktop Codex-like microtune comparison pass：本轮只改 Desktop。继续按 Codex 参考图做字体大小、颜色明度、字重、对齐、卡片厚度和 icon 底色微调：暗色 `--foreground` 收到 `#c2c3ca`，`--muted-foreground=#858893`，弱文本降到 `#787b86/#535661`；composer 输入文字降亮到 foreground/muted 76/24，面板边缘改黑色层级而非白描边，圆角 26px；模型/权限/工作区下拉背景统一为冷灰黑面板，row/label 字重降到 460-560；sidebar Agent/Thread 名称和图标底色减轻，Home stat/AgentTeam 卡片改为冷灰 rgba + 黑色阴影，修掉 `color-mix(... transparent)` 带出的暖色偏色。浅色主题也用同一脚本复截，确认 Home 卡片没有被黑边污染。截图证据：`.tmp/desktop-microtune-home-live.png`、`.tmp/desktop-microtune-composer-active-live.png`、`.tmp/desktop-microtune-model-popover-live.png`、`.tmp/desktop-microtune-home-light-live.png`；probe：`.tmp/desktop-microtune-live-probe.json`，关键值 `body color=rgb(194,195,202)`、textarea `15.5px/400`、sidebar title `12px/520`、composer `rgba(44,44,53,0.86)/26px`、model popover `rgba(29,30,37,0.97)/12px`。验证通过 `app/desktop && corepack.cmd pnpm exec playwright test desktop-microtune-live.spec.ts --config .tmp\playwright-agent-scheduling.config.ts --reporter=line`（1 passed）、`corepack.cmd pnpm exec vitest run src\__tests__\ChatView.test.tsx --reporter=dot`（21 passed）、`corepack.cmd pnpm exec vitest run src\__tests__\PromptInput.test.tsx --reporter=dot`（39 passed，既有 React act warning）、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。

> 2026-05-29 Mobile Settings clear-session retry evidence pass：本轮继续不写 `app/desktop/**`。补齐 Clear session bottom sheet 的失败/重试路径证据：浏览器预览中 Tauri native bridge 不可用时，点击 Confirm clear 后 sheet 不关闭，失败文案留在 sheet 内，危险态确认按钮切换为 `Retry clear`，Cancel/Close 仍保留 44px 操作。Playwright 390x844 dark viewport 截图：`app/mobile/screenshots/mobile-settings-clear-retry-pw-390x844.png`；probe `app/mobile/screenshots/mobile-settings-clear-retry-pw-390x844.probe.json` 断言 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、`dialogOpen=true`、sheet `width=390` 且 pinned bottom、`Retry clear=176x44`。验证通过：`app/mobile && corepack.cmd pnpm exec playwright test .tmp-mobile-settings-clear-retry.spec.ts --reporter=line`（spec 已清理）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Mobile Settings clear-session confirmation source pass：本轮继续不写 `app/desktop/**`。Settings 之前已有 `settings.clearSession.*` i18n 和 README 说明，但真实 Clear 按钮仍直接调用原生命令；现在 Clear 先打开底部确认 sheet，再由 Confirm clear 调用 `clearHubAccessToken()`，pending/error 状态保留在 sheet 内，Cancel/关闭都是 44px 操作目标，失败时可在同一危险态按钮上重试。sheet 复用 Mobile glass/bottom safe-area 结构，显示 Storage/Effect 上下文，不引入渐变或左 rail。Playwright 390x844 dark viewport 截图：`app/mobile/screenshots/mobile-settings-clear-sheet-pw-390x844.png`；probe `app/mobile/screenshots/mobile-settings-clear-sheet-pw-390x844.probe.json` 断言 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、sheet `width=390` 且 pinned bottom、关闭/取消/确认按钮均 44px+。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm exec playwright test .tmp-mobile-settings-clear-sheet.spec.ts --reporter=line`（spec 已清理）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Mobile Settings language-control source pass：本轮继续不写 `app/desktop/**`。Settings 里原本已有 i18n 初始化、localStorage 持久化和 `<html lang>` 同步，但真实页面没有语言切换控件；现在 `SettingsView` 直接渲染 English / 简体中文 的 44px segmented control，使用现有 `mobileLanguages` 和 `i18n.changeLanguage()`，并在卡片右侧显示当前语言 badge。样式复用 Mobile glass card / segmented toolbar，不引入渐变、不引入左 rail；文案覆盖 zh/en。Playwright 390x844 dark viewport 截图：`app/mobile/screenshots/mobile-settings-language-en-pw-390x844.png`、`mobile-settings-language-zh-pw-390x844.png`；probe `app/mobile/screenshots/mobile-settings-language-pw-390x844.probe.json` 断言 English -> 简体中文 切换后 `lang=zh-Hans`、`storedLanguage=zh`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、两个语言按钮均 `163x44`。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm exec playwright test .tmp-mobile-settings-language.spec.ts --reporter=line`（spec 已清理）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Web legacy prototype file removal pass：本轮继续不写 `app/desktop/**`。接着 page export convergence，把深层旧原型本体从 Web 源码移除：删除 `app/web/src/pages/{agent-square,group-workspace,private-chats,projects,workbench}/*Page.tsx` 和对应 CSS module，保留顶层 `src/pages/{Workbench,AgentSquare,PrivateChats,GroupWorkspace,Project}.tsx` 作为 `@/App` bridge。`app/web/README.md` 已改为明确 `src/pages/` 只是 legacy URL bridges，真实 UI 是 `App -> WebLayout`，旧能力必须迁入 shell/slot 后再恢复。这样 Web 代码里不再保留第二套装饰性页面体系，也减少旧粒子/渐变/卡片布局被重新接入的风险。截图证据复验：`app/web/screenshots/web-prototype-files-removed-agent-square-pw-390x844.png`。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、旧原型引用扫描确认 `app/web/src` 无 `AgentSquarePage`/`GroupWorkspacePage`/`PrivateChatsPage`/`ProjectPage`/`WorkbenchPage` 引用、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/web app/mobile app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Web legacy page export convergence pass：本轮继续不写 `app/desktop/**`。接着 Web legacy route shell bridge，进一步封掉旧原型页被重新接回的公开入口：`app/web/src/pages/{Workbench,AgentSquare,PrivateChats,GroupWorkspace,Project}.tsx` 现在全部导出 `@/App`，不再 re-export 深层 `src/pages/*/*Page.tsx` 原型组件；`rg` 确认顶层 pages/router 不再引用 `@/pages/agent-square`、`@/pages/group-workspace`、`@/pages/private-chats`、`@/pages/projects`、`@/pages/workbench`。这保证即使后续代码重新 import 顶层 pages，也仍进入统一 `App -> WebLayout` Desktop-like shell，而不是第二套装饰性 UI。视觉证据：`app/web/screenshots/web-page-export-converged-agent-square-pw-390x844.png`，并继续保留 Web legacy route shell bridge 的 390x844 截图和 probe：`web-legacy-agent-square-shell-pw-390x844.png`、`web-legacy-group-shell-pw-390x844.png`、`web-legacy-project-shell-zh-pw-390x844.png`、`web-legacy-shell-routes-pw-390x844.probe.json`。顺手把 route context UI 文案从工程化的 `Shell route/外壳路由`、`Shell/外壳`、`Catalog URL/目录 URL` 收敛为 `Context/上下文`、`Workspace/工作台`、`Catalog/目录`。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果；最终定向 `git diff --check` 另见本轮收尾。

> 2026-05-29 Web legacy route shell bridge pass：本轮继续不写 `app/desktop/**`。为避免 Web 旧原型路由继续成为第二套 UI，`app/web/src/router.tsx` 不再 lazy-load `src/pages/*`，`/`、`/chats`、`/settings`、`/agent-square`、`/group/:id`、`/project/:id` 和 fallback 全部渲染统一 `App -> WebLayout`。`WebLayout` 新增 route context panel：`/agent-square` 显示 Agent catalog workspace，`/group/:id` 显示 Group command workspace，`/project/:id` 显示 Project command workspace；面板使用低亮玻璃、整面边框、密集 metric/action 内容，不使用渐变、不使用左 rail，且 zh/en 文案不再出现 legacy/prototype 这类工程说明。Playwright 390x844 dark viewport 截图：`app/web/screenshots/web-legacy-agent-square-shell-pw-390x844.png`、`web-legacy-group-shell-pw-390x844.png`、`web-legacy-project-shell-zh-pw-390x844.png`；probe `app/web/screenshots/web-legacy-shell-routes-pw-390x844.probe.json` 断言三条 legacy URL 均 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、无旧 preview-shell 文案、`routePanelCount=1`、`mobileNavCount=4`。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、`corepack.cmd pnpm exec playwright test .tmp-web-legacy-shell-routes.spec.ts --reporter=line`（spec 已清理）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/web app/mobile app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Mobile Threads filter/recovery source pass：本轮继续不写 `app/desktop/**`。先修正 `app/mobile/README.md` 与真实源码不一致的问题：README 之前已经描述 Threads 的 All/Active/Archive filter 和 recovery card，但 `ThreadListView` 仍是旧版简单列表；现在 `ThreadListView` 接入 All/Active/Archive 44px segmented toolbar，Archive 视图隐藏 Continue handoff card，活跃视图补 Desktop-like handoff summary，空列表/筛选空结果不再稀疏。Hub/threads 查询失败时统一使用共享 `MobileRecoveryPanel`，区分 Hub reachable 但 workflow unavailable 与 Hub offline，并提供 Retry + Settings 44px action；状态 badge/local workspace/Hub metric 全部走 zh/en i18n，Hub metric 改成短 `OK/Down`、`正常/断开`，避免 390px 截断。Playwright 390x844 dark viewport 截图：`app/mobile/screenshots/mobile-threads-filters-handoff-pw-390x844.png`、`mobile-threads-filter-archived-no-rails-pw-390x844.png`、`mobile-threads-recovery-settings-pw-390x844.png`、`mobile-threads-filter-archived-zh-pw-390x844.png`；probe `app/mobile/screenshots/mobile-threads-filters-recovery-pw-390x844.probe.json` 断言 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、segmented buttons `87x44`、recovery actions `168x44`。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、临时 Playwright spec `corepack.cmd pnpm exec playwright test .tmp-mobile-threads-filters.spec.ts --reporter=line`（spec 已清理）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Web Messages i18n / filled surface pass：本轮继续不写 `app/desktop/**`。接着 Web mobile surface-nav 之后，收口真实 `/chats` Messages 路径的登录后组件，而不是维护旧页面原型：`IMContactList` 去掉硬编码英文 Contacts/Add/Search/Online/Offline/empty 文案，统一走 `im.contact.*` zh/en；移动端 Add、Add confirm、search input、contact row 控件补到 44px+，selected contact 继续使用整面 tint + 1px 内描边，不用左侧 rail。`IMView` 的 authenticated no-selection 从单个居中空态改成 Hub timeline header + Sessions/Messages/Agent handoff 三块低亮玻璃状态卡；移动 topbar 隐藏被挤压成 `Agent...` 的 brand text，仅保留 AH mark，避免窄屏乱码感。IAB 连接本轮仍超时，因此用 Playwright CLI 在 `localhost:5175` 390x844 dark viewport mock Hub auth/session 截图：`app/web/screenshots/web-mobile-messages-locked-filled-pw-390x844.png`、`web-mobile-messages-authenticated-filled-pw-390x844.png`、`web-mobile-messages-add-contact-pw-390x844.png`；probe `app/web/screenshots/web-mobile-messages-filled-pw-390x844.probe.json` 断言 `/chats`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、`noSelectionCards=3`、`addButton=44x44`、`searchInput=348x44`。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、跨 Web/Mobile/Shared 渐变/left-rail 源扫描零结果、定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 仅 LF/CRLF warning。

> 2026-05-29 Web mobile surface-nav / empty-fill pass：本轮继续不写 `app/desktop/**`。Web 真实入口仍是 `app/web/src/App.tsx -> layouts/WebLayout.tsx`，但窄屏此前隐藏 Workbench/Messages segmented tabs，导致手机 Web 没有清晰 tab-root；已在 `WebLayout` 增加移动底部 surface nav，直接复用 Desktop slot shell 的 Workbench、Messages、Settings、Run detail 状态，点击会同步 `/`、`/chats`、`/settings` history 或 run overlay，不启用旧 `src/pages/*` prototype。补齐 `webShell.surface.runDetail` zh/en i18n。Messages 未登录态不再是一整块空玻璃，`IMView` 增加 Hub session locked / Desktop shell / Realtime paused 三块状态说明和 Sign in 44px action；RunDetail 无 active run 时显示 Run Detail 标题和三条 no-active-run/no-output/no-sources 状态卡，避免移动 run overlay 空白。IAB 连接仍超时，因此用 Playwright CLI 在 `localhost:5175` 390x844 dark viewport 复验：`app/web/screenshots/web-mobile-surface-nav-workspace-pw-390x844.png`、`web-mobile-surface-nav-messages-pw-390x844.png`、`web-mobile-surface-nav-settings-pw-390x844.png`、`web-mobile-surface-nav-run-overlay-pw-390x844.png`，probe `app/web/screenshots/web-mobile-surface-nav-pw-390x844.probe.json` 断言四个 surface 均 `scrollWidth=390`、底部 nav visible、四个 tab 均 90.5x53、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargetCount=0`、无 raw i18n key。

> 2026-05-29 Desktop Codex-like microtune continuation：本轮只改 Desktop。继续按用户要求对照 Codex 低亮度暗色界面微调：暗色 `--foreground` 从前序偏亮值压到 `#c9cad1`，muted/text-weaker 同步降亮；Chat 正文从 16px 降到 15.5px，用户气泡去掉内高光、边框降到 `rgba(255,255,255,0.052)`，移动窄屏聊天横向 padding 从 8px 级别修到 16px；短会话 `.messageList` 改为 flex 填满聊天可用区并 `justify-content:flex-end`，最新回复不再顶在上方并在其后留下大块空白。Composer/权限/工作区/模型触发器降到 13.5px/420；全局 anti-zoom 输入规则从 `max-width:767px` 收窄到 `pointer: coarse`，避免 Desktop 窄窗口把 textarea 强制回 16px；Home 统计卡片压低字号和字重、卡片半径统一到 10px；顺手修复 `components/settings/utils.ts` 残留 raw key `settings.status.localSource` 为 `settings.statusLocalSource`。IAB pipe 本轮不可用，仅暴露 Chrome extension 且截图调用超时，因此使用 Playwright 客户端 viewport 截图验收：`.tmp/desktop-home-visual-after-pw.png`、`.tmp/desktop-chat-final-stable-after-pw.png`、`.tmp/desktop-model-picker-after-pw.png`；probe：`.tmp/desktop-home-visual-after-probe.json`、`.tmp/desktop-chat-final-stable-after-probe.json`、`.tmp/desktop-model-picker-after-probe.json`，断言 `overflow=false`、Home title `13px/500`、stat value `22px/560`、Chat list `display=flex/flex=1 0 auto/justify=flex-end`、last message bottom 721 <= composer top 809、textarea `15.5px/400`、model trigger `13.5px/420`，无 `settings.status.localSource`。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx --reporter=dot`（26 tests，既有 `act(...)` warning）、`corepack.cmd pnpm typecheck`；`rg -n "settings\.status\.localSource|status\.localSource" app/desktop/src` 无结果。

> 2026-05-29 Mobile Runs no-rail status pass：本轮继续不写 `app/desktop/**`。Mobile Runs 队列从旧状态色左 rail 语义收敛为整面玻璃 tint + balanced 1px border + icon/badge 状态表达；`.mobileScroll` 增加底栏安全滚动空间，`mobileAppMain` 固定为 overflow-hidden flex/grid 容器，避免最后一条 run row 被 bottom nav 压住。IAB 连接本轮超时，因此用 Playwright CLI 在 `localhost:5174` 390x844 dark viewport mock Hub health/runs 复验并截图：`app/mobile/screenshots/mobile-runs-status-no-rails-pw-390x844.png`，probe `app/mobile/screenshots/mobile-runs-status-no-rails-pw-390x844.probe.json` 断言 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargetCount=0`、无 raw i18n key、四种状态 row 均为 370x86 且左右 border 均为 1px、`lastRunClearsBottomNav=true`。源扫描 `rg -n "rail|stripe|border-left|inset [0-9.]+px 0 0|linear-gradient|radial-gradient|conic-gradient" app\mobile\src -g "*.css" -g "*.tsx" -g "*.ts"` 与跨 Web/Mobile/Shared 渐变/particle/left-rail 扫描均为零结果。验证补跑 `app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build` 和定向 `git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`。

> 2026-05-29 Desktop Settings 窄窗口导航收敛：本轮只改 Desktop。内置浏览器进入 Settings 后发现 726px 宽窗口下 settings nav 是 3000px 横向 chip rail，虽然无全局 overflow，但视觉上像调试滚动条且无搜索空态。已将窄窗口 Settings nav chip 收敛到 34px 高、13px/540（active 580），隐藏突兀横向 scrollbar，保留横向滚动可达；settings 搜索无匹配时不再空白，显示 `没有匹配的设置` role=status。顺手修复 split settings sections 中过时的 `settings.status.localSource` i18n key，统一为 `settings.statusLocalSource`，避免后续接回真实 Settings route 时出现 raw key。IAB 截图：`.tmp/desktop-settings-nav-after-iab.png`、`.tmp/desktop-settings-search-empty-iab.png`；probe：`.tmp/desktop-settings-nav-after-probe.json`、`.tmp/desktop-settings-search-empty-probe.json`，断言 `globalOverflow=false`、`docScrollWidth=726`、nav chip `h=34/fontSize=13px/fontWeight=540-580`、空搜索 `navButtonCount=0` 且 `emptyText=没有匹配的设置`、无 raw i18n key。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx --reporter=dot`（26 tests，既有 `act(...)` warning）、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。

> 2026-05-29 Web shell route/i18n continuation：本轮继续不写 `app/desktop/**`。在 `app/web/src/layouts/WebLayout.tsx` 上把真实 Web 入口推进为 Desktop-like shell URL 状态：`/` 映射 Workbench，`/chats` 映射 Messages，`/settings` 映射 Settings；top tab/Settings 按钮点击会 `history.pushState`，浏览器 `popstate` 会恢复 `mainSurface`，避免旧 `router.tsx` 原型继续成为悬空事实源。同步把 Web shell topbar/aria/status/account/run-detail 文案收进 `app/web/src/i18n/locales/{zh,en}/common.json`，补 `webChat.startFailed` 中英文 fallback；Settings 窄屏返回按钮、横向 nav chip、switch、select、segmented button 补 44px 移动触摸尺寸。内置浏览器截图/探针：`app/web/screenshots/web-shell-messages-route-iab-390x844.png`、`app/web/screenshots/web-shell-messages-route-iab-390x844.probe.json`、`app/web/screenshots/web-shell-settings-route-iab-390x844.png`、`app/web/screenshots/web-shell-settings-route-iab-390x844.probe.json`；断言 `/chats` 选中 tab=`消息`，`/settings` 路径保持 Settings，二者均 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`coloredLeftRailCount=0`、`smallTargetCount=0`、无 raw i18n key。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）；源扫描 `Particle|Particles|particles|canvasRef|projectParticles|pc-particles|linear-gradient|radial-gradient|conic-gradient|border-left:` 对 `app\web\src app\mobile\src app\shared\src` 仍为零结果。

> 2026-05-29 Web no-left-rail shell pass：本轮继续不写 `app/desktop/**`。清掉 Web shell 核心列表仍残留的左侧彩条式选中态：`app/web/src/components/AgentList.module.css`、`ThreadPanel.module.css`、`components/IM/IMContactList.module.css` 和 legacy `pages/projects/ProjectPage.tsx` 不再使用 `box-shadow: inset Npx 0 0 ...` 做 active/selected rail，改成整面玻璃 tint + 全边框/内描边。源扫描 `rg -n "inset [0-9.]+px 0 0|border-left:|linear-gradient|radial-gradient|conic-gradient|Particle|Particles|particles|canvasRef|projectParticles|pc-particles" app\web\src app\mobile\src app\shared\src -g "*.css" -g "*.tsx" -g "*.ts"` 返回零结果。IAB 本轮 pipe 不可用，只剩 Chrome extension，截图用 Playwright CLI 固定 390x844 复验：`app/web/screenshots/web-shell-messages-no-rails-pw-390x844.png`，probe `app/web/screenshots/web-shell-messages-no-rails-pw-390x844.probe.json` 断言 `/chats`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargetCount=0`、无 raw i18n key。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、`corepack.cmd pnpm exec playwright test .tmp-no-rails.spec.ts --reporter=line`（临时 spec 已删除）。

> 2026-05-29 Desktop 全局搜索与 Codex-like 视觉微调：本轮只改 Desktop 与 Desktop handoff/SOP。Sidebar/窄窗口 toolbar 的搜索入口不再是 no-op，也不再宣称“联系人”：`SearchDialog` 现在支持初始 query、统一搜索 threads + messages、选择 thread 会切换真实会话，选择 message 会滚到对应 `[data-message-id]`；窄窗口 IAB 下新增真实搜索按钮。内置浏览器证据：`.tmp/desktop-global-search-dialog-iab.png`、`.tmp/desktop-global-search-select-iab.png`，probe `.tmp/desktop-global-search-dialog-probe.json` / `.tmp/desktop-global-search-select-probe.json` 断言 dialog visible、query 保留、`optionCount=8`、有 thread result、无 contacts 文案、无原生 file input 文案、选择后 dialog 关闭且 thread 可见。按用户最新“对照 Codex 微调字色/大小/对齐/icon”继续压低暗色主题明度：`--foreground=#d2d3d8`、`--muted-foreground=#8f919b`、`--text-weaker=#858893`，composer/input/user bubble 边缘降到低对比，PromptInput/权限/工作区/模型控件字重收敛到 430-440，模型名与推理强度间距为 8px，模型下拉 section label 改为 i18n `推理强度 / 模型路由`，无硬编码 `REASONING / MODEL ROUTE`。IAB 截图：`.tmp/desktop-visual-microtune-before-iab.png`、`.tmp/desktop-visual-microtune-after-iab.png`、`.tmp/desktop-visual-microtune-model-picker-localized-iab.png`；probe：`.tmp/desktop-visual-microtune-before-probe.json`、`.tmp/desktop-visual-microtune-after-probe.json`、`.tmp/desktop-visual-microtune-model-picker-localized-probe.json`，断言 composer `h=88/radius=24px`、model/reason gap `8`、`currentRows=1`、无 `newapi`、无 `ClaudeClaude`、中文下拉 label 可见、console error 为 0。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/SearchDialog.test.tsx src/__tests__/PromptInput.test.tsx --reporter=dot`（44 tests，既有 `act(...)` warning）、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。

> 2026-05-29 Mobile/Web Desktop-aligned continuation：本轮未写入 `app/desktop/**`，只收口 `app/web/**`、`app/mobile/README.md`、`app/web/README.md`、`docs/handoff/STATE.md`。Web 继续按 Desktop command-center shell 收敛：移动端 topbar/account/status/prompt controls 补到 44px 触摸目标，prompt textarea 移动端 `min-height=44px`；清掉 Web 旧原型页和未使用组件里的粒子 canvas/particle CSS，避免 Agent Square、Private Chats、Project、legacy Workbench 重新接回路由时带回装饰背景；保留 `src/App.tsx -> layouts/WebLayout.tsx` 作为真实 Web 入口，`router.tsx` / `src/pages/*` 记录为 legacy prototypes，后续应把能力迁入 Desktop-like slot shell，而不是用旧原型覆盖当前 shell。源扫描 `rg -n "Particle|Particles|particles|canvasRef|projectParticles|pc-particles|linear-gradient|radial-gradient|conic-gradient|border-left:" app\web\src app\mobile\src app\shared\src -g "*.css" -g "*.tsx" -g "*.ts"` 返回零结果；Web in-app browser 390x844 最新截图 `app/web/screenshots/web-workbench-desktop-aligned-touch-iab-390x844.png`，probe `app/web/screenshots/web-workbench-desktop-aligned-touch-iab-390x844.probe.json` 断言 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`coloredLeftRailCount=0`、`smallTargetCount=0`、无 raw i18n key。Mobile Settings native/i18n 证据沿用 `app/mobile/screenshots/mobile-desktop-aligned-settings-native-iab-390x844.png`，probe 已验证 `scrollWidth=390`、无渐变/左 rail、无 raw i18n key、`lang=zh-Hans`、标题 `设置` 和原生命令按钮 44px+。验证通过：`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅 Vite 大 chunk warning）、`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md`（仅 LF/CRLF warning）。剩余工作：真正把 legacy Web route 能力迁入 `WebLayout`/slot 架构，补 Desktop 组件级共享，而不是继续维护独立 Web 原型；Mobile 继续 Hub workflow API/session contract、OIDC deep-link、secure store、Android notification permission。

> 2026-05-29 Desktop composer Codex 对齐微调：本轮只改 Desktop。按用户最新参考图继续压低 composer 视觉厚度和文字明度：移除 composer 顶部选中 Agent 胶囊，选中 Agent 仍保留在 placeholder 与发送参数；窄窗口下 composer 从 112px 收敛到 88px，外圆角固定 24px，输入文字为 16px/400，底部 `+`、权限、工作区、模型、发送按钮统一 32px 高并同一基线；模型/权限/工作区控件字重降到 460，底色保持透明，不再像厚卡片。模型下拉按“可见 provider 分组 + 可见模型名”去重，同名 `Claude Sonnet 4.6` / `DeepSeek V4 Pro` 不再重复成多行，选中态只保留一个 `aria-current` 和一个 check，`newapi` 前缀与 `ClaudeClaude` 均不可见。内置浏览器真实截图：`.tmp/desktop-composer-tuning-after-iab.png`、`.tmp/desktop-composer-active-tuning-iab.png`、`.tmp/desktop-composer-model-picker-visible-dedupe-iab.png`、`.tmp/desktop-composer-worktarget-tuning-iab.png`；probe：`.tmp/desktop-composer-tuning-after-probe.json`、`.tmp/desktop-composer-active-tuning-probe.json`、`.tmp/desktop-composer-model-picker-visible-dedupe-probe.json`、`.tmp/desktop-composer-worktarget-tuning-probe.json`，断言 composer `h=88/radius=24px`、按钮 `h=32`、`currentRowCount=1`、`duplicateVisibleRows=[]`、无容器/服务器未接线占位。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/PromptInput.test.tsx --reporter=dot`（39 tests，既有 `act(...)` warning）、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。

> 2026-05-29 Desktop Codex-like typography 收口：本轮只改 Desktop。按用户反馈“字太粗、太白、太晃眼”，将暗色主题主 ink 从 `#e3e4e6` 压到 `#d8d9de`，muted/text-weaker 同步降亮度；`AgentList`、`ThreadPanel`、top menu、workspace header、ChatView 正文/思考过程、PromptInput workspace/model/permission controls 和 `ModelReasoningPicker` 下拉统一降字重，选中态改为低对比浅底 + check，不再用更白粗字强调。内置浏览器真实客户端截图：`.tmp/desktop-soft-typography-final-iab.png`、`.tmp/desktop-soft-model-picker-final-iab.png`；probe：`.tmp/desktop-soft-typography-final-probe.json`、`.tmp/desktop-soft-model-picker-final-probe.json`，断言 `--foreground=#d8d9de`、模型 trigger `fontWeight=480`、模型行 `fontWeight=470`、`hasClaudeClaude=false`、原生 file input 文案不可见、console error 为 0。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/SettingsPage.test.tsx --reporter=dot`（86 tests，仍有既有 `act(...)` warning）、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。测试策略同步：保留 Chat/Prompt/Settings 逻辑测试，视觉细节用 IAB 截图 + computed-style probe 验收，避免为每轮字体/颜色迭代维护脆弱断言。

> 2026-05-29 Mobile/Web Desktop-aligned glass pass：本轮写入 `app/mobile/**`、`app/web/**`、`app/shared/**` 类型兼容补丁、`docs/handoff/STATE.md` 和对应 README，未修改 `app/desktop/**`。按用户要求停止自造 Mobile/Web UI 方向，改为照 Desktop command-center 的密集玻璃面板、低对比边框、文档流聊天和 tab-root 行为收敛；明确移除 Mobile 可见页和 Web 目标页的渐变卡片、colored left rail / `border-left` 状态条。Mobile 修复 `App` tab root：Runs 未选 run 时渲染 `RunListView`，Chat 未选 thread 时渲染空态；BottomNav 切 Chat/Runs 会回到对应 root；ChatView 从旧 Tailwind-like skeleton/TODO 改为 Mobile-native 语义 CSS，Agent 消息文档流、User 右侧紧凑气泡、copy action、context panel 和真实 `createThreadMessage` 发送；`main.tsx` 恢复 `./i18n` 初始化，BottomNav/Threads 使用 i18n 文案。`src/styles/global.css` 改为 Desktop-aligned mobile glass token 与语义类，不再依赖不存在的 Tailwind pipeline。Web 修复 `vite.config.ts` 端口为 5175，避免抢 Mobile 5174；Workbench/Private Chats/Project preview 以及 DiffViewer/RunDetail/PermissionDialog 去除明显渐变和左侧彩条；`i18n/index.ts` 将旧 flat `locales/{zh,en}.json` 并入 common namespace，修复 Web 入口裸 `agent.*` / `welcome.*` / `prompt.*` key。为让 Mobile/Web 可被真实构建验证，最小修复 shared 类型：补 `HubEnvelope` / `HubCustomAgent` / `WorkbenchSectionSource` 导出、`EventClient.onConnection`、`exactOptionalPropertyTypes` 下的 optional 字段构造与 diff line 构造。内置浏览器截图证据：`app/mobile/screenshots/mobile-glass-final-typechecked-threads-iab-390x844.png`、`app/mobile/screenshots/mobile-glass-final-typechecked-runs-iab-390x844.png`、`app/web/screenshots/web-glass-final-typechecked-workbench-iab-1440x920.png`、`app/web/screenshots/web-glass-final-typechecked-workbench-iab-390x844.png`；probe 结果：Mobile 390x844 `gradients=0`、`leftBorders=0`、`scrollWidth=390`、无 raw key；Web 1440/390 `gradients=0`、无 raw `agent/welcome/prompt` key、390 `scrollWidth=390`，Web 剩余 `leftBorders=1` 来自 shell 结构分隔而非彩色卡片 rail。验证通过：`app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`；`app/web && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`（仅大 chunk warning）；`git diff --check -- app/mobile app/web app/shared docs/handoff/STATE.md` 通过（仅 CRLF warning）。

> 2026-05-29 Desktop composer/空态清理：本轮只改 Desktop。按用户反馈移除 Chat 空态里无用的 TeamRun/local orchestration dock，真实 TeamRun 功能仍保留在 Home/Settings；`MainView` 不再接收空态 dock 专用 props，`App` 不再向 main-view 透传这些参数。`PromptInput` 的 composer、工作区按钮、权限按钮、模型按钮、发送按钮统一为 36px 控件高度和 10px 控件圆角，composer / workspace menu / model picker 改为低对比暗边缘 + 黑色阴影，去掉明显白色描边；workspace menu 去掉未接线的容器/服务器占位，只保留当前真实可用的 Local Edge、本地文件夹和已注册本地目标。修复删除 TeamRun dock 后遗漏的 `previewTeamText` 编译错误，route decision 改用通用 inline preview helper。内置浏览器真实截图：`.tmp/desktop-composer-cleanup-iab.png`、`.tmp/desktop-worktarget-cleanup-iab.png`、`.tmp/desktop-modelpicker-cleanup-iab.png`；probe：`.tmp/desktop-composer-cleanup-probe.json`、`.tmp/desktop-composer-visible-buttons-probe.json`、`.tmp/desktop-modelpicker-cleanup-probe.json`。验证通过 `app/desktop && corepack.cmd pnpm typecheck`；定向 `git diff --check` 仅 LF/CRLF warning。

> 2026-05-29 Desktop 最新 `g` 裁切补漏：用户指出当前可见 `g` 仍被挡后，本轮只改 Desktop。根因收敛到 TeamRun dock 的紧凑 chip/action 使用 `inline-flex + overflow` 但缺少文本下沿 headroom；已补 `ChatView.module.css` 中 TeamRun status/metrics/action 的 `line-height` 与下边距，并把 workspace 当前线程标题从 inline style 收敛为 `.workspaceThreadTitle`。内置浏览器重新打开 `http://localhost:5173/?qa=g-visible-fix-1780020000000` 截图 `.tmp/desktop-g-after-iab.png`，裁切图 `.tmp/desktop-g-team-card-after.png`、`.tmp/desktop-g-composer-after.png`、`.tmp/desktop-g-sidebar-runtime-after.png`；probe `.tmp/desktop-g-after-iab-probe.json` 显示 TeamRun dock `本地 Edge` / `可用 Runtime: 3` / `Hub 账号` 等 `lineHeightRatio>=1.55`、`paddingBottom>=3`、`clientHeight<=scrollHeight` 无裁切风险。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/g-descender-current.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`；`git diff --check` 仅 LF/CRLF warning。

> 2026-05-29 Desktop 最新接手：继续处理用户指出的可见 `g` 被挡问题，Home 最近线程 `ago`、`AgentTeam`/`Edge` 小标签、workspace header `AgentHub`、Chat empty title `AgentHub`、composer `@Agent` 占位和 `TokenDance` provider pill 已补 descender/headroom 与宽度；本轮内置浏览器重新打开 `http://localhost:5173/` 并截图 `.tmp/desktop-current-iab-check.png`，DOM probe 显示 `Agent Runtime`、runtime 名称、`Edge v1` 和 Chat empty title 均 `clientHeight==scrollHeight`，无单行 descender 裁切。既有证据还包括 `.tmp/desktop-g-final-after-iab.png`、`.tmp/desktop-g-descender-current-pw.png`、`.tmp/desktop-g-descender-recent-ago-crop-pw.png`、`.tmp/desktop-g-title-after-pw.png`、`.tmp/desktop-g-title-crop-after-pw.png`、`.tmp/desktop-g-title-after-probe.json`。

## 本次后端/运维推进（2026-05-27 凌晨）

- **生产 Hub 真实部署**：服务器禁止 build 后已切换为”本机/CI 构建镜像 → scp tar → `docker load` → `docker compose up -d --no-build --no-deps hub-server`”。生产 compose 改为 `image: ghcr.io/tokendancelab/agenthub-hub:latest`、`pull_policy: never`、`127.0.0.1:8090:8080`，保留 `dns: 127.0.0.11` 运维补丁。
- **部署证据**：生产服务器当前 `agenthub-hub` 运行 image `sha256:919673032e59...`，Docker health `healthy`，此前 `GET http://127.0.0.1:8090/health` 与公网 `http://api.hub.vectorcontrol.tech/health` 均返回 `status=ok`；2026-05-27 复查公网 health 返回 `status=ok`、`migrations=37`，`https://api.hub.vectorcontrol.tech/health` 仍无可用 TLS。未登录访问 `GET /web/agent-teams/{id}/runs/{run_id}/state` 返回 401，证明 state route 已注册且仍受 Hub session 保护。
- **资源控制**：服务器构建误触发后已清理 `deployments-hub-server:latest`、悬空层、`golang:1.25-alpine`、`alpine:3.21` 和临时 tar；当前只保留 latest 镜像与一份迁移 36 后 rollback 镜像。部署后观测：Hub ~8 MiB、PostgreSQL ~19 MiB、Redis ~3 MiB，根盘约 73% 使用、7.9 GiB 可用。
- **AgentTeam 后端推进**：新增 `agent_team_events` append/list repository、TeamRunState replay service 和 `GET /web/agent-teams/:id/runs/:run_id/state` endpoint；projection 当前覆盖 members、assignments、route decisions、terminal reason。验证通过 Hub 聚焦测试、`hub-server && go test ./... -short -count=1`、OpenAPI YAML 解析和部署态健康检查。
- **下一步后端顺序**：继续 AT-2 剩余的 TeamTask/RunEvent/approval/artifact/budget projection，再推进 AT-3 typed route rejection/guardrail audit；不要重复做 server-side build。

## Desktop UI / QA 接手记录（2026-05-27 晚间）

- **范围**：只处理 `app/desktop/**` 与 Desktop 交接文档；Mobile、Hub、Edge 的既有改动不回退。当前工作树仍有大量其他方向改动，提交前必须拆分确认。
- **SOP/脱敏沉淀**：新增 `docs/operations/desktop-ui-qa-sop.md`，覆盖 Desktop UI done 定义、客户端 viewport 截图、真实发送 QA、DOM overlap probe、runtime/thread continuity、session isolation、按钮/菜单审计、账号/同步入口 QA、handoff 模板和脱敏替换表。tracked docs 只记录 `.tmp/...` 相对路径、命令摘要和脱敏结论，不写 token/cookie/account ID/真实私有 prompt/绝对 secret path。
- **账号入口 QA/fix**：app 左下角 Hub 账号入口修为“未登录打开登录 modal；已登录打开 Settings 账号页”，不再把已登录状态导向登录 modal。登录/账号入口统一使用账号头像图标，不再使用钥匙或登录箭头图标；tooltip 文案改为 `Hub 账号` / `Hub Account`。Settings 左侧底部只做账号入口，退出登录只放账号页主内容。证据 `.tmp/desktop-footer-account-signed-out.png`、`.tmp/desktop-footer-auth-modal-signed-out.png`、`.tmp/desktop-settings-sidebar-account-entry.png`、`.tmp/desktop-settings-account-signed-out.png`、`.tmp/desktop-footer-account-signed-in.png`；QA JSON `.tmp/desktop-footer-account-qa.json` 中 console `access_token` 已脱敏为 `REDACTED`，断言 `authModalOpened=true`、`hasSidebarSignOut=false`、`hasAccountSignIn=true`、`signOutOnlyInMainContent=true`。读图确认左下角是账号头像入口，无钥匙 icon。
- **Settings 假开关治理（2026-05-28）**：真实渲染路径仍是 `app/desktop/src/components/SettingsPage.tsx`，不是拆出的 `settings/sections/*`。已给未接线能力加真实 disabled/no-write 语义，避免 `agentScheduling`、`skillSync`、`remoteControl`、`platformSync`、`auditTrail` 继续表现成可启用云同步；`taskSync` / `groupChat` 依赖 Hub session，`enableMcp` 依赖 Edge online + MCP-capable runtime。证据 `.tmp/desktop-settings-task-sync-disabled.png`、`.tmp/desktop-settings-skill-sync-disabled.png`、`.tmp/desktop-settings-remote-control-disabled.png`、`.tmp/desktop-settings-platform-sync-disabled.png`、`.tmp/desktop-settings-audit-disabled.png`、`.tmp/desktop-iab-settings-skill-disabled.png`；QA JSON `.tmp/desktop-settings-controls-qa.json` 断言 disabled 控件点击后仍 disabled、`aria-disabled=true`、不写入 `localStorage`，console 中 `access_token=REDACTED`。
- **Desktop 聊天气泡/会话隔离修复（2026-05-28）**：修复用户气泡消失的三段根因：`ThreadPanel` 传 `ThreadInfo` 但 App 当 string 存导致 `[object Object]` thread id；App 未把 Edge `user_message` thread items 投影为 ChatView 用户消息；`MainView.resolveViewMode` 在 run starting 时用 loading skeleton 覆盖 optimistic user bubble。同步修复 `handleSend` 使用已选 `activeThreadId`，避免新建 Claude Code thread 后 threads 查询未刷新时 run 回落到 `thread_local`。后续补齐历史回放：未选择具体 thread 时 `useChatMessages` 仍从 live tail 订阅，选择具体 thread 后用 Edge event bus replay + selected thread 过滤恢复历史 agent 回复，避免 reload 后只剩用户气泡；`ChatView` 过滤不可见 `session_init` / standalone success result，消除只有时间戳的空 agent 行，历史 thinking 以折叠“思考过程”展示。证据 `.tmp/desktop-chat-user-bubbles-fixed-pw.png`、`.tmp/desktop-real-claude-send-fixed-pw.png`、`.tmp/desktop-user-bubble-playwright-632.png`（Playwright reload 后同一 Claude Code thread 显示 2 条用户气泡 + 2 条 agent 回复，`rowCount=4`、`userRows=2`、`scrollHeight=clientHeight=799`、console 无 error；截图只含客户端 viewport，不含 token/cookie/账号）。
- **Desktop 上下文真实发送/输出渲染复验（2026-05-28）**：定位“已发送但看不到回复”的根因不是 Claude Code 未产出，而是 Edge/WebSocket 与前端 replay/render 合并链路不稳。Edge `/v1/events` 已响应 Desktop 应用层 `{type:"ping"}`，避免前端持续 `WebSocket pong timeout`；Edge 不再把 Desktop `thread_*` 当作 Claude Code `--session-id`，而是为 thread 派生 UUID-shaped runtime session，首次 run 用 `--session-id`，有 assistant history 后用 `--resume`；非零退出会把 stderr/stdout 摘要持久化成失败 agent message。真实 Claude Code 三轮 context QA 已在同一 thread 产出 3 条 user message + 3 条 agent message，`GET /v1/threads/<qa-thread>/items` 逐项确认；内置浏览器 reload 后 DOM 只显示每轮 1 条 Agent 回复，Playwright 客户端截图 `.tmp/desktop-context-real-thread-after-fix.png`，登录 logo 截图 `.tmp/desktop-login-token-dance-logo-after-fix.png`，QA JSON `.tmp/desktop-context-logo-qa.json` 断言 `userRows=3`、第二轮 marker 回复 1 次、最终输出 1 次、`consoleMessages=[]`、`chatWaitError=null`、`hasTokenDanceLogo=true`、`hasAHPlaceholder=false`。已补 `mergeChatMessages` 按 runId 去重 live agent / persisted `agent_message`，并补 `useChatMessages` 按 runId 分隔不同 run 的 live agent rows，避免相邻 run 输出被合成一条回复。
- **Desktop 聊天视觉复核（2026-05-28）**：按内置浏览器当前真实 Claude Code 上下文 QA thread 读图后继续收敛 ChatView。Agent 元信息不再在每条回复上显示 `claude-opus-4-7[1M][1m]` 这类长模型名，屏幕只显示运行时名 `Claude Code`，完整模型名保留在 tooltip；Claude 模型名也不再误判成通用 Bot icon。thinking block 保持 Codex-like 紧凑折叠，字体使用 `var(--font-sans)`，不再呈现大块灰色输入框。内置浏览器截图 `.tmp/desktop-chat-style-runtime-label-after-iab.png`、展开态截图 `.tmp/desktop-chat-thinking-readable-after-iab.png`，读图确认运行时 label 为 `Claude Code`、长模型名不在正文可见；DOM probe 断言 `hasLongModelVisible=false`、`hasClaudeCodeVisible=true`、`messageListGap=18px`、thinking `background=transparent`、`borderTopWidth=0`、`borderLeftWidth=1px`、fresh console warn/error 为 0。旧日志里仍能看到历史 `WebSocket pong timeout`，但刷新后等待窗口内没有新增 warning。
- **Desktop descender/g 裁切复核（2026-05-29）**：继 App shell 顶栏/tooltip/ThreadPanel/AgentList 行高补丁后，又补齐 ChatView 里 TeamRun dock、attachment chip、agent task/subagent/route decision、inline run status、context usage、tool use 等单行截断标签的 `line-height`、`min-height` 和下内边距，避免 `Agent`、`running`、`png` 等 descender 在 `overflow: hidden` 下被裁。内置浏览器截图 `.tmp/desktop-g-descender-fixed-settled-iab.png`、tooltip 截图 `.tmp/desktop-g-tooltip-fixed-iab.png`；验证通过 `app/desktop && corepack.cmd pnpm typecheck` 和针对 Desktop CSS/STATE 的 `git diff --check`（仅 CRLF warning）。
- **Desktop 快捷键面板与 `g` 裁切复核（2026-05-29）**：`ShortcutHelp` 与 Settings `键盘快捷键` 现在共用 `KEYBOARD_SHORTCUT_GROUPS`，只列真实接线的快捷键；本轮继续修复快捷键弹层中 `.commandLabel/.commandDetail` 对 `Agent` / `Edge` 等 descender 的裁切，Settings 同源列表也补 `line-height`、`min-height` 和下内边距。内置浏览器截图 `.tmp/desktop-g-shortcut-after-iab.png`、`.tmp/desktop-settings-keyboard-shortcuts-iab.png`；probe `.tmp/desktop-g-shortcut-after-probe.json`、`.tmp/desktop-settings-keyboard-shortcuts-probe.json` 断言 4 组 / 14 行可见、`clientHeight==scrollHeight`、无水平溢出。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/ShortcutHelp.test.tsx src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。
- **Desktop 紧凑标签 `g` 裁切补丁（2026-05-29）**：用户继续指出当前可见界面里 `g` 被挡，本轮只收敛 Desktop shell/composer 紧凑文本：`PromptInput` 工作区选择 pill、App 顶栏 `Edge v1` 状态、`AgentList` 标题和 runtime 名称增加 descender headroom，避免 `Agent Runtime` / `Edge` / `Orchestrator` 在 `overflow: hidden` 单行截断容器里贴底。证据 `.tmp/desktop-current-g-issue-iab.png`、`.tmp/desktop-g-descender-after-pw.png`、`.tmp/desktop-g-descender-chat-after-pw.png`；QA 脚本 `.tmp/g-descender-qa.mjs` 的 metrics 显示相关单行标签 `clientH==scrollH`。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/PromptInput.test.tsx src/__tests__/AgentList.test.tsx`、`corepack.cmd pnpm typecheck`、定向 `git diff --check`（仅 LF/CRLF warning）。注意：内置浏览器 automation tab 在刷新后出现 38x13 viewport 异常，因此最终验收补了固定 924x888 Playwright 客户端截图。
- **Desktop Home/composer `g` 裁切二次补丁（2026-05-29）**：用户再次指出当前画面 `g` 被挡后，复核定位到 Home 最近线程 `19m ago / 5h ago` 小字 `line-height: 1.4` 且无下内边距，composer `@Agent` textarea 下沿也偏紧，provider pill 还会把 `TokenDance` 右侧截断。已在 `HomeDashboard.module.css` 补 `statLabel`、`sectionTitle`、TeamRun 标题/status/metric、最近线程 title/meta/time 的 `line-height/min-height/padding-bottom`，并在 `PromptInput.module.css` 补 textarea 下沿与 provider pill 最小宽度。证据 `.tmp/desktop-g-final-after-iab.png`、`.tmp/desktop-g-descender-current-pw.png`、`.tmp/desktop-g-descender-recent-ago-crop-pw.png`；`.tmp/desktop-g-descender-current-probe.json` 显示 `threadItemTime`/`Edge 已连接`/`AgentTeam 编排` 的 `lineHeightRatio=1.55`，`threadItemTime paddingBottom=2`。验证通过 `corepack.cmd pnpm exec playwright test .tmp/g-descender-current.spec.ts .tmp/g-descender-thread-worktarget.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop workspace/empty title `g` 裁切补漏（2026-05-29）**：用户继续指出当前画面 `g` 被挡后，复核定位到当前 Home workspace header `AgentHub` 与 Chat empty title `我们该在 AgentHub 中做什么？` 仍缺下沿 padding；`App.module.css` 的 workspace/mobile title 统一补 `line-height: 1.5`、`min-height` 和下内边距，`EmptyState.module.css` 标题取消负字距并提升行高/下内边距，suggestion chip 补真实行高。内置浏览器初始截图 `.tmp/desktop-g-user-current-iab.png`；刷新后内置浏览器 viewport 异常缩到 `175x29`，最终用 Playwright 固定 924x888 截图 `.tmp/desktop-g-title-after-pw.png`、裁切 `.tmp/desktop-g-title-crop-after-pw.png` 和 probe `.tmp/desktop-g-title-after-probe.json` 复验，断言 `AgentHub` 标题 `lineHeightRatio>=1.42`、`paddingBottom>=2`、`letterSpacing` 为 normal/0、console 无 error/warn。验证通过 `corepack.cmd pnpm exec playwright test .tmp/g-empty-state-descender.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`、`corepack.cmd pnpm typecheck`。
- **Desktop 线程运行状态标记（2026-05-29）**：P1-4 会话列表不再用 `runCount` 推断伪未读；`App` 新增全量 `useRuns()` 查询并把 `/v1/runs` 结果传给 `ThreadPanel`，由真实 `RunInfo.status` 派生 `运行中`、`待审批`、`失败` pill，`completed` 不显示状态，当前选中 thread 不显示活动 dot。精确未读仍需 Thread read cursor / lastSeen item id 或 Hub unread counter 后续接入。Playwright 截图 `.tmp/desktop-thread-status-pw.png`，probe `.tmp/desktop-thread-status-probe.json` 覆盖 running、waiting_approval、failed、completed 四类 run，断言 `scrollWidth<=innerWidth` 且 console 为空；验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/ThreadPanel.test.tsx src/__tests__/edgeClient.test.ts src/__tests__/threadQueries.test.tsx`（41 tests）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/thread-status-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **bytedance/AgentTeam 复核（2026-05-28）**：`docs/reference/cross-comparison/11-bytedance-feature-map.md` 与 `13-agentteam-competitive-roadmap.md` 已同步为当前口径：调度者 Agent / subagent 要求还没有全部实现。现状是 Hub 端 `AgentTeam` / `AgentTeamRun` / `AgentTeamTask` / `AgentTeamEvent` / `TeamRunState`、typed `CoordinatorRouteDecision`、route guardrails、approval/conflict 后端切片，以及 Desktop `useHubIntegration` route decision bridge 有源码证据；缺口是 AT-4 两个真实 Runtime Profile 的 local TeamRun smoke，以及 Desktop/Web TeamRun Console（task board、member status、subagent activity row、branch switch、typed team blocks、pending approval count、result blocks）。后续不能把 Edge 本地 Orchestrator/sub-agent registry 或 Hub handler/service 单测当作 bytedance 产品完成证明。
- **Desktop 模型目录与真实发送修复（2026-05-28）**：Desktop 模型选择不再使用硬编码假选项。Edge 新增 `/v1/model-catalog`，只读本机 Edge adapter 映射、Codex config、Claude Code settings/provider map 和 cc-switch 配置状态，返回前排除 API token/provider secret；浏览器端不直接请求 TokenDance Gateway `/v1/models`，避免暴露 TokenDance API key。PromptInput 下拉现在从 catalog 生成选项，选择本地映射时发送 `resolvedModel` 与 provider identity，例如 Claude Code 本地映射发送 `model=claude-opus-4-7[1M]`、`provider=claude-code`，不再把 `opus[1m]` 或团队 alias 二次映射成错误 provider。`modelSettingsStore.resolveRunRequestOptions` 已支持并保留 UI 传入的 `provider` / `modelAlias`，同时迁移旧 Claude 默认 alias 到 TokenDance Gateway 默认模型。
- **Desktop 线程路由兜底修复（2026-05-28）**：`threadStore` 恢复持久化、runtime/thread 反向绑定和 stale thread pruning；`App.handleSend` 在没有有效 `activeThreadId` 时会先创建真实 Edge thread，再发 `/v1/runs`，不再静默让 Edge 回落到 `thread_local`。内置浏览器可见发送 `THREAD ROUTING QA <qa-marker>` 后，Edge 新 run 落在新建 synthetic thread，非 `thread_local`；该临时 run 随后已取消。
- **模型/线程 QA 证据（2026-05-28）**：`.tmp/desktop-model-catalog-picker.png` 显示真实 catalog 来源 `Claude Code settings`、`Codex config`、`Edge adapter mappings`、`gpt-5.5` 和 `deepseek-v4-pro`；`.tmp/desktop-model-catalog-send.png` 显示发送后 composer provider 为 `Claude Code`，模型为 `claude-opus-4-7[1M]`，用户 synthetic prompt 在聊天流中可见。`.tmp/desktop-model-catalog-probe.json` 断言 `latestRunRequest.model=claude-opus-4-7[1M]`、`provider=claude-code`、`threadId=thread_97b77040b29bae98`、`requestCount=1`、`promptVisible=true`、`consoleMessages=[]`。
- **Desktop 附件发送 / 思考字体 QA（2026-05-28）**：`node .tmp/desktop-attachment-font-qa.mjs` 复跑至 `?qa=attachment-1779974536078`，确认底部 `+` 是真实附件入口，原生 file input 文案不可见且 input 为 1px/opacity 0/aria-hidden；选择 `.tmp/desktop-attachment-fixture.txt` 后只显示自定义附件 chip，发送 synthetic prompt 触发 1 次 `/v1/runs`。`.tmp/desktop-attachment-font-qa.json` 断言 `nativeFileInputTextVisible=false`、`hasAttachmentHeader=true`、`hasFixtureName=true`、`hasFixtureContent=true`、`threadId=thread_attachment_1779974536078`、`provider=tokendance-gateway`、`model=gpt-5.5`、`consoleMessages=[]`；内置浏览器 DOM 复核 `添加文件` 按钮 enabled。截图 `.tmp/desktop-attachment-hidden-pw.png`、`.tmp/desktop-attachment-chip-pw.png`、`.tmp/desktop-attachment-send-pw.png` 只含客户端 viewport。
- **Desktop 附件气泡紧凑渲染与长输出折叠（2026-05-28）**：修复 ChatView 对用户消息中 `Attached files:` 上下文的渲染方式。发送给 runtime 的 `/v1/runs.prompt` 仍包含附件上下文，但用户气泡只显示原始指令和附件 chip，不再把 `Attached files:`、内容预览或长文件文本作为聊天正文撑开。最新 QA JSON 断言 `hasAttachmentHeaderInBubble=false`、`hasFixtureContentInBubble=false`、`hasFixtureNameInBubble=true`、`hasPromptInBubble=true`、`hasAttachmentHeader=true`；元素截图 `.tmp/desktop-attachment-user-bubble-compact-element.png` 读图确认气泡紧凑。长 agent 文本输出默认折叠并显示 `展开完整输出`，`.tmp/desktop-attachment-send-pw.png` 读图确认长模型列表没有继续把 composer 顶出视口；本次新建测试 run `run_3462cbc18a0348ef` 已在 QA 后取消，Edge 状态为 `cancelled`。
- **Desktop 工作区/环境选择 QA（2026-05-28）**：Composer `工作区和环境` 不再只是展示 label。Tauri Desktop 下本地文件夹 `选择` 按钮会调用 `@tauri-apps/plugin-dialog.open({ directory: true, multiple: false })`；浏览器 QA 下同一按钮明确 disabled 并提示“仅 Desktop 原生窗口可打开系统目录选择器”，手填路径仍可验证 `run.workDir`。`node .tmp/desktop-workdir-qa.mjs` 以 `?qa=workdir-1779973342268` 打开页面，截图 `.tmp/desktop-workdir-menu-pw.png` 读图确认菜单不溢出且容器/服务器为 disabled/空态，`.tmp/desktop-workdir-selected-pw.png` 读图确认 composer 显示 `AgentHub` 工作区。QA JSON 断言 `browseDisabled=true`、`hasChooseFileText=false`、`latestRunRequest.threadId=thread_workdir_1779973342268`、`latestRunRequest.workDir=D:\Code\TokenDance\AgentHub`、`runRequestCount=1`、`consoleMessages=[]`；临时 run `run_5c6a6d4029bdaf6f` 已取消为 `cancelled`。
- **Desktop 自定义指令真实接线（2026-05-28）**：Settings `个性化 / 自定义指令` 不再是只有 chevron 的假入口。页面现在提供本地编辑器、保存、清空、状态 pill 和字符计数；保存到 `agenthub-settings.customInstructions` 后，App 发送本地 run 时会读取并写入 Edge `/v1/runs.appendSystemPrompt`，不拼进聊天正文。`node .tmp/desktop-custom-instructions-qa.mjs` 复跑至 `?qa=instructions-1779974171134`，`.tmp/desktop-custom-instructions-qa.json` 断言 `hasChevronOnlyInstructions=false`、`hasEnabledStatus=true`、`latestRunRequest.promptContainsInstruction=false`、`appendContainsInstruction=true`、`runRequestCount=1`、`consoleMessages=[]`。截图 `.tmp/desktop-custom-instructions-settings-pw.png` 读图确认设置页是可编辑表单；临时 run `run_cc6e4e7db47b769a` 已取消为 `cancelled`。
- **Desktop 顶栏菜单真实接线（2026-05-28）**：顶栏 `文件 / 编辑 / 查看 / 窗口 / 帮助` 不再是无动作按钮。`App.tsx` 现在提供 Codex-like dropdown：`文件` 可新建真实 Edge 会话、打开设置、进入 Hub 账号；`编辑` 可复制当前上下文、打开自定义指令、复制脱敏诊断；`查看` 可真实隐藏/恢复侧边栏、展开工作区、打开任务页、切换主题；`窗口` 在浏览器 QA 下诚实 disabled 并标注 Desktop-only，Tauri 下走 `getCurrentWindow()`；`帮助` 可打开快捷键弹窗。`node .tmp/desktop-top-menu-qa.mjs` 以 `?qa=top-menu-1779976456051` 验证 `fileMenuOk=true`、`sidebarHidden=true`、`sidebarRestored=true`、`windowMenuProbe.disabledCount=3`、`shortcutHelpVisible=true`、`consoleMessages=[]`。截图 `.tmp/desktop-top-menu-file-pw.png`、`.tmp/desktop-top-menu-view-pw.png`、`.tmp/desktop-top-menu-window-disabled-pw.png`、`.tmp/desktop-top-menu-help-pw.png`、`.tmp/desktop-top-menu-shortcuts-pw.png` 读图确认菜单不再被 sidebar/body 遮挡，且只截客户端 viewport。
- **Desktop 顶栏菜单字形裁剪修复（2026-05-29）**：用户指出帮助菜单内 `AgentHub` 的小写 `g` 被挡。根因是 `.topMenuItemLabel` 只有 17px 行盒且 `overflow: hidden`；已把菜单 label 行高和最小高度提升到 1.45em，保留横向 ellipsis。内置浏览器截图 `.tmp/desktop-top-menu-agenthub-g-fixed-iab.png`、`.tmp/desktop-top-menu-agenthub-g-fixed-full-iab.png`，复验截图 `.tmp/desktop-top-menu-agenthub-g-verify-crop-iab.png`、`.tmp/desktop-top-menu-agenthub-g-verify-iab.png`；probe `.tmp/desktop-top-menu-agenthub-g-fixed-probe.json` 与 `.tmp/desktop-top-menu-agenthub-g-verify-probe.json` 断言 label `height≈19.57px`、`lineHeight=19.575px`、`enoughHeadroom=true`。
- **Desktop Runtime 列表字形防回归（2026-05-29）**：继续复核侧栏 `Agent Runtime`、Runtime 名称、顶栏 `Edge` 等含 `g/p/y` 的文本，给 `AgentList.module.css` 的 title、runtime item、name、count pill 显式行高，避免不同缩放/字体栅格化下 descender 贴底。截图 `.tmp/desktop-g-lineheight-fix-playwright.png`、放大裁切 `.tmp/desktop-g-lineheight-fix-sidebar-3x.png`、`.tmp/desktop-g-lineheight-fix-top-3x.png`；验证 `corepack.cmd pnpm exec vitest run src/__tests__/AgentList.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/topMenuState.test.ts`、`corepack.cmd pnpm typecheck`。
- **Desktop Home/顶栏 g 字形复验（2026-05-29）**：针对当前 Home `AgentHub` / `AgentTeam 编排` / `Edge v1` / 最近线程 `ago` 的可见 descender，再给 workspace 标题、顶栏状态、Home section title、TeamRun 面板标题和最近线程标题/时间设置更宽松 line-height；同时把顶栏下拉菜单面板改为实色背景，避免背后 `AgentHub` 文本透过菜单造成“字被挡住”的视觉叠影。Playwright 客户端截图 `.tmp/desktop-g-descender-current-pw.png`、裁切 `.tmp/desktop-g-descender-topbar-edge-crop-pw.png`、`.tmp/desktop-g-descender-workspace-agenthub-crop-pw.png`、`.tmp/desktop-g-descender-home-agentteam-crop-pw.png`、`.tmp/desktop-g-descender-help-menu-pw.png`；probe `.tmp/desktop-g-descender-current-probe.json` 断言 console 为空，`Edge v1` 行盒提升到 18px，Home/Workspace 标题 line-height ≥ 1.45。验证：`corepack.cmd pnpm exec vitest run src/__tests__/HomeDashboard.test.tsx src/__tests__/topMenuState.test.ts src/__tests__/AgentList.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/g-descender-current.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop 会话/工作区 g/p/y 字形补漏（2026-05-29）**：用户继续指出可见 `g` 被挡后，复查当前客户端发现 ThreadPanel 单行会话名、PromptInput 工作区 `本地 Edge` pill、工作区菜单选项和 Runtime 列表仍有 `overflow:hidden` + 1.32/1.35 line-height 的 descender 风险。已把 `ThreadPanel.module.css`、`PromptInput.module.css`、`AgentList.module.css` 的相关单行文本提升到 1.42/1.45 行高并保留 ellipsis。Playwright 客户端截图 `.tmp/desktop-g-descender-thread-worktarget-pw.png`、`.tmp/desktop-g-descender-chat-composer-pw.png`、`.tmp/desktop-g-descender-worktarget-menu-pw.png`，裁切 `.tmp/desktop-g-descender-thread-green-crop-pw.png`、`.tmp/desktop-g-descender-composer-edge-crop-pw.png`；probe `.tmp/desktop-g-descender-thread-worktarget-probe.json` 断言含 descender 且 `overflow:hidden/clip` 的可见文本行高比例不低于 1.38。验证：`corepack.cmd pnpm exec vitest run src/__tests__/AgentList.test.tsx src/__tests__/ThreadPanel.test.tsx src/__tests__/PromptInput.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/g-descender-thread-worktarget.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop g 字形当前内置浏览器复验（2026-05-29）**：重启后重新打开内置浏览器 `http://localhost:5173/?qa=g-descender-iab-1780003000000`，客户端截图 `.tmp/desktop-g-current-iab.png` 读图覆盖 `Agent Runtime`、`OpenCode`、`green-842` 会话名、`AgentHub` Home 标题和 composer `本地 Edge`；DOM probe `.tmp/desktop-g-current-iab-probe.json` 显示可见 `overflow:hidden/clip` 的 descender 文本行高均为 1.42+，未复现新的下沿裁切。随后复跑 Playwright g/p/y 回归，截图刷新为 `.tmp/desktop-g-descender-thread-worktarget-pw.png`、`.tmp/desktop-g-descender-chat-composer-pw.png`、`.tmp/desktop-g-descender-worktarget-menu-pw.png`，命令 `corepack.cmd pnpm exec playwright test .tmp/g-descender-thread-worktarget.spec.ts --config .tmp/playwright-agent-scheduling.config.ts` 通过。
- **Desktop icon tooltip / 顶栏 g 字形补漏（2026-05-29）**：用户指出当前页仍有 `g` 被挡后，重新用内置浏览器复现到顶栏 `Agent 调度` hover tooltip 与 `Edge v1` 状态文本。App shell 的 `.iconTooltip`、`.topMenuTrigger`、`.topMenuItemLabel`、`.topMenuItem kbd`、`.statusBadge`、`.topBarDim` 已统一提升到 descender-safe 行高/上下 padding（tooltip/status 为 1.55），保留 ellipsis 与紧凑顶栏高度。最新证据 `.tmp/desktop-g-clipping-final-iab.png`、`.tmp/desktop-g-clipping-final-tooltip-iab.png`、probe `.tmp/desktop-g-clipping-final-probe.json`；probe 断言 viewport `924x843`、`Edge v1` 行盒 `height≈20.05px`、`lineHeight=17.05px`、`paddingTop=1px`、`paddingBottom=2px`、`overflow=visible`，可见 tooltip `Agent 调度` 行盒 `height≈30.05px`、`lineHeight=17.05px`、`paddingBottom=6px`、`opacity=1`。验证：`corepack.cmd pnpm typecheck`、`git diff --check -- app/desktop/src/App.module.css docs/handoff/STATE.md` 通过。
- **Desktop Orchestrator 新建会话/真实发送修复（2026-05-29）**：用户路径“点击 Orchestrator → 左侧会话 `+` 新建 → 发送消息”已复现并修复。根因是 `ThreadPanel` 自己调用 `useCreateThread()`，只走普通 `onSelect(thread)`，绕过 App shell 的 runtime-bound `handleCreateThread()`；现在 `ThreadPanel` 支持 `onCreate` 委托，App 两处 `thread-panel` slot 都传 `handleCreateThread`，所以新建线程会保留当前 runtime 绑定。同步修复 live agent 行作者显示：当 event 只给出 Claude/GPT/TokenDance 等模型/provider label 时，聊天行显示当前选择的父 runtime（如 `Orchestrator`），真实子 Agent 自定义名仍保留。内置浏览器真实点击和发送证据：`.tmp/desktop-orchestrator-new-thread-bound-fixed-iab.png`、`.tmp/desktop-orchestrator-send-started-iab.png`、`.tmp/desktop-orchestrator-send-finished-iab.png`、`.tmp/desktop-orchestrator-send-label-fixed-iab.png`；probe `.tmp/desktop-orchestrator-new-thread-bound-fixed-probe.json` 断言 header/runtime/composer 均为 Orchestrator，`.tmp/desktop-orchestrator-send-label-fixed-probe.json` 断言用户 `reply ok` 与 agent `ok` 渲染、消息 label 为 `Orchestrator`。Edge API 复核 `run_5b3f5db1f6a0f5b3` 落在 `thread_77d61b13e5e6ca7c`，thread items 含 `user_message=reply ok` 与 `agent_message=ok`。
- **Desktop 本地编排入口拆出 Hub 登录阻塞（2026-05-29）**：根据“Agent 编排应该是本地功能”的反馈，ChatView TeamRun dock 和 HomeDashboard AgentTeam 面板现在优先展示本地 `Orchestrator` 可用状态：识别本机 Orchestrator runtime、统计可用 Runtime 数、提供真实 `使用 Orchestrator`/`当前使用` action；Hub TeamRun 只作为同步和审计能力展示，未登录时不再把本地编排文案写成必须登录 TokenDance ID。同步修复 Home 面板 active status pill 文字与背景同色导致的蓝色空条。内置浏览器截图 `.tmp/desktop-local-orchestration-chat-iab.png`、`.tmp/desktop-local-orchestration-home-fixed-iab.png`；probe `.tmp/desktop-local-orchestration-chat-probe.json` 与 `.tmp/desktop-local-orchestration-home-fixed-probe.json` 断言 `hasLocalReady=true`、`hasHubSignedOutBlocker=false`、`hasHubSyncNote=true`、`hasUseOrchestrator=true`，Home status 文本为 `本地 3 个 Runtime` 且不再隐身。真实点击复验：从 Codex 状态点击 Home/Chat 的 `使用 Orchestrator` 后，header 与 composer 切回 Orchestrator，侧栏 runtime 选中同步；证据 `.tmp/desktop-home-use-orchestrator-action-extension.png`、`.tmp/desktop-chat-use-orchestrator-action-extension.png`，probe `.tmp/desktop-home-use-orchestrator-action-probe.json`、`.tmp/desktop-chat-use-orchestrator-action-probe.json` 断言切换后 `@Orchestrator` 且前端 error 为 0。验证：`corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/HomeDashboard.test.tsx src/__tests__/MainView.test.tsx`、`corepack.cmd pnpm typecheck`。
- **Desktop Chat typed subagent/route 卡片（2026-05-29）**：主聊天流现在消费真实协议名的 typed Edge events：`run.agent.route_decision` 投影为路由决策卡，`run.agent.task_started/progress/notification` 按 task id upsert 为 subagent 任务卡，`run.agent.task_dispatched`、`run.agent.child_spawn`、`run.agent.child_result` 投影为紧凑子 Agent 内联卡并按 child id / child run id 合并结果，避免多智能体编排只在 Settings Console 可见。ChatView 支持 `route_decision`、`agent_task` 与 `child_agent` block，`useChatMessages` 兼容 `taskId/task_id/agent_task_id/team_task_id`、`agentId/agentInstanceId/childId`、`subAgentRunId/childRunId`、`next_worker/member_id/worker` 等字段，并保留进度/完成 summary/result/error/duration。视觉 QA 截图 `.tmp/desktop-chat-subagent-cards-pw.png`、probe `.tmp/desktop-chat-subagent-cards-probe.json` 断言 route/task/child 卡存在、`scrollWidth=innerWidth=924`、console 为空；本截图 harness 使用 mocked typed Edge events 驱动真实 Desktop 前端，不等同于双真实 Runtime Profile 本地 TeamRun smoke。验证：`corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/useChatMessages.test.ts`（63 tests）、`corepack.cmd pnpm exec playwright test .tmp/chat-subagent-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`、`corepack.cmd pnpm typecheck`；本轮 CSS 复核保持 route/task/child 卡暗色对比度与字形下伸部安全。
- **Desktop ToolTimeline / composer clearance 补齐（2026-05-29）**：`ToolTimeline.tsx` 已接入 Agent 消息，按当前消息 blocks 派生 route/tool/file/task/child-agent 活动，作为 P1-3 工具时间线 Desktop MVP；仍不是完整 RunDetail 观测控制台。复测发现 tablet/mobile media query 覆盖了 `.stream` 的 `--composer-clearance`，导致滚到底时最新 child-agent 卡被 composer 遮住；已让响应式 padding 保留底部 clearance。同步把 `AgentList`、`ThreadPanel`、`PromptInput` 工作区 pill 与顶栏状态等单行英文标签提升 line-height/padding，避免 `g/p/y` descender 在 `overflow:hidden` 容器内贴底。证据：`.tmp/desktop-chat-subagent-cards-pw.png`、`.tmp/desktop-chat-subagent-cards-probe.json` 显示 child card bottom 550 <= input top 727-8 且 `scrollWidth=innerWidth=924`；当前客户端截图 `.tmp/desktop-g-lineheight-current-after-pw.png` 覆盖 `Agent Runtime`、`AgentHub`、`Edge v1`、`本地 Edge`。验证：`corepack.cmd pnpm exec playwright test .tmp/chat-subagent-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`、`corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/useChatMessages.test.ts`、`corepack.cmd pnpm typecheck`、定向 `git diff --check` 通过。
- **Desktop TaskList 任务总览 MVP（2026-05-29）**：P1-3 `Agent 任务列表` 已补 Desktop MVP。新增 `TaskList.tsx` / `TaskList.module.css`，从当前 Agent 消息内真实 `agent_task` 与 `child_agent` blocks 派生任务总览，显示总数、running/completed/failed 计数、worker id 与 child run id，不创建独立假任务状态。ChatView 渲染顺序为 `TaskList` → `ToolTimeline` → 单个 route/task/child 详情卡。Playwright 证据：`.tmp/desktop-chat-task-list-pw.png` 显示任务列表 2 项，`.tmp/desktop-chat-subagent-cards-pw.png` 显示滚到底后最新 child card 与 composer 不重叠；`.tmp/desktop-chat-subagent-cards-probe.json` 断言 `taskListText` 包含 `任务列表`、`Implement subagent cards`、`run_child_builder`，且 `scrollWidth=innerWidth=924`、console 为空。验证：`corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/useChatMessages.test.ts`（65 tests）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/chat-subagent-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop Chat Token/Context 用量条 MVP（2026-05-29）**：聊天流已消费真实 Edge typed usage 事件：`run.agent.session_metrics`、`run.agent.context_usage`、`run.agent.context_warning`、`run.agent.context_compaction` 统一 upsert 为当前 Agent 回复内的 `context_usage` block；`run.agent.result.usage` / `tokenUsage` 渲染为同风格 Token 用量条，避免用量只藏在 RunDetail。用量条展示输入/输出/总计、context window、remaining、阈值、费用、provider/model，并按 runId 合并 warning/compaction，减少刷屏。Playwright 证据 `.tmp/desktop-chat-context-usage-pw.png`、probe `.tmp/desktop-chat-context-usage-probe.json` 断言两个用量条可见，`88%`、`34.0K`、`1.8K`、`Claude Code / claude-sonnet` 渲染正确，最后条 bottom 455 <= composer top 727-8，`scrollWidth=innerWidth=924`、console 为空；内置浏览器复核 `.tmp/desktop-g-current-iab-fresh.png` / `.tmp/desktop-g-current-iab-fresh-probe.json` 同时确认当前客户端 `g/p/y` 单行标签仍有下沿余量。验证：`corepack.cmd pnpm exec playwright test .tmp/chat-context-usage-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`、`corepack.cmd pnpm exec vitest run src/__tests__/useChatMessages.test.ts src/__tests__/ChatView.test.tsx`（69 tests）、`corepack.cmd pnpm typecheck`、定向 `git diff --check` 通过。
- **Desktop Thread 管理 P1-4 MVP（2026-05-29）**：会话列表从纯扁平列表推进到真实活跃/归档状态和项目+日期分组。Edge 新增 `PATCH /v1/threads/{id}`、`POST /v1/threads/{id}:archive`、`DELETE /v1/threads/{id}`，`DeleteThread` 会清理关联 runs/items/order；Desktop `ThreadPanel` 新增活跃/已归档 tab、归档/恢复按钮、归档 pill 和多项目可见时的 `projectId · 日期` 分组。`threadQueries` 已修正乐观更新与失败回滚，覆盖所有 `['threads', projectId]` cache key，不再只写不存在的 `['threads']` exact key。Playwright 证据 `.tmp/desktop-thread-archive-active-pw.png`、`.tmp/desktop-thread-archive-archived-pw.png`、probe `.tmp/desktop-thread-archive-probe.json` 断言捕获真实 `POST /v1/threads/thread_archive_visual:archive` 和 `PATCH /v1/threads/thread_archive_visual {"status":"active"}`、`scrollWidth=innerWidth=924`、console 为空。验证：`edge-server && go test ./internal/store ./internal/api -run "TestStoreUpdatesAndDeletesThreads|TestThreadUpdateArchiveDeleteRoutes|TestProjectThreadRoutes" -count=1`、`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/ThreadPanel.test.tsx src/__tests__/edgeClient.test.ts src/__tests__/threadQueries.test.tsx`（37 tests）、`corepack.cmd pnpm exec playwright test .tmp/thread-archive-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`、`corepack.cmd pnpm typecheck`。
- **Desktop Prompt slash commands MVP（2026-05-29）**：Composer 现在支持 Codex-like `/` 命令 palette，命令均接真实状态而不是占位：可切换可用 Agent Runtime、选择真实 `model-catalog` 模型映射、设置 reasoning、permission、打开 workspace/execution target 菜单、切回 Local Edge、清空 composer/附件/草稿；命令 token 会从 textarea 移除，不进入 `/v1/runs.prompt`。同时修复 Playwright 发现的真实 hit-test bug：模型/推理按钮 chevron 之前会覆盖右侧发送按钮，导致 `发送` 可见但不可点；现在限制 ModelReasoningPicker 宽度并提高发送按钮层级。截图 `.tmp/desktop-prompt-slash-model-palette-pw.png`、`.tmp/desktop-prompt-slash-workspace-pw.png`；probe `.tmp/desktop-prompt-slash-probe.json` 断言 captured run request 为 `model=claude-opus-4-7[1M]`、`provider=claude-code`、`modelAlias=opus[1m]`、`prompt` 不含 slash command、`scrollWidth=innerWidth=924`、console 为空。本 Playwright harness mock 本地 Edge API / model catalog，但真实 Desktop 前端会发 POST `/v1/runs` 并由 probe 捕获请求体。验证：`corepack.cmd pnpm exec vitest run src/__tests__/PromptInput.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/prompt-slash-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop retry/fork 消息动作 MVP（2026-05-29）**：新增 `messageActions` 辅助逻辑，`retry` 对用户消息直接重发该用户 prompt，对 Agent 消息回溯最近一条用户 prompt，避免把 Agent 输出作为新 prompt；Agent 消息 action 增加 fork，`/retry`、`/fork` slash commands 接到同一逻辑。`/fork` 会创建真实 Edge thread、绑定当前 runtime、切换到新 thread，并向 composer 注入来源 thread 与最近用户请求的可编辑 draft；分叉只显示 `已创建分叉会话` 一个成功 toast，通用 `会话已创建` 被静默，减少重复反馈。Playwright 证据 `.tmp/desktop-retry-fork-thread-before-pw.png`、`.tmp/desktop-retry-action-after-pw.png`、`.tmp/desktop-slash-fork-palette-pw.png`、`.tmp/desktop-fork-draft-pw.png`；probe `.tmp/desktop-retry-fork-probe.json` 断言 retry POST `/v1/runs.prompt` 等于原始用户请求且不包含 Agent answer，fork POST `/v1/threads` 标题为 `Fork: ...`，composer draft 含来源 thread 和原用户请求，console 为空。验证：`corepack.cmd pnpm exec vitest run src/__tests__/messageActions.test.ts src/__tests__/PromptInput.test.tsx src/__tests__/ChatView.test.tsx src/__tests__/MainView.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/retry-fork-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop @mention MVP 收口（2026-05-29）**：Composer `@agent` / `@file` / `@thread` 已从只列 Agent 扩展为真实可用入口：`@agent` 切换 Runtime 并移除 token，`@file` 触发真实隐藏 file input/原生附件入口，`@thread` 插入 `@thread(title threadId)` 并随 prompt 发送。修复 Playwright 复现的两类真实交互 bug：MentionPopover 作为 PromptInput root 子节点时继承 `pointer-events:none` 且受 `.inputArea will-change: transform` 影响，导致可见但点击穿透/右侧越界；现在 portal 到 `document.body`、设置 `pointer-events:auto`、补 `--z-popover` token，并在 mouse down 保持 textarea 焦点。截图 `.tmp/desktop-mention-thread-palette-pw.png`、`.tmp/desktop-mention-thread-insert-pw.png`、`.tmp/desktop-mention-file-palette-pw.png`、`.tmp/desktop-mention-file-chip-pw.png`；probe `.tmp/desktop-mention-probe.json` 断言 `@thread(Planning g thread thread_mention_source)` 进入第一个 run prompt，附件上下文含 `MENTION-FILE-842`，`scrollWidth=innerWidth=924`、原生 “选择文件/No file chosen” 不可见、console 为空。验证：`corepack.cmd pnpm exec vitest run src/__tests__/useMention.test.tsx src/__tests__/MentionPopover.test.tsx src/__tests__/PromptInput.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/mention-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop Settings Agent 调度页本地编排状态（2026-05-29）**：`Agent 调度` Settings 不再只有 Hub TeamRun/AgentTeam 口径。页面 summary 新增 `本地编排` 卡片，详细块显示调度者 Runtime、可调用 Runtime 数和 Hub 同步边界，明确 Hub 登录只影响 TeamRun 同步/审计/远程可见性，不阻塞本地 Orchestrator。截图 `.tmp/desktop-settings-local-orchestration-extension.png`、`.tmp/desktop-settings-local-orchestration-panel-extension.png`；probe `.tmp/desktop-settings-local-orchestration-probe.json`、`.tmp/desktop-settings-local-orchestration-panel-probe.json` 断言 Settings 本地编排面板可见、包含 `Orchestrator`、`3/4` 可调用 Runtime 与 `Hub 同步边界`。验证：`corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、组合回归 `corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/HomeDashboard.test.tsx src/__tests__/MainView.test.tsx src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`。
- **Desktop TeamRun Console 卡片布局收口（2026-05-29）**：继续修正 Settings `Agent 调度` 里的 Hub TeamRun Console。`teamConsoleGrid` / `teamBuilderGrid` 改为自适应 320px 最小列，924px 客户端主内容区下不再出现 274px 挤压双列；审批与冲突行改为内部 action footer，`拒绝` / `允许` / `标记人工合并` 都留在对应卡片内部。Playwright QA 截图 `.tmp/desktop-agentteam-console-top-after-pw.png`、`.tmp/desktop-agentteam-console-actions-after-pw.png`；probe `.tmp/desktop-agentteam-console-after-probe.json` 断言所有 TeamRun row 宽度 560px、审批/冲突 `actionFooterCount=1`、`scrollWidth=innerWidth=924`、`consoleMessages=[]`。内置浏览器 DOM 复核当前真实未登录态下 TeamRun Console 显示 Hub 登录空态，本地编排块仍可见；内置浏览器截图接口本轮仍有 CDP timeout，视觉验收以 Playwright 客户端截图为准。验证：`corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/agent-scheduling-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop TeamRun branch switch（2026-05-29）**：Settings `Agent 调度` 的 TeamRun Console 不再只展示默认最新 run。`useHubAgentTeams` 查询新增 `selectedTeamId/selectedRunId`，Team/TeamRun 列表改为可点击分支选择；点击不同 Hub TeamRun 会重新请求对应 `TeamRunState`、TeamTask 和 TeamEvent。Playwright 截图 `.tmp/desktop-agentteam-console-branch-switch-pw.png` 读图确认从 Builder 分支切到 Review 分支后，header、成员状态、任务板、route log 和 `agent.result` 活动都换成 Review TeamRun；probe `.tmp/desktop-agentteam-console-after-probe.json` 断言 `scrollWidth=innerWidth=924`、console 为空。内置浏览器真实当前客户端截图 `.tmp/desktop-agentteam-live-iab.png` / `.tmp/desktop-agentteam-settings-live-iab.png` 复核未登录时本地 Orchestrator 编排仍可见，Hub TeamRun 是同步/审计边界。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/agent-scheduling-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop TeamRun result/artifact/budget blocks（2026-05-29）**：Settings `Agent 调度` 的 TeamRun Console 现在直接消费 `TeamRunState.terminal_reason`、`run_events` 中的 `agent.result`、`artifacts` 和 `budget`，新增结果、产物和预算区块，避免完成态只散落在活动列表里。Playwright 截图 `.tmp/desktop-agentteam-console-results-pw.png` 读图确认 terminal reason、`agent.result`、artifact path `reports/teamrun-summary.md` 和 token budget `1,280 / 4,000` 可见；probe `.tmp/desktop-agentteam-console-after-probe.json` 断言 `scrollWidth=innerWidth=924`、console 为空。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec playwright test .tmp/agent-scheduling-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。
- **Desktop TeamRun approval/control bridge（2026-05-29）**：Desktop `useHubIntegration` 已订阅 Hub WS `agent.control`，只处理 `kind: permission.decide`，兼容 `edge_control`/`edgeControl` 和 snake/camel `runId/requestId`，规范化 `allow|deny` 后 POST Local Edge `/v1/permissions/decide`，并在同一 Desktop session 内对成功或 in-flight 的 control 去重；malformed permission control 只 warn 一次，未知未来 control kind 静默忽略。TeamRun Console QA 真实点击 `允许` 与 `标记人工合并`，验证 Hub request body 与 UI 状态刷新，截图 `.tmp/desktop-agentteam-console-decision-after-pw.png`，probe `.tmp/desktop-agentteam-console-after-probe.json` 带 `approvalDecisions` / `conflictResolutions`。本轮继续补真实 HTTP Edge pending endpoint 集成测试：`useHubIntegration.test.ts` 启动本地 HTTP server 模拟 Edge pending registry，Hub `agent.control` 触发 Desktop hook 后真实 POST `/v1/permissions/decide`，断言 pending 被消费、`run.agent.permission_decided` tool metadata 被记录、重复 control 不会二次 POST。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/useHubIntegration.test.ts`（36 tests）、`corepack.cmd pnpm typecheck`、`edge-server && go test ./internal/api -run "TestPostPermissionDecide" -count=1`、`corepack.cmd pnpm exec playwright test .tmp/agent-scheduling-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`；AT-5 仍缺真实 Hub WS device delivery + Go Edge handler 同进程 live E2E 和 side-by-side diff/artifact 决策 UI。
- **Desktop TeamRun artifact source compare（2026-05-29）**：审批与冲突卡继续升级为可审阅的 source comparison，不再只是一个路径和一个“人工合并”按钮。`TeamConflictRow` 会按 `conflict_id` / normalized path 从 `TeamRunState.artifacts` 找到同一路径的 artifact 来源，逐个显示 agent task、member、edge run、action、status、时间，并把按钮放在卡片内部；可直接走 Hub 已支持的 `accept_agent_task` + `selected_agent_task_id`，也可 `keep_all` 或 `manual_merge`，不引入前端假状态。Playwright 截图 `.tmp/desktop-agentteam-console-conflict-compare-pw.png` 读图确认两个 source 卡和 `采用该来源` / `保留全部` / `标记人工合并` 都在冲突卡内部；probe `.tmp/desktop-agentteam-console-after-probe.json` 记录 `conflictSourceCount=2`、manual merge request body 和空 console。验证通过 `corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、`corepack.cmd pnpm typecheck`、`python -m json.tool src/i18n/locales/{zh,en}.json`、`corepack.cmd pnpm exec playwright test .tmp/agent-scheduling-visual.spec.ts --config .tmp/playwright-agent-scheduling.config.ts`。AT-5 剩余：真实 Hub delivery + Edge pending permission live E2E，以及 artifact 内容/diff content index 后的真正 side-by-side diff。
- **Desktop 浅色主题与侧栏 footer 修复（2026-05-28）**：浅色主题坏掉的主因是 `main.tsx` 先加载 `themes.css` 后加载 `tokens.css`，导致 `tokens.css :root` 深色默认值覆盖 `[data-theme='light']`；同时 `App.module.css` 仍有 root/sidebar/workspace/footer/auth modal 等硬编码深色默认。已改为 `tokens.css` 先、`themes.css` 后，并把 App 壳层、登录 modal、侧栏 Runtime/会话 hover/selected/search 的默认样式改成 light-first，dark 走 `[data-theme='dark']` 覆盖。用户指出左下角设置/账号/主题按钮漂到会话列表中段后，定位为 `.sidebarFooter { position:absolute }` 加 `.sidebarSection:last-of-type` 结构猜测失效（footer 也是 `div`，会话区未稳定 flex）；已给会话区显式 `threadSection` class，footer 改为参与 flex 排版。内置浏览器截图 `.tmp/desktop-light-theme-home-fixed-iab.png`、`.tmp/desktop-light-theme-auth-modal-fixed-iab.png`、`.tmp/desktop-sidebar-footer-fixed-dark-iab.png`、滚动后 `.tmp/desktop-sidebar-footer-scroll-fixed-iab.png`；DOM probe 断言 dark 下 `footerAtBottom=true`、`threadEndsBeforeFooter=true`、会话区 `scrollHeight=1951/clientHeight=553`，滚动后 `threadScrollTop=560` 且 footer 仍在 sidebar 底部。
- **Desktop Runtime 切换后历史回复消失修复（2026-05-29）**：复现用户路径“点击左侧 Codex Runtime → 再点 Local Thread 历史”后，确认 `thread_local` 旧数据只有 `user_message` + terminal run，没有旧版持久化的 `agent_message`，所以前端 replay 清掉 live 流后只剩用户气泡。已让 Desktop 回放同时读取 selected thread 的 `/v1/runs`，对已结束但无可回放 transcript 的 run 插入明确 Agent 行“本地没有保存可回放的 Agent 输出”，避免静默空白；新真实发送 `Desktop replay persistence QA 529` 产生 `run_1e221aa04f193d1c`，Edge `/v1/threads/thread_local/items` 已确认落 `agent_message` 内容 `REPLAY-PERSIST-529`。同轮发现 composer 残留已发送文本的根因是 `useInputDraft` 的 500ms debounce timer 在发送清空后继续写回旧草稿；已在 `clearDraft()` 取消 pending timer。内置浏览器截图 `.tmp/desktop-agent-reply-missing-before-iab.png`、`.tmp/desktop-agent-replay-fallback-after-iab.png`、`.tmp/desktop-runtime-switch-history-replay-composer-clear-iab.png`；DOM probe 断言切 Runtime 再点历史后 `hasReplayReply=true`、`hasComposerReply=true`、textarea `value=""`。
- **验证**：内置浏览器 DOM 复核 `Hub 账号` 存在且旧 `点击登录 Hub` 不存在；`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx` 通过（20 tests）；`corepack.cmd pnpm typecheck` 通过；`node .tmp/desktop-settings-controls-qa.cjs` 通过；定向 `git diff --check` 通过，仅 Windows LF/CRLF warning。
- **本轮新增验证（2026-05-28）**：`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/PromptInput.test.tsx src/__tests__/SettingsPage.test.tsx src/__tests__/modelSettingsStore.test.ts src/__tests__/edgeClient.test.ts src/__tests__/threadStore.test.ts src/__tests__/threadSelection.test.ts src/__tests__/threadRuntime.test.ts src/__tests__/chatMessages.test.ts src/__tests__/useChatMessages.test.ts src/__tests__/ChatView.test.tsx` 通过（10 files / 123 tests）；附件/字体收口后 `corepack.cmd pnpm exec vitest run src/__tests__/PromptInput.test.tsx src/__tests__/ChatView.test.tsx src/__tests__/modelSettingsStore.test.ts src/__tests__/edgeClient.test.ts src/__tests__/threadStore.test.ts src/__tests__/threadSelection.test.ts src/__tests__/threadRuntime.test.ts src/__tests__/chatMessages.test.ts src/__tests__/useChatMessages.test.ts` 通过（9 files / 104 tests），附件气泡紧凑渲染、附件移除和长输出折叠后 `corepack.cmd pnpm exec vitest run src/__tests__/ChatView.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/chatMessages.test.ts src/__tests__/useChatMessages.test.ts` 通过（4 files / 82 tests），工作区目录选择器补齐后同命令通过（4 files / 84 tests），自定义指令接线后 `corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/ChatView.test.tsx src/__tests__/chatMessages.test.ts src/__tests__/useChatMessages.test.ts` 通过（5 files / 105 tests）；本次 context/render 收口后 `corepack.cmd pnpm exec vitest run src/__tests__/AuthPage.test.tsx src/__tests__/LoginForm.test.tsx src/__tests__/chatMessages.test.ts src/__tests__/useChatMessages.test.ts` 通过（4 files / 59 tests），`corepack.cmd pnpm typecheck` 通过，`edge-server && go test ./internal/api ./internal/adapters ./internal/lifecycle -run "TestPostRunsPersistsUserPromptAndUsesThreadSession|TestPostRunsResumesThreadRuntimeSessionAfterAssistantHistory|TestThreadTranscriptEmitterPersistsAssistantMessage|TestWebSocketRespondsToApplicationPing|TestClaudeCode|TestProcessExecutorPublishesFailedForNonZeroExit|TestProcessExecutorPublishesFailedWhenCommandCannotStart" -count=1` 通过，`node .tmp/desktop-context-logo-qa.mjs` 通过；`corepack.cmd pnpm build` 通过（仅既有 Tauri dynamic import/chunk-size warning）；`app/desktop/src-tauri && cargo check` 通过（既有 `auto_restart` unused warning）；`edge-server && go test ./internal/api -run "TestPostRunsAcceptsDesktopModelRoutingMetadata|TestGetModelCatalogRedactsLocalConfigSecrets" -count=1` 通过；`node .tmp/desktop-model-catalog-qa.mjs`、`node .tmp/desktop-attachment-font-qa.mjs`、`node .tmp/desktop-workdir-qa.mjs`、`node .tmp/desktop-custom-instructions-qa.mjs` 通过。内置浏览器截图能力本轮仍出现 CDP capture timeout，因此可见交互用 Browser DOM/真实 reload 验证，最终截图仍采用 Playwright 客户端 viewport 文件。

## 本次会话完成（2026-05-26 晚间）

### Desktop/Mobile Tauri 拆分
- **问题**：Mobile Agent 修改了 Desktop 的 `tauri.conf.json`，把 devUrl 改成 5174、frontendDist 改成 `../../mobile/dist`，并在 `lib.rs` 中混入 `#[cfg(target_os = "android")]` 条件编译——两个 Tauri 项目不该共享。
- **修复**：恢复 Desktop `tauri.conf.json`（5173、`../dist`、`pnpm dev`），恢复 `lib.rs` 移除 cfg 守卫。
- **Mobile 脚手架**：创建 `app/mobile/src-tauri/` 独立 Tauri 项目（`com.agenthub.mobile`、5174、无 tray/edge/挂 keyring），含 oidc/secure_store/notifications stub。
- **规则落地**：`AGENTS.md` 新增端口分配表（5173/5174/8090/3210）和 Rust 隔离规则。Desktop Agent 只能改 `app/desktop/src-tauri/`，Mobile Agent 只能改 `app/mobile/src-tauri/`。需要共享 Rust 代码时先提议创建 `app/shared-rust/`。

### Mobile Android 构建与 Emulator（2026-05-26 深夜）
- **APK 构建**：`app/mobile` 独立 Tauri 项目已成功构建 debug APK（`app/mobile/src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk`）。
- **APK 大小**：~480 MB，几乎全来自 4 个 CPU 架构的 Rust debug `.so`（108-125 MB/arch）。Release build（`--release`）或单 ABI（x86_64 emulator only）可大幅缩小。
- **Emulator**：AVD `agenthub-emu` 已创建（API 35, x86_64, google_apis），emulator 可成功启动并通过 `adb` 连接。
- **Gradle 代理**：`app/mobile/src-tauri/gen/android/gradle.properties` 已配 127.0.0.1:7897 代理（Clash Verge Rev），Java 需用 Android Studio JBR（`C:\Program Files\Android\Android Studio\jbr`）。
- **`tauri android dev` 未验证**：`beforeDevCommand` 启动 Vite 时端口 5174 被占用；尚未完成完整 dev 循环验证。

### Mobile 前端骨架（app/mobile/src/）
- **路由**：4-tab BottomNav（Threads / Chat / Runs / Account），glass 风格，48dp touch target；Account 是手机端右下角身份/登录入口，避免把登录埋进 Desktop 风格 Settings。
- **视图**：ThreadListView（listThreads + command-center overview）、ChatView（Mobile-native bubble/composer + createThreadMessage）、RunListView（recent run queue）、RunStatusView（getRun + approval/diff/logs）、SettingsView（native command entry）。
- **Hooks**：`useHubSocket.ts` — WebSocket hook，generation counter 模式，exponential backoff + jitter。
- **样式**：`global.css` — 完整 `--td-*` 设计 token + 暗色模式 + Mobile 自有语义 class。此前界面写了 Tailwind-like class 但没有 Tailwind 构建链，emulator 上会退化成原生按钮/错位布局；本轮已移除该依赖假设。
- **共享**：消费 `@agenthub/shared`（types, apiClient, eventClient, UI 组件）。
- **结构整理**：`App.tsx` 只保留 tab route state；`components/BottomNav.tsx` 管导航；Chat root 空态已迁移到 shared `@agenthub/shared/ui` `EmptyState` 并用 Mobile class 覆盖触控样式；视图保持在 `views/`；Mobile 本地说明见 `app/mobile/README.md`。
- **Native bridge 入口**：`src/native/mobileCommands.ts` 已封装 Tauri command 调用和 notification plugin 权限/探针；`SettingsView` 已接入 Sign in / Check session / Clear / Test alert 按钮并显示真实 native-layer 成功或错误状态，native action 失败时状态面板会保留 44px Retry 动作。OIDC 与 secure store 仍是 Rust stub，按钮会暴露当前 "not yet implemented" 错误，后续接 Android deep link/Keystore 时复用该前端入口。
- **Notification capability**：`app/mobile/src-tauri/capabilities/default.json` 已增加 `notification:default`，允许 Mobile webview 调用 Tauri notification plugin 的权限请求与本地通知探针。
- **Mobile 设计系统推进（2026-05-27）**：`HUB_API_URL` 修正为当前公网 Hub API `http://api.hub.vectorcontrol.tech`（shared client 负责 `/v1`），避免此前浏览器/预览请求拼成 `/api/v1/v1/*`。Threads 页新增 command-center 概览、指标 tile、搜索入口和 retry 状态；Runs tab 从空态改为真实 recent run queue，点 run 进入日志详情；Chat 页从 Desktop shared ChatBubble/ChatInput 改为 Mobile-native bubble/composer，并接入 `createThreadMessage`，避免暗色移动界面 meta/输入框对比和间距问题。
- **截图证据（2026-05-27）**：Playwright 390x844 dark color scheme + mocked Hub workflow 数据已截 `app/mobile/screenshots/mobile-design-after-threads-mocked-dark.png`、`mobile-design-after-chat-mocked-dark.png`、`mobile-design-after-runs-mocked-dark.png`、`mobile-design-after-run-detail-mocked-dark.png`、`mobile-design-after-settings-mocked-dark.png`。真实 localhost 浏览器预览会被生产 Hub CORS 拦截，已另截 `mobile-design-after-threads.png` / `mobile-design-after-runs.png` 表明 layout 无横向溢出但请求 blocked；Tauri/Android 现在通过 native `hub_request` bridge 访问 Hub REST，仍需重装 APK 后复验 live Hub runtime。
- **Mobile approval/diff/resources surface（2026-05-27）**：`RunStatusView` 已接 `listApprovals`、`decideApproval`、`getRunDiff`、`listRunItems`、`listArtifacts` 和 `listPreviews`，waiting approval run 会显示 compact approval 卡、Approve/Reject 双操作、approval metadata、diff file preview、structured run blocks、artifact/preview resources 和日志。资源项已补移动端操作：artifact `Copy` path、preview `Open` URL；Open 通过 `src/native/resourceActions.ts` 优先走 Tauri shell plugin，浏览器 QA fallback 到 `window.open`。2026-05-27 继续补了 resource detail bottom sheet：artifact/preview 的图标+标题信息块现在是整块详情入口，不再依赖右侧小 info 图标，sheet 展示 Resource ID、状态/大小、Copy path 或 Open preview 主操作；artifact 行内 Copy 与 sheet 内 Copy path 成功后都会原地切到 `Copied`。同日继续补了 Run detail sticky section navigator，Review/Diff/Blocks/Outputs/Logs 五个 44px chip 可在长详情页中快速跳转，且点击后保留 active chip/`aria-current` 反馈，手机长页面能知道当前审阅 section。Diff preview 从整块横向滚动 `<pre>` 升级为移动端逐行渲染：文件级 +/− chips、hunk/add/delete/context 行分色、长行自动换行。Structured blocks 从普通卡片升级为移动端 review timeline：每个 run item 有左侧 rail、序号 chip、kind/role/time chips，并按 approval/diff/code/file/message 区分 rail/icon 色彩。Run logs 从单块 `<pre>` 升级为移动端日志行：line number、source chip、wrap text，并按 approval/diff/mobile/stderr 轻量分色；本轮继续补 All/Review/Diff/Mobile/Error 44px filter chips 和 pending review dock 下方滚动留白，Error 过滤后日志行不再被 dock 遮挡。截图 `app/mobile/screenshots/mobile-design-approval-diff-mocked-dark.png` 覆盖 390x844 dark mobile review 状态；`mobile-design-diff-lines-mocked-dark.png` 覆盖逐行 diff；`mobile-design-run-section-nav-outputs-mocked-dark.png` 覆盖 section nav 跳转到 Outputs 与 active Outputs chip；`mobile-design-run-blocks-mocked-dark.png` 覆盖结构化 run blocks timeline；`mobile-design-run-resources-mocked-dark.png` 覆盖 artifact/preview resources；`mobile-design-run-logs-mocked-dark.png` 覆盖移动端日志行与 active Logs chip；`mobile-design-run-logs-filter-error-mocked-dark.png` 覆盖 Error log filter；`mobile-design-resource-action-feedback-mocked-dark.png` / `mobile-design-light-resource-action-feedback-mocked.png` 覆盖 Copy action 状态反馈；`mobile-design-resource-detail-sheet-mocked-dark.png` / `mobile-design-light-resource-detail-sheet-mocked.png` 覆盖资源详情 sheet；`mobile-design-resource-detail-copy-mocked-dark.png` / `mobile-design-light-resource-detail-copy-mocked.png` 覆盖 sheet 内 Copy path 成功态；截图指标 `scrollWidth=390/innerWidth=390`，log filter chips 74-90x44，section chips 67-92x44，Approve/Reject 164x44，Copy/Open 68-69x44，资源详情信息块为 240/239x52，sheet Copy path/Close 165x44，底栏按钮 94x54。
- **Mobile visual QA 脚本（2026-05-27）**：新增 `app/mobile/scripts/visual-qa.mjs` 与 `corepack.cmd pnpm visual:qa`。脚本要求本地 Vite `localhost:5174` 运行，mock Hub workflow 数据，复截 Threads/Chat/Runs/approval diff/run blocks/resources/run detail/Settings，并阻断横向溢出、低于 44px 的 touch target、unexpected console output。本轮脚本首次运行暴露 Run detail Back 回 Threads 且未清 selectedRun 的交互 bug；已修为返回 Runs list。
- **Mobile queue filters（2026-05-27）**：Threads 页移除不可操作的 search placeholder，改成 All / Active / Archived 横向筛选 chips；Runs 页新增 All / Review / Active / Closed 横向筛选 chips，并把指标改为 Active / Review / Total，更符合手机端快速收敛待处理队列的交互逻辑。Runs filter toolbar 现在用四列自适应紧凑 chip，390px 下 Closed count 不再被右侧裁掉；Closed 计数现在与 Closed filter 一致，包含 finished/failed/cancelled，避免 reject 后 failed run 出现在终态列表但计数仍少 1。`visual:qa` 已扩展点击筛选并新增截图 `mobile-design-threads-filter-archived-mocked-dark.png`、`mobile-design-runs-filter-review-mocked-dark.png`；390x844 dark 下筛选按钮 92-122x44，无横向溢出。
- **Mobile queue refresh feedback（2026-05-27）**：Threads/Runs 概览卡的刷新不再只靠右上角图标旋转；refetch in-flight 时会在指标下方显示紧凑 `Refreshing ...` status pill，并给 44px refresh button 标记 `aria-busy`。`visual:qa` 通过第二次请求延迟模拟慢刷新，新增 `mobile-design-threads-refreshing-mocked-dark.png` 与 `mobile-design-runs-refreshing-mocked-dark.png`。
- **Mobile empty filter recovery（2026-05-27）**：Threads/Runs 筛选结果为空时不再只显示说明文字；空筛选态新增 `Show all` 44px CTA，直接恢复完整队列，避免手机端用户从空 chip 状态倒推如何退出。`visual:qa` 新增 dark 截图 `mobile-design-threads-empty-filter-mocked-dark.png`、`mobile-design-runs-empty-filter-mocked-dark.png` 与 light 截图 `mobile-design-light-threads-empty-filter-mocked.png`、`mobile-design-light-runs-empty-filter-mocked.png`。
- **Mobile filter-scoped shortcuts（2026-05-27）**：Threads 的 Continue handoff shortcut 只在 All/Active 语境展示，Archived filter 不再继续露出 active handoff；Runs 的 Next review shortcut 只在 All/Review 展示，Closed/Active filter 不再抢占顶部 viewport。`visual:qa` 复截 `mobile-design-threads-filter-archived-mocked-dark.png` 并新增 `mobile-design-runs-filter-closed-mocked-dark.png`、`mobile-design-light-runs-filter-closed-mocked.png`。
- **Mobile thread handoff shortcut（2026-05-27）**：`ThreadListView` 在存在 active thread 时新增 Continue handoff 快捷卡，直接展示下一条可继续线程和 last activity；本轮已从右侧箭头小按钮改成整卡 366x80 触控目标，更符合手机端快速继续任务的操作习惯。`visual:qa` 新增/复截 dark 截图 `mobile-design-threads-handoff-mocked-dark.png` 与 light 截图 `mobile-design-light-threads-handoff-mocked.png`。
- **Mobile threads queue rows（2026-05-27，2026-05-29 no-rail 口径更新）**：`ThreadListView` 的 thread row 与 Runs 队列保持密度一致，展示 last activity 时间和 project context 第二行；当前状态表达用 badges/icons/balanced borders，不再依赖左侧状态彩条。`visual:qa` 复截 `mobile-design-threads-handoff-mocked-dark.png`、`mobile-design-threads-filter-archived-mocked-dark.png` 与 `mobile-design-light-threads-mocked.png`，390x844 下 thread row 366x79，无横向溢出。
- **Mobile runs triage shortcut（2026-05-27）**：`RunListView` 在存在 waiting approval run 时新增 Next review 快捷卡，直接展示待审 run 和时间；本轮已从右侧箭头小按钮改成整卡 366x80 触控目标，减少手机端从队列筛选到审阅详情的精确点击要求。`visual:qa` 新增/复截 dark 截图 `mobile-design-runs-triage-mocked-dark.png` 与 light 截图 `mobile-design-light-runs-triage-mocked.png`。
- **Mobile tab-root navigation（2026-05-27）**：`App.tsx` 新增 Mobile 底栏导航处理；在 Run detail 中再次点击 active Runs tab 会清空 `selectedRun` 并回到 Runs 队列，符合手机 tab root 返回习惯，不再只能依赖顶部 Back。`visual:qa` 新增截图 `mobile-design-runs-tab-return-mocked-dark.png` 覆盖该路径。
- **Mobile bottom nav badges/safe area（2026-05-27）**：`BottomNav` 现在从共享 Threads/Runs query cache 读取 active thread 数和 waiting approval run 数，在 Threads/Runs tab 上显示紧凑数字 badge；badge 不改变底栏触控目标，并通过 accessible label 暴露 `2 active threads` / `1 pending reviews`。Approval decide 成功后 `RunStatusView` 会 invalidate `["runs"]`，让底栏 Runs pending badge 随 resolved checkpoint 清空；`visual:qa` 新增 `mobile-design-bottom-nav-badges-mocked-dark.png`，并在 approval/rejection success 截图中断言 Runs tab 不再带 pending reviews label。真实 Pixel 7 portrait WebView 发现 Android gesture handle 与选中 tab 背景过近后，`global.css` 新增 `--td-safe-bottom: max(16px, env(safe-area-inset-bottom))`，BottomNav、Latest floating button 和 run detail dock 预留统一使用该底部安全区。
- **Mobile i18n foundation（2026-05-27，2026-05-29 Account 口径更新）**：Mobile 已启用 `i18next` / `react-i18next`，默认跟随 `navigator.language`，语言选择持久化到 `agenthub.mobile.language`，并同步 `<html lang>`。Account 内保留 English / 简体中文 手机端语言切换入口；首批迁移覆盖全局 shell、BottomNav、Chat 空态和 shared recovery action。`visual:qa` 新增 `mobile-design-settings-language-zh-mocked-dark.png` 覆盖 390x844 中文 Settings/底栏；2026-05-29 起底栏右下入口改为 Account。
- **Mobile Settings i18n expansion（2026-05-27）**：Settings 首屏继续迁移到 i18n，新增 readiness/account/notification/about cards、native action status、retry action、clear-session confirmation sheet 的中英文 key；中文模式下 `mobile-design-settings-language-zh-mocked-dark.png` 现在显示 `运行时就绪`、`原生移动桥接`、`账户`、`登录`、`检查会话`、`通知`、`测试提醒`、`关于` 和本地化底栏。内置浏览器复核 `documentElement.lang=zh-Hans`、`gradientNodes=0`，Settings DOM 无 `Native mobile bridge` / `Sign in` / `Check session` 等首屏英文残留；`visual:qa` 复截仍保持 `scrollWidth=390/innerWidth=390` 和 >=44px touch targets。
- **Mobile queue i18n / tab-root polish（2026-05-27）**：Threads/Runs 队列继续迁移到 i18n，覆盖 header/status、overview、filters、refreshing、recovery、empty/filter-empty 和 queue status badge；当时用移动端本地 badge 解决 shared 英文固定标签问题。2026-05-30 已回收为 shared `StatusBadge` + consuming-app localized label，当前不再维护 `MobileStatusBadge`。同时修正 Runs bottom tab：从 Settings/Threads 回到 Runs 时会清掉旧 `selectedRun`，避免底部主导航把用户带回过期详情；底栏 active/hover 样式也已收敛，内置浏览器 hover QA 下不会把非当前 tab 看成当前 tab。`visual:qa` 新增/复截 `mobile-design-threads-zh-mocked-dark.png` 与 `mobile-design-runs-zh-mocked-dark.png`，390x844 下无横向溢出、按钮仍满足 44px。
- **Mobile Desktop-style glassmorphism pass（2026-05-27，2026-05-29 no-rail 口径更新）**：按 Desktop `glass-panel` / Settings glass tokens 收敛 Mobile 视觉，移除 `app/mobile/src/styles/global.css` 内所有 `linear-gradient` / `radial-gradient` / `conic-gradient`；新增 `--td-glass-panel`、`--td-glass-panel-strong`、`--td-glass-hover`、`--td-glass-shadow` 等 Mobile 玻璃 token。Threads/Runs overview、queue rows、handoff/triage cards、recovery cards、Run detail approval/diff/blocks/resources/log panels、Settings readiness/card surface 统一改为半透明 rgba + blur/saturate + 细边框 + 轻内高光，当前状态色收敛为 balanced border/icon/chip/full-row tint，不再做大面积渐变铺底或左侧彩条。内置浏览器 `localhost:5174` 已刷新并验证 DOM/导航可交互；`visual:qa` 复截 `mobile-design-after-threads-mocked-dark.png`、`mobile-design-run-blocks-mocked-dark.png`、`mobile-design-settings-language-zh-mocked-dark.png`、`mobile-design-light-runs-after-rejection-return-mocked.png` 等 mocked dark/light 证据；重建并安装 debug APK 后用 `$env:ADB_SERIAL='emulator-5556'; $env:AGENTHUB_EMULATOR_LAUNCH_DELAY_MS='9000'; corepack.cmd pnpm emulator:qa` 复截真实 WebView Threads/Chat/Runs/Settings，避免冷启动首屏空白。
- **Mobile runs queue rows（2026-05-27，2026-05-29 no-rail 口径更新）**：`RunListView` 的 run row 保留 thread context 第二行；Review/Active/Closed 现在通过整行玻璃 tint、balanced 1px border、状态 icon 和 status badge 扫读，且可直接看到 run 属于哪个 thread。`visual:qa` 复截 `mobile-design-runs-triage-mocked-dark.png` 与 `mobile-design-runs-filter-review-mocked-dark.png`，390x844 下 run row 366x79，无横向溢出；2026-05-29 新增 Playwright 证据 `mobile-runs-status-no-rails-pw-390x844.png`，probe 断言四种状态 row 370x86、无 left-only border / inset rail / gradient、最后一行可滚到 bottom nav 上方。
- **Mobile run detail summary strip（2026-05-27）**：`RunStatusView` 顶部 Run detail panel 的 summary strip 已覆盖 Review / Diff / Blocks / Outputs / Logs，进入详情即能判断审批、文件变更、结构化 block、输出资源和日志规模，再决定是否跳转到对应 section。`visual:qa` 新增 dark 截图 `mobile-design-run-summary-mocked-dark.png` 与 light 截图 `mobile-design-light-run-summary-mocked.png`。
- **Mobile run summary shortcuts（2026-05-27）**：`RunStatusView` 的 Review/Diff/Blocks/Outputs/Logs summary tiles 已从静态指标升级为 54px+ 可点击 section shortcuts，点击会直接滚到对应 section，并同步 sticky section nav active state。`visual:qa` 新增 `mobile-design-run-summary-shortcut-blocks-mocked-dark.png`，覆盖从顶部 Blocks summary 直达 structured blocks 的手机路径。
- **Mobile run section scroll-spy（2026-05-27）**：`RunStatusView` 的 sticky Review/Diff/Blocks/Outputs/Logs section nav 不再只依赖点击更新 active chip；移动端手动滚动到对应 section 时会根据 scroll container 位置自动更新 active section。`visual:qa` 新增 `mobile-design-run-scroll-spy-blocks-mocked-dark.png`，验证滚到 structured blocks 后 Blocks chip 自动成为当前 section。
- **Mobile active section chip auto-reveal（2026-05-27）**：`RunStatusView` 现在会在 `activeSection` 变化时把对应 sticky section chip 横向滚入可见区域，并补了底部判定，解决 Logs 作为最后一个 section 时标题无法越过原阈值、active state 停在 Outputs 的问题。本轮继续把 chip auto-reveal 从居中滚动改为 nearest-edge 滚动，并给 section nav 增加 inline scroll padding，避免 Blocks/Logs 场景把相邻 chip 硬切在 390px 视口边缘。`visual:qa` 新增 `mobile-design-run-scroll-spy-logs-mocked-dark.png`，覆盖手动滚到 Logs 后 Logs chip 自动显现。
- **Mobile review action dock（2026-05-27）**：`RunStatusView` 对 pending approval 新增底部审阅 dock，固定在 Run detail scroll 区外、BottomNav 上方；用户滚到 Diff / Blocks / Outputs / Logs 后仍能直接 Approve / Reject。新增截图 `mobile-design-review-action-dock-mocked-dark.png`，且 `mobile-design-run-section-nav-outputs-mocked-dark.png` 复截证明滚到 Outputs 时 dock 仍可见；dock 按钮 108x44，页面无横向溢出。
- **Mobile approval confirmation sheet（2026-05-27）**：Run detail 的 Approval panel 和底部 review dock 不再直接提交 Approve/Reject；首次点击会打开移动端 bottom confirmation sheet，展示 decision、request kind、run id，并提供 `Confirm approve/reject` 与 `Cancel`，降低手机误触审批风险。Approve/Reject 现在使用 decision-specific sheet tone：Approve 展示 Continue run 摘要，Reject 展示危险态 Reject checkpoint 摘要和红色 confirm 按钮。提交时 sheet 会保持打开并在确认动作附近显示 `Submitting approval decision to Hub...`，禁用 scrim/close/confirm/cancel 以避免重复提交；Hub/session 失败时在 sheet 内显示 `Decision was not submitted. Check Hub session and retry.`，重新打开决策 sheet 会 reset 旧错误；失败态下 Confirm/Cancel 留在 sheet 内可继续操作；transient 503 后再次 Confirm 可恢复成功并关闭 sheet，Approve/Reject 两条路径均有 dark/light 截图覆盖；成功后 Run detail 将 pending 双按钮替换为只读 approved/rejected decision lock，Run header badge 与 Review summary tile 同步切到 done/error 与 moss/danger 状态色，Reject success 使用危险态反馈和 error status badge，收起底部 review dock，并给出 44px `Back to queue` 下一步动作。`visual:qa` 新增截图 `mobile-design-approval-confirm-sheet-mocked-dark.png`、`mobile-design-reject-confirm-sheet-mocked-dark.png`、`mobile-design-approval-submit-pending-mocked-dark.png`、`mobile-design-approval-submit-error-mocked-dark.png`、`mobile-design-rejection-submit-error-mocked-dark.png`、`mobile-design-approval-submit-retry-success-mocked-dark.png`、`mobile-design-rejection-submit-retry-success-mocked-dark.png`、`mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-rejection-submit-error-mocked.png`、`mobile-design-light-approval-submit-retry-success-mocked.png`、`mobile-design-light-rejection-submit-retry-success-mocked.png`、`mobile-design-approval-submit-success-mocked-dark.png`、`mobile-design-light-approval-submit-success-mocked.png`、`mobile-design-rejection-submit-success-mocked-dark.png` 与 `mobile-design-light-rejection-submit-success-mocked.png`，确认 sheet 按钮 165x44，无横向溢出。
- **Mobile light-mode visual QA（2026-05-27）**：`visual:qa` 已追加 light color scheme 覆盖，复用 mocked Hub workflow 截 `mobile-design-light-threads-mocked.png`、`mobile-design-light-review-dock-mocked.png` 与 `mobile-design-light-approval-confirm-sheet-mocked.png`。浅色模式下 Threads 卡片、筛选 chips、Run detail approval card、底部 review dock 和审批确认 sheet 均保留桌面端 light-first glass/compact surface 方向；390x844 下 `scrollWidth=390/innerWidth=390`，触控目标保持 >=44px。
- **Mobile chat context panel（2026-05-27）**：`ChatView` 顶部 summary 从 Project/items 单行升级为 thread context panel，展示 project、thread status、message count、last activity、Hub handoff scope，避免手机对话页脱离 Desktop/Hub 线程上下文。新增截图 `mobile-design-chat-context-mocked-dark.png`；首次截图暴露 CSS 级联导致 project 名被挤成三行，已修正为竖向 header + 三枚紧凑 chip，390x844 下无横向溢出。
- **Mobile chat activity cards（2026-05-27）**：`ChatView` 不再丢弃非 message 的 `ThreadItem`；approval/diff/code/file 等线程活动会渲染为移动端 compact activity card，带类型 icon、左侧 rail、role/time meta 和正文，保留 Desktop typed activity 语义。`visual:qa` mock 已加入 approval + diff thread item，并新增 dark 截图 `mobile-design-chat-activity-cards-mocked-dark.png` 与 light 截图 `mobile-design-light-chat-activity-cards-mocked.png`；390x844 下 `scrollWidth=390/innerWidth=390`。
- **Mobile chat latest jump（2026-05-27）**：`ChatView` 新增 scroll-aware `Latest` 浮动按钮；当手机用户上滑查看线程上下文、离最新消息超过阈值时显示 92x44 回到底部操作，点击后滚回 latest anchor 并隐藏按钮，避免长线程里手动拖到底。`visual:qa` 新增 `mobile-design-chat-latest-jump-mocked-dark.png` 并点击验证返回 latest。
- **Mobile chat copy feedback（2026-05-27）**：`ChatView` 的 message bubble 与 activity card 新增 44px `Copy` 操作，复制后在原按钮位置切换为 `Copied` 成功反馈，避免手机审阅时依赖系统文本选择复制 Agent 输出或用户指令。`visual:qa` 新增 `mobile-design-chat-copy-feedback-mocked-dark.png` 覆盖 Agent message copy 路径。
- **Mobile chat empty CTA（2026-05-27）**：当时 `MobileEmptyState` 增加可选 action，Chat tab 在尚未选择 thread 时不再停在纯说明空态；新增 `Browse threads` CTA 可直接回到 Threads 队列。2026-05-30 已回收为 shared `EmptyState` + Mobile class overrides，当前不再维护 `MobileEmptyState`。`visual:qa` 新增 dark 截图 `mobile-design-chat-empty-cta-mocked-dark.png` 与 light 截图 `mobile-design-light-chat-empty-cta-mocked.png`。
- **Mobile chat composer scope（2026-05-27）**：`ChatView` 在 composer 上方新增持久 reply scope chips，展示 project、message count、thread status；用户滚到底部输入时仍能看到本次回复归属，贴近 Desktop composer scope 的移动端压缩版。`visual:qa` 新增 dark 截图 `mobile-design-chat-composer-scope-mocked-dark.png` 与 light 截图 `mobile-design-light-chat-composer-scope-mocked.png`。
- **Mobile chat send feedback（2026-05-27）**：`ChatView` 的发送 pending/error/success 反馈从消息滚动区移到 composer dock 与对话流底部：慢 POST 时显示 `Sending` 用户气泡，mocked 503 时显示 `Not sent` 用户气泡且失败 draft 会回填到 composer，失败状态行提供 44px `Retry` 明确重试动作，transient 503 后点击 Retry 可恢复到 `Sent` 状态，成功后保留 `Sent` 用户气泡直到 Hub replay 包含新消息。新增 latest-message scroll anchor，发送后自动滚到最新气泡，避免手机端用户滚动查错或重输。`visual:qa` 新增慢 POST 截图 `mobile-design-chat-send-pending-mocked-dark.png`、mocked 503 截图 `mobile-design-chat-send-error-mocked-dark.png` / `mobile-design-chat-send-error-retry-mocked-dark.png` / `mobile-design-light-chat-send-error-retry-mocked.png`、Retry 恢复截图 `mobile-design-chat-send-retry-success-mocked-dark.png` / `mobile-design-light-chat-send-retry-success-mocked.png` 与成功截图 `mobile-design-chat-send-success-mocked-dark.png`。
- **Mobile chat tab-root navigation（2026-05-27）**：`App.tsx` 补齐 Chat active tab root 行为；在 Chat 线程详情中再次点击底栏 Chat 会清空 `selectedThread` 并返回 Chat 根空态，与 Runs 详情二次点击 Runs 返回队列一致。`visual:qa` 新增 dark 截图 `mobile-design-chat-tab-root-mocked-dark.png` 与 light 截图 `mobile-design-light-chat-tab-root-mocked.png`。
- **Mobile chat thread recovery（2026-05-27）**：`ChatView` 的 thread items 拉取失败态从居中错误块改为复用 `MobileRecoveryPanel` 的上下文内恢复卡，保留 thread context panel，并提供 Retry 与 Threads 两个 44px 操作；timeline 未同步时 composer 会切到只读 paused 状态，不再展示 stale Send 输入；`visual:qa` 新增 mocked 503 截图 `mobile-design-chat-recovery-mocked-dark.png` 与 `mobile-design-light-chat-recovery-mocked.png`，同时验证次级 Threads action 可回到线程队列。
- **Mobile settings readiness panel（2026-05-27）**：`SettingsView` 顶部新增 Runtime readiness 面板，把 TokenDance ID、Hub session native command、Notifications permission gate 三类移动端运行准备度收拢为紧凑状态行，避免 Settings 只剩分散按钮。`visual:qa` 新增 dark 截图 `mobile-design-settings-readiness-mocked-dark.png` 与 light 截图 `mobile-design-light-settings-readiness-mocked.png`。
- **Mobile settings readiness commands（2026-05-27）**：Runtime readiness 三个 tile 已从静态状态行升级为可点击 mobile command targets：TokenDance ID tile 启动登录，Hub session tile 直接检查 native storage，Notifications tile 触发 notification probe。Settings 原生命令状态面板现在会在状态变化时自动滚入当前滚动容器，触发过的 readiness tile 会保留 active anchor，避免短屏点击首屏 tile 后反馈留在折叠区或失去动作来源。`visual:qa` 新增 `mobile-design-settings-readiness-tile-action-mocked-dark.png` 与 390x640 短屏 `mobile-design-settings-compact-feedback-mocked-dark.png`，覆盖从首屏 Hub session tile 触发 action feedback 的路径。
- **Mobile settings action feedback（2026-05-27）**：`SettingsView` 原生命令按钮在 pending 时会禁用，状态反馈升级为带图标的 `role=status` panel，并把浏览器预览下的 Tauri bridge 缺失错误归一为可读反馈。`visual:qa` 新增点击 Check session 后的 dark 截图 `mobile-design-settings-action-feedback-mocked-dark.png` 与 light 截图 `mobile-design-light-settings-action-feedback-mocked.png`。
- **Mobile settings clear confirmation（2026-05-27）**：`SettingsView` 的 destructive Clear session 不再直接清 native Hub token；点击后先打开移动端 bottom confirmation sheet，展示 Storage/Effect 影响，并提供 `Confirm clear` 与 `Cancel` 两个 44px 操作，避免手机误触退出。确认后 sheet 不再立即关闭：pending 时显示 `Clearing stored Hub session...` 并锁住 scrim/关闭/重复提交，native bridge/secure-store 失败时在 sheet 内显示错误与 `Retry clear`，成功后才关闭并回到 Settings 状态面板。`visual:qa` 新增 dark 截图 `mobile-design-settings-clear-confirm-mocked-dark.png`、`mobile-design-settings-clear-error-mocked-dark.png` 与 light 截图 `mobile-design-light-settings-clear-confirm-mocked.png`、`mobile-design-light-settings-clear-error-mocked.png`。
- **Mobile recovery states（2026-05-27）**：新增 `MobileRecoveryPanel`，Threads/Runs 在 Hub/API 失败时不再退回居中死端错误态，而是在队列上下文内展示紧凑恢复卡、最近尝试时间、44px Retry 和 Settings 二级动作；`visual:qa` 新增 mocked 503 截图 `mobile-design-threads-recovery-mocked-dark.png`、`mobile-design-runs-recovery-mocked-dark.png`、`mobile-design-light-threads-recovery-mocked.png`、`mobile-design-light-runs-recovery-mocked.png`，并新增 `mobile-design-runs-recovery-settings-mocked-dark.png` 验证 recovery card 可直接跳到 Settings native bridge readiness。
- **UI 验收**：本轮已重打 debug APK、安装到 AVD `agenthub-emu` / `emulator-5556`，并用 `adb exec-out screencap` 截图验收 Threads、Chat empty、Runs empty、Settings 四个入口。截图在 `app/mobile/screenshots/mobile-ui-threads-emulator.png`、`mobile-ui-chat-empty-emulator.png`、`mobile-ui-runs-empty-emulator.png`、`mobile-ui-settings-emulator.png`。
- **Android WebView CSP/CORS 复验（2026-05-27）**：真实 emulator 起初黑屏/Offline 后，经 logcat 发现旧 Hub URL 的 CSP 拦截；重建并安装 debug APK 后 CSP 报错消失，但 WebView fetch 仍会被生产 Hub 对 `http://tauri.localhost` 的 CORS 策略阻止，截图为离线错误态而非 live thread list。随后 Mobile REST host 已切到 `http://api.hub.vectorcontrol.tech` 并接入 Tauri native `hub_request` bridge，下一步需要重装 APK 复验 live Hub。历史截图：`app/mobile/screenshots/mobile-ui-threads-emulator-csp-fixed.png`、`mobile-ui-settings-emulator-csp-fixed.png`。
- **Android native bridge 复验（2026-05-27）**：本轮用当前源码重新 `tauri android build --debug`、`adb install -r` 到 emulator `127.0.0.1:16384`，并截 `app/mobile/screenshots/mobile-ui-threads-native-bridge-emulator.png`、`mobile-ui-runs-native-bridge-emulator.png`、`mobile-ui-settings-native-bridge-emulator.png`。Threads/Runs 在真实 WebView 中显示新的 recovery card；logcat 未再出现 CSP/CORS/Access-Control 报错。剩余 live-Hub blocker 已缩小为 API contract：公网 `http://api.hub.vectorcontrol.tech/health` 返回 JSON，但 `http://api.hub.vectorcontrol.tech/v1/health` 与 `/v1/threads` 返回空 `text/plain 200`，Mobile 当前 shared Edge-style `/v1/*` client 无法从部署态 Hub 得到 JSON workflow 数据。
- **Mobile Hub reachability split（2026-05-27）**：Threads/Runs 不再把 workflow `/v1/*` JSON 同步失败误报为 Hub Offline。新增 Mobile 专用 `src/native/hubHealth.ts` 直接检查部署态 `http://api.hub.vectorcontrol.tech/health`；当 `/health` reachable 但 Threads/Runs endpoint 不返回 workflow JSON 时，顶部状态显示 `Reachable`，signal row 分别显示 `Hub reachable; workflow sync pending` / `Hub reachable; run sync pending`，恢复卡标记为 `Workflow recovery` 并解释 API/session contract 未就绪。Threads 已重建安装 APK 并截 `app/mobile/screenshots/mobile-ui-threads-health-reachable-emulator.png`；Runs 先由 `visual:qa` 复截 `app/mobile/screenshots/mobile-design-runs-recovery-mocked-dark.png`，随后重建安装 APK 并截真实 WebView `app/mobile/screenshots/mobile-ui-runs-health-reachable-emulator.png`。
- **Mobile emulator QA 复验（2026-05-27）**：用当前源码重新 `tauri android build --debug` 并 `adb install -r` 到 emulator `127.0.0.1:16384`；在真实 Tauri WebView 中点击 Sign in 后，Rust OIDC stub 返回 `not yet implemented — coordinate with desktop agent on OIDC flow`，状态面板保留 `Retry sign in` 44px 恢复动作。新增 `corepack.cmd pnpm emulator:qa`，自动通过 adb 启动 `com.agenthub.mobile`、复截 Threads/Runs 真实 workflow recovery 状态、进入 Settings、触发 Sign in 并复截 `mobile-ui-current-emulator.png` / `mobile-ui-threads-emulator-current.png` / `mobile-ui-runs-emulator-current.png` / `mobile-ui-settings-emulator-current.png` / `mobile-ui-settings-login-recovery-emulator.png`。截图：`app/mobile/screenshots/mobile-ui-threads-emulator-current.png`、`app/mobile/screenshots/mobile-ui-runs-emulator-current.png`、`app/mobile/screenshots/mobile-ui-settings-login-recovery-emulator.png`。
- **验证命令**：`cd app/mobile && corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm tauri android build --debug` 均已通过。本轮 APK 仍是 universal debug 包，体积问题仍归 APK release/single ABI 待办。2026-05-26 接手补充 native bridge 后，`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build` 与 `$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; corepack.cmd pnpm tauri android build --debug` 再次通过；2026-05-27 已完成 Threads/Runs 重装复截。
  - 2026-05-27 Mobile UI/design pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；Playwright mocked workflow 截图通过 `scrollWidth=390/innerWidth=390`，底栏按钮 94x54，active Runs tab 可从 detail 返回 390px 队列页，shortcut cards 366x80，Threads row 366x79，Runs row 366x79，Chat empty CTA 160x44，Chat activity cards 无横向溢出，Settings action buttons 44px 高，section nav chips 67-92x44，log filter chips 74-90x44，approval Approve/Reject 164x44，resource Copy/Open 44px 高，resource detail sheet Copy path/Close 165x44。`visual:qa` 曾拦截 Copy/Open 仅 38px 高的问题，已修正为 44px；section nav 初版截图只露出按钮上沿，已修为 sticky 50px 高导航条并重截 `mobile-design-run-section-nav-outputs-mocked-dark.png`。`D:\Code\TokenDance\scripts\verify-design-hygiene.ps1` 通过但输出既有 warnings（主要在 `agenthub-home` / `tokendance-org`），本轮未新增 Mobile 阻断项。
  - 2026-05-27 Android WebView pass：`$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; corepack.cmd pnpm tauri android build --debug` 通过；`adb install -r app-universal-debug.apk` 成功；`adb exec-out screencap -p` 截到 Threads/Settings 真实 emulator 画面；logcat 证明 CSP blocker 已变为 Hub CORS blocker。
  - 2026-05-27 Mobile API host/native bridge follow-up：Mobile REST/WS host 已对齐当前公网 `http://api.hub.vectorcontrol.tech` / `ws://api.hub.vectorcontrol.tech/ws`，Tauri runtime 下 shared client 会走 Rust `hub_request`，Rust 侧只 allowlist `http://api.hub.vectorcontrol.tech/`。`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 已重新通过；`visual:qa` mock 路由同步为 `http://api.hub.vectorcontrol.tech/v1/**`，390x844 dark workflow 仍为 `scrollWidth=390/innerWidth=390`，触控目标均 >=44px。公网复查：`http://api.hub.vectorcontrol.tech/health` 返回 `status=ok`、`migrations=37`，HTTPS 仍不可用；随后已重建安装 APK 复验 native bridge。
  - 2026-05-27 Mobile Hub reachability split：Threads 侧 `corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；`$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; corepack.cmd pnpm tauri android build --debug` 通过；`adb install -r` 成功；`adb exec-out screencap -p` 截到 `mobile-ui-threads-health-reachable-emulator.png`，真实 WebView 顶部为 `Reachable`，signal row 为 `Hub reachable; workflow sync pending`，logcat 过滤未见 CSP/CORS/Access-Control blocker。随后 Runs 侧补齐同一状态模型，`visual:qa` 复截 `mobile-design-runs-recovery-mocked-dark.png` 覆盖 `Reachable` + `Hub reachable; run sync pending` + `Workflow recovery`；再次 `tauri android build --debug`、`adb install -r` 后截 `mobile-ui-runs-health-reachable-emulator.png`，真实 WebView 顶部为 `Reachable`，Runs signal row 为 `Hub reachable; run sync pending`。
  - 2026-05-27 Mobile filter interaction pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Threads Archived filter 与 Runs Review filter 截图，`visual:qa` 证明筛选 chips 44px 高、页面 `scrollWidth=390/innerWidth=390`、无 unexpected console output。
  - 2026-05-27 Mobile queue refresh feedback pass：`visual:qa` 新增慢刷新 mocked 截图，Threads/Runs 在 refetch 中分别显示 `Refreshing thread handoff...` / `Refreshing run queue...` status pill；刷新按钮 44x44 且带 `aria-busy`，页面保持 `scrollWidth=390/innerWidth=390`。
  - 2026-05-27 Mobile empty filter recovery pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；新增 Threads/Runs 空筛选 dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Show all CTA 102x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile filter-scoped shortcut pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Archived filter 截图确认 Continue handoff 不再出现，Closed filter dark/light 截图确认 Next review 不再出现；390x844 下 `scrollWidth=390/innerWidth=390`，Closed row 366x79，筛选 chips 92x44，无 unexpected console output。
  - 2026-05-27 Mobile review dock pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 pending approval 底部 action dock 截图，`visual:qa` 证明 dock Approve/Reject 均为 108x44，滚到 Outputs 后仍保留可见审阅动作。
  - 2026-05-27 Mobile approval confirmation pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；新增 approval confirmation bottom sheet 截图 `mobile-design-approval-confirm-sheet-mocked-dark.png`，后续补充 reject 危险态截图 `mobile-design-reject-confirm-sheet-mocked-dark.png`；Confirm/Cancel 均为 165x44，关闭按钮 44x44，页面 `scrollWidth=390/innerWidth=390`，无 unexpected console output。
  - 2026-05-27 Mobile approval submit feedback pass：approval confirmation sheet 已补提交 pending/error 原地反馈，慢 decide POST 时 sheet 保持打开并禁用 dismiss/重复提交，mocked 503 时 sheet 内显示失败状态；Approve/Reject 失败态都保留 Confirm 与 Cancel 操作；新增 transient 503 retry-success 路径，第二次 Confirm 后 sheet 关闭并进入 approved/rejected decision lock，Approve/Reject 两条路径均覆盖 dark/light；新增/复截 `mobile-design-approval-submit-pending-mocked-dark.png`、`mobile-design-approval-submit-error-mocked-dark.png`、`mobile-design-rejection-submit-error-mocked-dark.png`、`mobile-design-approval-submit-retry-success-mocked-dark.png`、`mobile-design-rejection-submit-retry-success-mocked-dark.png`、`mobile-design-light-approval-submit-error-mocked.png`、`mobile-design-light-rejection-submit-error-mocked.png`、`mobile-design-light-approval-submit-retry-success-mocked.png` 与 `mobile-design-light-rejection-submit-retry-success-mocked.png`，通过 `corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 和 Mobile/doc 范围 `git diff --check` 验证。
  - 2026-05-27 Mobile approval success feedback pass：approval decide 成功后会记录本次 decision，Run detail 原地显示 approved/rejected decision lock，替代成功态里误导性的禁用 Approve/Reject 按钮，并提供 `Back to queue` 44px 下一步动作；Run header badge 与 Review summary tile 现在随 approved/rejected 切 done/error 和 moss/danger 语义色；同时 invalidate Runs query，确保底栏 pending review badge 随 resolved checkpoint 清空；`visual:qa` 将 mocked approvals list 和 runs list 在 decide 后切到 approved/rejected/non-review，新增/复截 `mobile-design-approval-submit-success-mocked-dark.png`、`mobile-design-light-approval-submit-success-mocked.png`、`mobile-design-rejection-submit-success-mocked-dark.png` 与 `mobile-design-light-rejection-submit-success-mocked.png` 覆盖 sheet 关闭后的 approve/reject 成功态，并新增 `mobile-design-runs-after-approval-return-mocked-dark.png` / `mobile-design-runs-after-rejection-return-mocked-dark.png` / `mobile-design-light-runs-after-approval-return-mocked.png` / `mobile-design-light-runs-after-rejection-return-mocked.png` 覆盖 `Back to queue` 后 Runs 队列 `Review0`、无 stale `Next review`、无 pending review badge，以及 reject 后 Closed count 包含 failed run。
  - 2026-05-27 Mobile light-mode pass：`corepack.cmd pnpm visual:qa` 已覆盖 light color scheme，新增 Threads、Review dock 与 approval confirmation sheet 浅色截图；浅色截图目视复核通过，确认 sheet 的 Confirm/Cancel 均为 165x44，关闭按钮 44x44，验证结果仍无横向溢出、无低于 44px 的按钮、无 unexpected console output。
  - 2026-05-27 Mobile chat context pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Chat context 截图，`visual:qa` 复截后确认 context panel 不再挤压换行，页面 `scrollWidth=390/innerWidth=390`。
  - 2026-05-27 Mobile chat latest jump pass：`ChatView` 新增离底检测和 `Latest` 浮动按钮，`visual:qa` 将 Chat scroll container 拉到顶部后截 `mobile-design-chat-latest-jump-mocked-dark.png`，按钮 92x44，点击后回到 latest anchor 并消失。
  - 2026-05-27 Mobile chat copy feedback pass：`ChatView` 为 message 与 activity card 补 44px Copy/Copied 操作，`visual:qa` 点击 Agent message copy 后新增 `mobile-design-chat-copy-feedback-mocked-dark.png`；390x844 下无横向溢出，copy 按钮仍满足 >=44px touch target。
  - 2026-05-27 Mobile chat composer scope pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Chat composer scope dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Send 按钮 78x52，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile chat send feedback pass：`corepack.cmd pnpm visual:qa` 新增 Chat send pending/error/success 路径；慢 POST 时对话流显示 `Sending` 临时用户气泡且 composer dock 显示发送中状态，mocked 503 时对话流显示 `Not sent` 用户气泡、composer dock 显示可见失败反馈、保留 draft，并提供 44px `Retry` 明确重试动作；新增 transient 503 retry-success 路径，点击 Retry 后恢复为 `Sent`，Retry action 清空；成功 POST 后保留 `Sent` 用户气泡直到 Hub replay 包含该 user message；截图 `mobile-design-chat-send-pending-mocked-dark.png` / `mobile-design-chat-send-error-mocked-dark.png` / `mobile-design-chat-send-error-retry-mocked-dark.png` / `mobile-design-chat-send-retry-success-mocked-dark.png` / `mobile-design-light-chat-send-error-retry-mocked.png` / `mobile-design-light-chat-send-retry-success-mocked.png` / `mobile-design-chat-send-success-mocked-dark.png` 均保持 390x844 无横向溢出和 >=44px 按钮。
  - 2026-05-27 Mobile chat tab-root pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Chat 线程详情中点击 active Chat tab 会回到 Chat 根空态，新增 dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Browse threads CTA 160x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile chat recovery pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Chat thread items mocked 503 时显示上下文内 recovery card，保留 thread context，composer 切只读 paused 状态且不再展示 Send action，Retry/Threads 按钮均为 166x44，dark/light 截图 `mobile-design-chat-recovery-mocked-dark.png` / `mobile-design-light-chat-recovery-mocked.png` 证明 390x844 下 `scrollWidth=390/innerWidth=390`、无 unexpected console output。
  - 2026-05-27 Mobile bottom nav badge/safe-area pass：`App.tsx` 复用 `threads` / `runs` query cache 计算 active thread 与 waiting approval run 数，`BottomNav` 在 Threads/Runs tab 显示 compact badge；`visual:qa` 新增 `mobile-design-bottom-nav-badges-mocked-dark.png`，并断言 accessible nav label 包含 `2 active threads` 与 `1 pending reviews`。真实 Pixel 7 portrait 截图暴露 Android gesture handle 与选中 tab 背景距离过近后，底栏改用 `--td-safe-bottom` 最小 16px 预留，随后需用 portrait `emulator:qa` 复截确认。
  - 2026-05-27 Mobile settings readiness pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Settings readiness dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Settings action buttons 79-131x44，底栏按钮 94x54，无 unexpected console output。目视复核确认 Stub / Bridge / Probe 状态不再全部用成功色，暗色和浅色均无文字挤压。
  - 2026-05-27 Mobile settings readiness commands pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Runtime readiness tiles 改为首屏 command targets，新增 `mobile-design-settings-readiness-tile-action-mocked-dark.png`，390x844 下 Hub session tile 334x54，点击后显示 native bridge unavailable action feedback，无横向溢出、无 unexpected console output。随后补齐短屏反馈可达性与动作锚点：状态面板会在 command 状态变化时滚入视口，触发过的 readiness tile 保持 active，`visual:qa` 新增 `mobile-design-settings-compact-feedback-mocked-dark.png` 并断言 390x640 下 status panel 位于 viewport 内、Hub session tile 保持 active。
  - 2026-05-27 Mobile settings action feedback pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Settings Check session feedback dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Settings action buttons 79-131x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile settings login recovery pass：`SettingsView` 的 native action error panel 补 44px Retry 动作；Sign in/OIDC stub 或 browser preview native bridge 失败后，状态面板会显示 `Retry sign in`，并保留 TokenDance ID readiness tile active anchor。状态面板现在用 header-aware `scrollIntoView({ block: "start" })`、上下 scroll margin 和 Settings 专用底部 scroll reserve 定位，避免恢复按钮贴着底栏，同时减少状态反馈后上方 Settings context 被 header 随机截断。`visual:qa` 新增/复截 `mobile-design-settings-login-recovery-mocked-dark.png` 与 `mobile-design-light-settings-login-recovery-mocked.png`。
  - 2026-05-27 Mobile settings clear error feedback pass：Settings Clear confirmation sheet 已补 native clear pending/error 原地反馈；browser preview 下 Confirm clear 会保留 sheet 并显示 `Native bridge is unavailable in browser preview.`，按钮切到 `Retry clear`，避免 destructive action 失败后用户回到背后页面找状态。`visual:qa` 新增/复截 `mobile-design-settings-clear-error-mocked-dark.png` 与 `mobile-design-light-settings-clear-error-mocked.png`，并继续检查 390px 无横向溢出和 >=44px touch targets。
  - 2026-05-27 Mobile runs triage pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Runs Next review dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Next review 打开按钮 44x44，筛选 chips 92x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile runs row triage pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Runs queue row 增加状态色 rail 和 thread context，`mobile-design-runs-triage-mocked-dark.png` / `mobile-design-runs-filter-review-mocked-dark.png` 复截后 run row 366x79，页面 `scrollWidth=390/innerWidth=390`，无 unexpected console output。
  - 2026-05-27 Mobile thread handoff pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Threads Continue handoff dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Continue handoff 打开按钮 48x48，筛选 chips 92-122x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile run detail summary pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Run detail summary dark/light 截图，summary strip 覆盖 Review/Diff/Blocks/Outputs/Logs，390x844 下 `scrollWidth=390/innerWidth=390`，section nav chips 67-92x44，Approve/Reject 164x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile run summary shortcuts pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Run detail summary tiles 改为可点击 section shortcuts，新增 `mobile-design-run-summary-shortcut-blocks-mocked-dark.png`，390x844 下 Blocks summary tile 为 107x54，点击后 Blocks sticky nav chip 82x44 保持 active，底部 review dock 按钮 108x44，无 unexpected console output。
  - 2026-05-27 Mobile output resource target pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Run detail Outputs 资源行去掉小 info 图标依赖，图标+标题信息块成为整块详情入口，artifact 行内 Copy 与 sheet 内 Copy path 成功后会原地切到 `Copied`；`mobile-design-run-resources-mocked-dark.png`、`mobile-design-resource-action-feedback-mocked-dark.png`、`mobile-design-resource-detail-sheet-mocked-dark.png`、`mobile-design-resource-detail-copy-mocked-dark.png`、`mobile-design-light-resource-action-feedback-mocked.png`、`mobile-design-light-resource-detail-sheet-mocked.png` 与 `mobile-design-light-resource-detail-copy-mocked.png` 复截后 resource info target 为 240/239x52，Copy/Open 为 68-69x44，无横向溢出、无 unexpected console output。
  - 2026-05-27 Mobile diff readability pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Diff preview 改为逐行 add/delete/hunk/context 渲染并新增 `mobile-design-diff-lines-mocked-dark.png`，390x844 下 `scrollWidth=390/innerWidth=390`，无 unexpected console output。
  - 2026-05-27 Mobile structured blocks timeline pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Run detail structured blocks 改为移动端 timeline 卡片，包含序号、kind/role/time chips 和类型色 rail/icon；复截 `mobile-design-run-blocks-mocked-dark.png` 与 `mobile-design-light-run-blocks-mocked.png`，390x844 下 `scrollWidth=390/innerWidth=390`，section nav chips 67-92x44，底部 review dock 按钮 108x44，无 unexpected console output。
  - 2026-05-27 Mobile logs readability pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Run logs 从 raw `<pre>` 改为移动端日志行，stdout/stderr 拆成 line number + source chip + wrapped text，并按 approval/diff/mobile/stderr 分色；新增 `mobile-design-run-logs-mocked-dark.png`，390x844 下 `scrollWidth=390/innerWidth=390`，Logs section nav chip 72x44，底部 review dock 按钮 108x44，无 unexpected console output。
  - 2026-05-27 Mobile section nav active-state pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa` 通过；Run detail sticky section nav 点击后设置 active chip 与 `aria-current`，`mobile-design-run-section-nav-outputs-mocked-dark.png` 显示 Outputs active，`mobile-design-run-logs-mocked-dark.png` 显示 Logs active，390x844 下无横向溢出，section nav chips 保持 67-92x44。
  - 2026-05-27 Mobile section scroll-spy pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Run detail scroll container 监听手动滚动并自动更新 active section，新增 `mobile-design-run-scroll-spy-blocks-mocked-dark.png`，390x844 下 `scrollWidth=390/innerWidth=390`，Blocks chip 82x44，底部 review dock 按钮 108x44，无 unexpected console output。
  - 2026-05-27 Mobile active section chip auto-reveal pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Run detail `activeSection` 变化时会把对应 sticky section chip 横向滚入可见区域，并在滚到底部时将最后一个 Logs section 置为 active；新增 `mobile-design-run-scroll-spy-logs-mocked-dark.png`，390x844 下手动滚到 Logs 后 Logs chip 72x44 可见，底部 review dock 按钮 108x44，无 unexpected console output。
  - 2026-05-27 Mobile recovery state pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`corepack.cmd pnpm visual:qa` 通过；新增 Threads/Runs mocked 503 dark/light 截图，390x844 下 `scrollWidth=390/innerWidth=390`，Recovery Retry/Settings 按钮均为 166x44，底栏按钮 94x54，无 unexpected console output。
  - 2026-05-27 Mobile recovery settings action pass：`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm visual:qa`、`corepack.cmd pnpm build` 通过；Recovery card 新增 Settings 二级动作，Runs recovery 截图验证 Retry/Settings 均为 166x44，并点击 Settings 后截 `mobile-design-runs-recovery-settings-mocked-dark.png`，390x844 下无横向溢出、无 unexpected console output。
  - 2026-05-27 Mobile Android native bridge recheck：`$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; corepack.cmd pnpm tauri android build --debug` 通过；`adb install -r app-universal-debug.apk` 成功；`adb exec-out screencap -p` 截到 Threads/Runs/Settings 当前真实 emulator 画面。`adb shell ping -c 1 api.hub.vectorcontrol.tech` 0% packet loss，`adb shell dumpsys connectivity` 显示 WIFI `VALIDATED`；logcat 搜索 `CORS|CSP|Access-Control|ERR_|api.hub` 未见旧 CORS/CSP blocker。
  - 2026-05-27 Mobile emulator QA pass：`corepack.cmd pnpm visual:qa` 先通过 mocked dark/light 登录恢复截图；随后 `$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'; corepack.cmd pnpm tauri android build --debug` 通过，`adb install -r` 成功；新增并验证 `$env:ADB_SERIAL='127.0.0.1:16384'; corepack.cmd pnpm emulator:qa`，自动截到真实 WebView Threads/Chat/Runs/Settings bottom-tab matrix、Threads/Runs workflow recovery 与 Settings login recovery。后续发现 `127.0.0.1:16384` 外部 emulator 固定横向逻辑帧，已改为冷启动 Android Studio AVD `agenthub-emu` / `emulator-5556`（Pixel 7, `1080x2400`, `SurfaceOrientation=0`）并用 `$env:ADB_SERIAL='emulator-5556'; corepack.cmd pnpm emulator:qa` 复截 portrait 证据；脚本首屏等待从 1.8s 调整为默认 4.2s，可用 `AGENTHUB_EMULATOR_LAUNCH_DELAY_MS` 覆盖，避免冷启动时 Threads 截到空白 WebView。当前截图：`mobile-ui-threads-emulator-current.png`、`mobile-ui-chat-emulator-current.png`、`mobile-ui-runs-emulator-current.png`、`mobile-ui-settings-emulator-current.png`、`mobile-ui-settings-login-recovery-emulator.png`。

### Mobile Rust 骨架（app/mobile/src-tauri/src/）
| 模块 | 状态 | 说明 |
|------|:--:|------|
| `lib.rs` | ✅ | Tauri builder + `#[tauri::mobile_entry_point]`，plugin shell + notification |
| `notifications.rs` | ✅ | `notify_run_completed` / `notify_run_failed`（已完整实现） |
| `oidc.rs` | ⚠️ stub | `start_oidc_login` 返回 "not yet implemented"（需适配系统浏览器 + 深链 `agenthub://`） |
| `secure_store.rs` | ⚠️ stub | store/read/clear 均返回 "not yet implemented"（需平台安全存储，不能用 desktop keyring-rs） |

### Mobile 待办（下一轮）
1. **OIDC 深链**：实现 `agenthub://` custom URI scheme，PKCE + 系统浏览器 → TokenDance ID → 回调 app。
2. **Secure Store**：选型 Android Keystore / Tauri plugin，替换当前 stub。
3. **APK 体积**：切 release build 或单 ABI（x86_64 for emulator），预估可降到 ~50 MB。
4. **UI 打磨下一步**：当前已完成基础 Mobile-native shell、tab 空态、Settings 原生能力入口与登录态错误恢复、Threads command-center 概览、Runs queue、Chat mobile bubble/composer、Run detail/log surface、approval/diff preview、结构化 run blocks、artifact/preview links；下一步按 Desktop 主线做真实 Android emulator 复截和 Hub workflow/session contract 对齐，而不是新增独立 Mobile 产品概念。
5. **`tauri android dev` 闭环**：解决端口冲突，验证 emulator 热重载流程。
6. **前端通知权限**：Android 13+ 运行时通知权限请求（`POST_NOTIFICATIONS`）。
7. **Android Hub 访问**：native API bridge 已接入，下一步重建安装 APK，在 emulator 上确认 Threads/Runs 通过 `hub_request` 访问 live Hub，不再受 `http://tauri.localhost` CORS 影响。
8. **Visual QA 扩展**：`emulator:qa` 已覆盖 Android emulator 安装后的 Threads/Chat/Runs/Settings bottom-tab matrix、Threads/Runs workflow recovery 和 Settings 登录恢复复截；当前权威设备是冷启动 Android Studio AVD `agenthub-emu` / `emulator-5556` portrait，而不是固定横向逻辑帧的 `127.0.0.1:16384` 外部实例。后续继续扩到 approval/diff + structured run blocks 真实 WebView 复截。

### Worktree 清理（3 进 3 出）
- `feat/openapi-docs`：22 endpoints + 13 schemas → 提交 → 合并 → 删分支
- `feat/skillmd`：SKILL.md discovery + 3 adapter injection + 测试 → 提交 → 合并 → 删分支
- `feat/settings-split`：settingsShared.tsx 930 行提取 → 提交 → 合并 → 删分支
- 共 4 个新 commit 已 push 到 `dev/delicious233`

### AgentTeam 底层状态确认
- **AT-1 已完成**：模型（AgentTeam/AgentTeamMember/AgentTeamRun/AgentTeamAssignment）、迁移（0033/0034）、仓库（15 func）、服务（18 method）、处理器（15 endpoint）、路由（15 条 under `/web/agent-teams`）全部就绪，测试 7/7 通过。
- **AT-2 部分完成**：StartTeamRun 会创建 Hub session → agent instances → trigger supervisor → dispatch。缺失：① Supervisor 输出解析自动创建 TeamAssignment ② TeamEvent append-only log ③ TeamRunState replay projection。
- **AT-3 待实现**：CoordinatorRouteDecision 结构化委派 + guardrails（MAX_DEPTH=3, MAX_ACTIVE=5, cycle detection, timeout=30min, budget inheritance）。
- **模型已新增**：CoordinatorRouteDecision、AgentTeamEvent、TeamRunState、TeamBudget 类型写入 `model/agent_team.go`。
- **迁移已创建**：0036 `agent_team_events` 表（up + down SQL）。
- **仓库/服务/处理器**：尚未补全。

### 5 个待修后端 Issue
- `0374d91` 已修复 #145 #142 #138 #105。仅 #173 状态待确认。

## 下一步（按角色分工，2026-05-26 深夜）

### Desktop Agent（客户端）
- [ ] 修复 UI popover 裁剪 bug（top-right 图标 hover，`.root { overflow: hidden }` 截断 run card）
- [ ] Desktop Settings 接入 AgentTeam 管理界面（CRUD team/member，消费 `/web/agent-teams` API）
- [ ] Desktop TeamRun Console：task board、member status、subagent activity

### Mobile Agent（移动端）
- [ ] OIDC 深链（`agenthub://`）+ PKCE + 系统浏览器（前端按钮已接 Tauri command；Rust 仍是 stub）
- [ ] Platform secure store（Android Keystore）（前端读取/清除入口已接 Tauri command；Rust 仍是 stub）
- [ ] `tauri android dev` 端口冲突解决 + 热重载闭环
- [x] Mobile UI 基础修复：去除未生效 Tailwind-like class 假设，改为 Mobile-native semantic CSS，完成 emulator 截图验收
- [x] Mobile Settings 原生能力入口：TokenDance ID 登录、Hub session 检查/清除、notification probe 前端桥接
- [x] Mobile Settings action feedback：原生命令 pending 禁用按钮，状态反馈用 role=status 图标 panel，QA 覆盖 Check session 错误态
- [x] Mobile 设计系统推进：Threads/Runs/Chat/Run detail 对齐 dense command-center，完成 390x844 dark mocked workflow 截图
- [x] Mobile approval/diff/resources preview surface：waiting approval + diff files + structured blocks + artifacts/previews + approve/reject 操作 + resource detail sheet，完成 390x844 dark mocked 截图
- [x] Mobile diff readability：Diff preview 改为移动端逐行分色和 +/− chips，避免手机横向拖动审阅长 diff
- [x] Mobile structured blocks timeline：Run detail structured blocks 增加序号、metadata chips、balanced borders 和类型色 icon，提升手机端审阅扫读效率
- [x] Mobile logs readability：Run logs 从 raw pre 升级为移动端日志行，按 source/severity 分色并保留底部审阅 dock
- [x] Mobile run section active state：Review/Diff/Blocks/Outputs/Logs sticky nav 点击后保留 active chip 与 `aria-current`，长页面审阅位置更清楚
- [x] Mobile visual QA 脚本：`corepack.cmd pnpm visual:qa` 可复截 mocked workflow 并校验 overflow/touch target/console
- [x] Mobile Android WebView CSP 修复：`tauri.conf.json` 已允许当前 Hub HTTP/WS host，emulator logcat 从 CSP blocker 推进到 Hub CORS blocker
- [x] Mobile native API bridge：Tauri runtime 下 REST 走 Rust `hub_request`，allowlist `http://api.hub.vectorcontrol.tech/`，避免 WebView CORS 作为主链路 blocker
- [x] Mobile queue filters：Threads 与 Runs 增加移动端横向筛选 chips，`visual:qa` 覆盖筛选后截图
- [x] Mobile queue refresh feedback：Threads/Runs 慢刷新时显示 overview status pill 和 `aria-busy` refresh button，截图覆盖 refetch in-flight
- [x] Mobile review action dock：pending approval run detail 底部保留 Approve/Reject 操作，长内容滚动时不丢失审阅上下文
- [x] Mobile approval confirmation sheet：Approve/Reject 先弹 decision-specific bottom sheet 再提交，Reject 使用危险态确认摘要和红色 confirm，降低移动端误触审批风险
- [x] Mobile rejection success feedback：Reject 提交成功后使用 rejected 文案、危险态 decision lock、Run header error badge、Review summary danger tile 和 error status badge，`visual:qa` 截图覆盖 reject 成功闭环
- [x] Mobile light-mode screenshot coverage：`visual:qa` 增加 light color scheme 截图，覆盖 Threads、pending review dock 和 approval confirmation sheet
- [x] Mobile chat context panel：Chat 页补 project/status/messages/last activity/handoff 摘要，避免移动端对话脱离线程上下文
- [x] Mobile chat latest jump：长线程上滑后显示 92x44 Latest 浮动按钮，点击回到最新消息并用 `visual:qa` 截图覆盖
- [x] Mobile chat copy feedback：消息和 activity card 补 44px Copy/Copied 操作，手机端可复制 Agent 输出或用户指令，`visual:qa` 截图覆盖成功反馈
- [x] Mobile chat composer scope：composer 上方补 project/items/status 持久 scope，移动端输入时不丢回复归属
- [x] Mobile chat send feedback：发送 pending/error/success 状态固定在 composer dock/对话流，对话流显示 Sending/Not sent/Sent 气泡，失败保留 draft 并提供 44px Retry，用截图覆盖慢 POST、503、retry-success 与成功 POST
- [x] Mobile settings login recovery：native Sign in/session/notification action 失败后在状态面板提供 44px Retry 动作，dark/light 截图覆盖登录恢复
- [x] Mobile bottom nav badges/safe area：底栏 Threads/Runs tab 显示 active thread / pending review 数字 badge，并给 Android gesture handle 保留最小底部安全区；`visual:qa` 与 portrait emulator 截图覆盖
- [x] Mobile runs triage shortcut：Runs 顶部新增 Next review 快捷卡，减少 waiting approval 的移动端审阅路径
- [x] Mobile runs row triage：Runs row 增加整行状态 tint、balanced border、status badge 和 thread context，筛选后仍能快速判断 run 状态与归属
- [x] Mobile thread handoff shortcut：Threads 顶部新增 Continue handoff 快捷卡，减少 active thread 的移动端继续路径
- [x] Mobile run detail summary strip：Run detail 顶部补 Review/Diff/Blocks/Outputs/Logs 摘要，进入详情先判断完整审阅证据面
- [x] Mobile recovery states：Threads/Runs/Chat Hub/API 失败态改为上下文内恢复卡，`visual:qa` 覆盖 mocked 503 截图
- [x] Mobile Android native API bridge emulator 复验：重建安装 APK，真实 WebView 无 CSP/CORS blocker，恢复态截图已更新
- [x] Mobile Hub reachability split：Threads/Runs 在 `/health` 可达时显示 Reachable，把 workflow JSON 同步失败留在恢复卡而不是误判 Offline
- [x] Mobile Desktop-style glassmorphism：移除 Mobile CSS 显式渐变铺底，对齐 Desktop 玻璃面板 token，核心列表/详情/Settings surface 改为 rgba + blur/saturate + 细边框 + 状态色 border/icon/chip/full-row tint
- [x] Mobile Settings i18n 扩展：Settings readiness/account/notification/about、native action status/retry、clear-session confirmation sheet 已接 i18n，中文截图覆盖首屏无英文残留
- [ ] Mobile Hub workflow API/session contract：对齐部署态 Hub JSON 路由，避免当前 `/v1/*` 空 `text/plain 200` 让 Threads/Runs 保持 recovery state
- [ ] Mobile 真实 Android emulator 复截 approval/diff + 结构化 run blocks
- [ ] **禁止修改** `app/desktop/src-tauri/` 下任何文件

### 后端（我负责）
- [ ] AT-3：Supervisor 路由决策解析 + guardrails（model/migration 已有，待 service/handler/repo）
- [ ] AT-2 补全：TeamEvent 持久化 + TeamRunState replay（migration 0036 已有）
- [ ] #173 状态确认
- [ ] 生产部署验证（注意：不在服务器编译，磁盘敏感）

### 共享端口与资源
| 资源 | Desktop | Mobile |
|------|---------|--------|
| Tauri 项目 | `app/desktop/src-tauri/` | `app/mobile/src-tauri/` |
| Vite 端口 | **5173** | **5174** |
| Rust crate | `agenthub-desktop` | `agenthub-mobile` |
| 前端共享 | `app/shared/` (`@agenthub/shared`) | 同左 |

### 分支与质量
- 分支：`dev/delicious233`（唯一事实源）
- Hub: 13/13 ✓ | Edge: 16/16 ✓（含新增 skills 包）
- `feat/* → dev/delicious233 → master`
- 小步 commit + push，跨方向改 API 先开 draft PR

## 快速上手

```bash
git clone https://github.com/TokenDanceLab/AgentHub.git
cd AgentHub
git checkout dev/delicious233
```

### 运行后端测试
```bash
cd edge-server && go test ./... -short -count=1   # 13/13 包
cd ../hub-server && go test ./... -short -count=1  # 13/13 包
```

### 运行前端测试
```bash
cd app/desktop && pnpm typecheck && pnpm test:ci
cd app/web && corepack.cmd pnpm typecheck && corepack.cmd pnpm exec vite build
```

Web 单元回归可先跑 `cd app/web && corepack.cmd pnpm test -- src/utils/hubAdapters.test.ts`。当前 `vite build` 通过；根/wrapper `pnpm build` 如在 Windows Node/libuv lifecycle 上异常，按既有工程债单独处理，不等同于 Vite 构建失败。

### 前端 TypeScript 检查
```bash
cd app/desktop && pnpm typecheck    # 生产源码检查；测试文件 strict index/optional 债不作为本轮 release gate
```

### Storybook
```bash
cd app/desktop && pnpm storybook    # http://localhost:6006
```

## 三层架构

```
Desktop (React 19 + Tauri) → Edge Server (Go, :3210) → CLI Agents
                           → Hub Server (Go, :8080) → PostgreSQL + Redis
```

| 层 | 技术栈 | 测试 | 关键特性 |
|---|------|:--:|------|
| **Desktop** | React 19, TypeScript, Zustand, TanStack Query, OKLCH tokens, CSS Modules | CI-safe Vitest；全量 edge-real/lint 仍有既有债 | viewRegistry, @shared/ui, Storybook, RunState 状态机, IM UI, AuthPage, 虚拟滚动 |
| **Edge** | Go, gorilla/websocket, NDJSON | 13/13 包 | 3 Adapter (Claude/Codex/OpenCode), Prometheus, event bus dropped counter, Orchestrator, E2E 19/19 API |
| **Hub** | Go, Gin, GORM, Redis, PostgreSQL | 13/13 包 | DI 架构, CORS→BodyLimit→RateLimit 链, 28 migrations, 公开 API |

CI 说明：Go lint 已迁移到 golangci-lint v2 配置，gosec 已切到当前 `github.com/securego/gosec/v2` 模块路径；当前 Edge/Hub 仍有既有 lint/gosec 债。Actions 中 lint/gosec 作为可见债务步骤运行，build/test/race/govulncheck/coverage 仍是硬阻断。

## 生产部署

| 服务 | URL | 位置 |
|:--|:--|:--|
| 官网 | https://hub.vectorcontrol.tech | nginx → agenthub-home/out/ |
| Hub API | http://api.hub.vectorcontrol.tech | nginx:80 → Docker `127.0.0.1:8090` |
| Hub 代码 | `/opt/agenthub-hub/` (dev/delicious233) | Docker Compose |

详细部署记录：`.ops/deployment-record.md`（本地，不入仓库）

## 当前进度

### 已完成批次
- **P0-P3**：Edge/Desktop 基础功能
- **M3b-M4**：AgentHook, 消息树, 安全管道, Hub 骨架
- **M5**：工程基础收敛（62 commits）
- **M6**：生产部署（Docker, nginx, Cloudflare DNS）
- **M7**：Desktop P0 打磨（TanStack Query + Zod + 虚拟滚动 + 心跳）

### 关键里程碑
- ✅ Hub DI 5 阶段完成（全局单例删除）
- ✅ Desktop P0 全部完成
- ✅ Edge↔Hub 3层 E2E 打通
- ✅ Repository 测试 0%→75.5%
- ✅ 生产部署（独立 PG/Redis，与 AIhub 隔离）
- ✅ 官网 Hub API 集成
- ✅ Desktop 卡片式 UI 重构（左导航 + 中间主卡片 + 右侧面板）
- ✅ 无边框 Tauri 窗口 + One Dark Pro 暗色主题
- ✅ i18n 跟随系统语言（navigator.language）

### 本轮进展（2026-05-25）

**后端 B10 批次（2026-05-25 晚间，19 fix，3 Team 并行）**：
- Hub Core Service（5 commits）：#154 session last_message_at + #132 过期扫描含 running + #159 备注可清空 + #120 UpdateRemark 404 + #157 消息搜索过滤 + #122 私聊好友校验
- Agent + Edge Callbacks（3 commits）：#130 stream 去重 + #109 生命周期强制 + #99 离线 dispatched + #137 队列失败日志 + #179 NDJSON 失败终止 + #177 CLI LookPath + #108 cancel 响应对齐
- WS + Auth + Middleware（1 commit）：#178 WS 多设备路由 + #96 撤回原 session + #93 已读序列前进 + #88 typing 成员校验 + #78 DeleteForMe 缓存 + #82 WS 认证对齐
- 测试：hub-server 13/13 + edge-server 15/15 全绿，race detector 通过
- 19 issue 已关闭；5 个纯后端 issue 待修（#145 #142 #138 #173 #105）

- Desktop：项目文档后台 sweep 已完成，`docs/architecture/system-architecture.md` / `docs/architecture/product-requirements.md` / `docs/architecture/implementation-guide.md` / `docs/roadmap.md` / README 系列 / archive + ADR 索引已统一 Runtime/Profile/Configuration/Execution Target、TokenDance ID、Hub/Edge/Desktop/Web 边界。
- Desktop：设置页按 Codex App 截图方向重构为全屏设置工作台，并新增任务列表、IM 群聊、Agent 调度、在线 IM、Agent 市场、Skill/MCP、模型配置、模型映射、cc-switch、多端、远控、账号鉴权和安全审计等一等入口；顶部快捷图标可直达任务列表和 Agent 调度分区。
- Desktop：Settings 已新增 `Agent Profiles` 与 `Execution Targets` 两个一级页面，把用户概念明确拆成 Agent Profile（Runtime + Model + Configuration）和 Execution Target（Local Edge / Hub Relay / SSH/Tailscale / Cloud Edge）；页面直接消费 `useHealth()` 与 `useAgentList()`，`HealthResponse`/Zod schema 已保留 `/v1/health.checks.runners` 扩展字段，Connections 页同步显示 Edge runner summary。
- Desktop/Hub/Web：TokenDance ID 登录入口已作为账号体系主入口进入登录页和 Settings/账号页；Hub Server 已实现 `POST /client/auth/oidc/authorize` + `/callback` 的 code exchange、ID token JWKS 校验、`tokendance_sub` 映射和 Hub access/refresh session 签发。Web 已接入浏览器 PKCE redirect callback：授权时可传本轮 `redirect_uri`，Hub 将其绑定到 state 并用于 token exchange，Web 回跳 `/auth/tokendance/callback` 后用 sessionStorage 中的 verifier 换 Hub session。Desktop 代码路径已接系统浏览器、本机回调捕获与 Hub OIDC exchange；Tauri 打包路径使用系统凭据存储 Hub session，浏览器开发 fallback 已收敛到 tab-scoped sessionStorage 并清理 legacy localStorage Hub token key。发行版登录、logout/reconnect 与截图证据仍需闭环。
- Web/Desktop fallback：Web Hub access/refresh token 和 token-source hint 已从持久 `localStorage` 收敛到 tab-scoped `sessionStorage`，并会在 load/save 时清理旧 `agenthub_hub_token`、`agenthub_hub_refresh_token`、`agenthub_token_source`；Desktop 非 Tauri 浏览器开发 fallback 也不再把 Hub access token 写入 `localStorage`。这只是浏览器端风险降级，公开 Web 发布前仍需 BFF/HttpOnly cookie 或等价 server-owned session 方案。
- Web：主工作区发送链路已从 `edgeClient` stub 切到 Hub session/message/task。Threads 面板读取/创建 Hub sessions；Hub 允许创建 owner-only group session 作为 Web workspace 会话，Workspace 空态发送或选 Agent 时可按需创建该会话，再写入 Hub message、邀请 Agent、通过 `/web/agent-tasks` 触发 Hub→Desktop/Edge `agent.dispatch`；取消走 `/web/agent-tasks/{id}/cancel`。浏览器仍不持有 Relay key，也不直接调用 Edge。
- Desktop/Edge/Hub：2026-05-26 Runtime bridge 输出回传已补齐。Desktop App 根部挂载 `DesktopHubTaskBridge`，先恢复/获取 Hub-issued access token，再开启 Hub WS 并调用 `useHubIntegration` 监听 `agent.dispatch`/`agent.cancel`；bridge 将 Hub task 转成 Local Edge `POST /v1/runs`，把 stdout `run.output.batch` 与结构化 `run.agent.*` 作为 `event_type + payload` 回传 Hub stream，并用有界可见输出缓存生成 `done.final_content`。Edge direct callback 模式也会在 `hubTaskId` 存在时 stream raw stdout/结构化文本，并用真实输出而非状态字符串作为 done 内容；Hub callback chunk 切分保持 UTF-8 完整。
- Desktop/Web/Hub：2026-05-26 Hub dispatch 真实启动 payload 已补强。Hub `agent.dispatch` 会把触发消息文本作为 `prompt` 下发；`/web/agent-tasks` 支持按 `agent_type` / `agent_instance_id` / `custom_agent_id` 指定目标，不再在用户已选择 Runtime 时静默回退到第一个 agent；Web 会把 Hub Agent Profile 的 `runtime_id`、model/provider/reasoning hint 传给 Hub，自动邀请 Claude Profile 时使用 Edge adapter id `claude-code`；Desktop bridge 会把 legacy `claude` 规范化为 `claude-code`，并在启动 Local Edge run 前先创建/复用 Hub session 对应的 Edge thread，避免真实 Web→Hub→Desktop→Edge 链路空 prompt、未知 agentId 或 thread 404。Web Hub-only fallback 不再把 mock runner 计为 Local Edge ready。
- Hub/Web/Desktop/Edge：2026-05-26 Agent Profile runtime config bridge 已补齐仓内链路。Hub 合并 CustomAgent 默认 `model_params` 与触发时 model params，Web AgentProfile 会把 `permission_mode`、`tool_allowlist` 和 `target_preferences.work_dir` 投影到 task trigger，Desktop bridge 会把 Hub `model_params` / `system_prompt` / `tool_whitelist` 翻译为 Edge `/v1/runs` 的 `model`、`reasoningEffort`、`thinkingMode`、`permissionMode`、`workDir`、`includePartial`、`systemPrompt`、`appendSystemPrompt`、`allowedTools`、`configOverrides` 和 `ephemeral`，Edge API/ProcessExecutor 继续传入 adapter `RunProcessContext`。这证明 Profile 配置能进入 Codex/Claude Code/OpenCode 启动参数；仍不等同于远程 Edge workspace allowlist 或完整阻塞式 HITL。
- Web：2026-05-26 Web Hub/Local Edge 边界已加结构 gate。`app/web/src/hooks/useHubIntegration.ts`、`api/eventClient.ts`、`hooks/useEventStream.ts`、旧 `useChatMessages.ts`、`useRunners.ts` 等未接主入口的直连 Edge 遗留面已删除；`scripts/verify-web-hub-boundary.ps1` 会阻断 Web 源码出现 `/v1/runs`、`/v1/events`、`127.0.0.1:3210`、`createEventStream` 或 `edgeBaseUrl`。验证通过 `.\scripts\verify-web-hub-boundary.ps1`、`app/web && corepack.cmd pnpm typecheck`、`app/web && corepack.cmd pnpm build`。
- Desktop/Edge：2026-05-26 Runtime readiness gate 已补齐。`scripts/verify-runtime-readiness.ps1` 只做 secret-free 结构检查，覆盖 AgentAdapter、Claude Code/Codex/OpenCode adapter、Edge env/profile/API、Desktop Settings runtime inventory、Web Hub-only stub 边界和文档 caveat；它不执行真实 CLI、不读取 CLI auth，不替代三大 Runtime live smoke。
- Desktop/Edge：2026-05-26 三大 Runtime live smoke 已完成。`scripts/integration-smoke.ps1 -SkipBuild` 分别用 Codex、Claude Code、OpenCode 真实 CLI 在隔离端口启动 Edge，均收到结构化 `run.agent.*` 事件并 `run.finished`；本轮同时修复 `scripts/client-smoke.ps1` 的真实 runId cancel 顺序，mock Edge client smoke 19/19 通过。
- Desktop：2026-05-26 权限决策前端路径已收紧。`useChatMessages` 不再发送 Edge 当前会丢弃的 WebSocket `permission_decide` 控制帧；App 使用 pending permission request 自带 `runId` 调 `/v1/permissions/decide`，REST 成功后才标记本地决策。当前仍未声称完成真正阻塞式 human-in-the-loop；Claude Code stdin can_use_tool 回写和远程 Edge 决策证明仍是后续安全闭环。
- Web：2026-05-26 Hub 主聊天已能把 Desktop/Edge bridge 写入 Hub `message.new` 的 runtime payload JSON 渲染为结构化块。`app/web/src/utils/hubAdapters.ts` 会把 `callId/toolName/input` 转成 tool block、`path/action/diff` 转成 file change block、`success/error/usage` 转成 result block；普通文本和普通 JSON 仍按文本显示，避免把 Hub 消息误解析为工具事件。
- Web/Desktop：2026-05-26 Hub REST envelope 已在两端 client 层对齐。Hub handler 统一返回 `{code,data,message}`；`app/web/src/api/hubClient.ts` 与 `app/desktop/src/api/hubClient.ts` 现在会解包 `data`、兼容旧裸 JSON mock，并把 Hub error envelope 转成 `AppError`，避免 TokenDance ID login、`/client/auth/me`、sessions/messages/profile 等生产响应被当成裸对象读取。Web Agent 列表登录后读取 Hub `GET /web/agent-profiles` 并映射成 Agent Profile；无 Hub token 时才保留显式 preview fallback。
- Web：2026-05-26 Hub-only boundary 已收紧。浏览器端删除旧 Local Edge `eventClient`、`edgeAuth`、`useHubIntegration`、`useChatMessages`、Local Edge status/event/runners hooks，权限弹窗类型迁到 `app/web/src/types/permissions.ts`；新增 `scripts/verify-web-hub-boundary.ps1` 并接入 runtime readiness，阻断 `app/web/src` 重新出现 Local Edge loopback、`/v1/runs`、`/v1/events` 或 Desktop-only Hub-Edge bridge。
- Hub：2026-05-26 Hub session boundary 已收紧。新增 `RequireHubSession()`，并把 `/client/auth/me`、contacts、sessions/messages、attachments、notifications、`/web/*`、`/edge/*` 串到 Hub-issued session gate；TokenDance ID bearer 只作为 identity compatibility 被识别，不能直接授权 Hub 产品 API、设备路由或 Web task dispatch。
- Desktop/Web/Hub：2026-05-26 Hub WebSocket 鉴权已对齐真实 `/client/ws` 路由。Hub 路由在 HTTP upgrade 前通过 `WSAuthMiddleware` 只接受 Hub-issued HS256 access token；Desktop/Web `createHubWS` 现在把当前 Hub session token 放入 `access_token` query，并在重连时刷新 URL。`hub-server/internal/handler/ws_test.go` 覆盖 Hub-local query token 能注册 WebSocket session，TokenDance bearer 仍在 upgrade 前拒绝。
- Web/Hub：2026-05-26 Web fallback 登录设备边界已对齐。`app/web/src/api/hubAuth.ts` 的账号密码 fallback 改为请求 `device_type=web`，避免浏览器会话误标为 Desktop；OpenAPI 已把 `/client/auth/login` 的 `device_type` 和 UUID `device_id` 标为必填，Web Vitest 覆盖该 payload。
- Desktop/Web：2026-05-26 客户端 `agenthub_device_id` 持久值已增加自修复。`app/desktop/src/api/deviceId.ts` 与 `app/web/src/api/deviceId.ts` 会复用/规范化 UUID，发现旧版 `desktop_*` 或其他非 UUID 值时立即生成新 UUID 并覆盖，避免 TokenDance ID/Hub login 在 handler UUID 边界被 400 拦下。
- Web：`app/web/tsconfig.json` 已恢复 `strict: true`、`strictNullChecks: true`、`noUncheckedIndexedAccess: true` 与 `exactOptionalPropertyTypes: true`；已清理 Web/shared optional DTO、Hub/IM adapter、permission、composer、Settings 与 private chat 的 exact optional 形状，当前 `corepack pnpm typecheck` 与 `corepack pnpm build` 通过。
- Web：暗色 Web shell 已按 Codex App 纯色深灰目标收敛：遵循 `codex-theme-v1` 的 `surface=#25252d`、`ink=#e3e4e6`、`accent=#5d68cc`，移除 `app/web/src` 内全部 `linear/radial/conic-gradient` 与 gradient mask；composer 直接复用 Desktop 单层 capsule 的宽度、字号、边框、blur 和低阴影结构，移除 Web 额外 goal/card 堆叠，空态建议项改为低噪 inline chips。Playwright 桌面/移动 smoke 覆盖运行时 0 个 gradient 节点、无 raw i18n key、无横向溢出、无 console error；截图证据保留为本地 ignored 产物，不纳入 Git。
- Desktop：左栏概念从“智能体/能力 chips”改为 `Agent Runtime`，不再把“流式输出/工具调用/文件修改”等基础能力当产品主概念；Runtime 卡片展示本地 Edge + CLI adapter 元信息，基础 capability 仅保留在协议/后端层。
- Desktop：App shell 已支持左侧栏折叠、右侧运行详情彻底关闭、左右栏宽拖拽 resize。真实 run 验证中，右侧运行面板展开宽度 360px，关闭后完全不占空间，主工作区从 640px 扩展到 1012px；两条 resize separator 可见。
- Desktop：移动端工具栏已补 Settings、Hub 登录、主题切换与菜单入口；375px Playwright 验证无横向溢出。
- Desktop：`useChatMessages` reducer 内的 runStore/queryClient 副作用已移到事件处理路径，修复 React “setState while rendering” console error；合法 `RUNNING/STREAMING/WAITING_FOR_INPUT -> COMPLETED` 不再输出误报 warning。
- Desktop：真实接口验证 `http://127.0.0.1:3210/v1/health` ok，`POST /v1/runs` 返回 202 accepted；右栏关闭/重开、Settings 任务/群聊/调度入口、i18n raw key、移动端布局均通过 Playwright 检查，截图见 `app/desktop/screenshots/shell-right-panel-real-run-closed.png`、`app/desktop/screenshots/settings-tasks-im-scheduling.png`。
- Web：~~已派 gpt-5.5 xhigh worker `Hegel` 在独立 worktree `D:\Code\TokenDance\AgentHub\.worktrees\webui-desktop-port` / `feat/webui-desktop-port` 推进 Web UI 移植，硬约束 TokenDance ID、设备/Hub 同步、在线 IM/群聊、任务列表、Agent 调度、市场、Skill/MCP、模型映射、cc-switch、远控与审计入口。~~ (分支已于 2026-05-25 清理；Web UI 路线保留 `dev/trump` 分支)
- Web：gpt-5.5 xhigh 只读审查 `Ampere` 已完成，审查修复（移动端溢出、Toggle 语义/触控）已合入主线。分支及 worktree 已于 2026-05-25 删除，无需合并。
- Desktop/shared：`app/shared/pnpm-lock.yaml` 已同步 `@types/react`、`@types/react-dom`、`typescript` 等已声明 dev dependency；此前 `pnpm exec tsc --noEmit` / Desktop build 受 shared React 类型 lockfile 过期阻塞的问题已解除，`app/desktop && pnpm build` 已通过。
- Desktop：新增验证已通过 Settings `Agent Profiles` / `Execution Targets` Playwright 桌面和 375px 移动端检查：无 console error、无 raw i18n key、无横向溢出；截图见 `app/desktop/screenshots/settings-agent-profiles.png`、`app/desktop/screenshots/settings-execution-targets.png`、`app/desktop/screenshots/settings-execution-targets-mobile.png`。Web worktree 验证已通过 `corepack.cmd pnpm exec vitest run src/pages/ecosystem/EcosystemConsole.test.tsx`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`，移动端复测 `docScrollWidth=375`、switch `52x44`、无 console error，截图见 `.worktrees/webui-desktop-port/app/web/screenshots/ecosystem-console-mobile-fixed.png`。
- Desktop：run 启动反馈已继续推进。`AppError` 现在保留 HTTP status，并把 Edge 409 `active_run_exists` 顶层 `runId` 归入 `details.runId`；`PromptInput` 支持 async send result，只有 Edge 接受 run 后才清空草稿；启动中会禁用输入/重复提交；409 时打开现有 run、显示 toast，并保留未接受的草稿。
- Desktop：Toast 容器已挂回 App shell。此前 toast store 会写入但页面不可见，导致 `active_run_exists`、复制、连接状态等通知无法显示；本轮已修复。
- Desktop：新增验证已通过 `python -m json.tool src/i18n/locales/{en,zh}.json`、`pnpm vitest run src/__tests__/errors.test.ts src/__tests__/PromptInput.test.tsx src/__tests__/Toast.test.tsx`（42/42）；Playwright 模拟 Edge 409 覆盖草稿保留、toast 可见、无横向溢出，截图见 `app/desktop/screenshots/run-start-active-conflict.png`，当前页面截图见 `app/desktop/screenshots/run-start-feedback-desktop.png`。
- Desktop：Settings `Tasks` 已从预留 surface 接入真实数据面。任务页现在通过 `useRuns()` 展示 Local Edge run 总数/active 数、最近 run，通过 `useTaskBridgeStore` 展示 Hub dispatch bridge task 总数/active 数和桥接队列，并保留审批队列入口。
- Desktop：Tasks 验证已通过 `pnpm vitest run src/__tests__/SettingsPage.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/errors.test.ts src/__tests__/Toast.test.tsx`（43/43）、`python -m json.tool src/i18n/locales/{en,zh}.json`、`git diff --check`；Playwright 桌面和 375px 移动端检查无横向溢出、无 raw i18n key，截图见 `app/desktop/screenshots/settings-tasks-real-runs.png`、`app/desktop/screenshots/settings-tasks-real-runs-mobile.png`。
- Desktop：已修复 Playwright 暴露的重复 terminal run event 噪声。`RunStateMachine.transition()` 现在把同状态 transition 视为幂等 no-op，重复 `run.finished` / WebSocket replay 不再输出 `COMPLETED -> COMPLETED` warning；真实 Settings Tasks 桌面和 375px 移动端复测 `logs: []`，截图见 `app/desktop/screenshots/settings-tasks-runstate-idempotent.png`、`app/desktop/screenshots/settings-tasks-runstate-idempotent-mobile.png`。
- Desktop：Settings `Agent Scheduling` 已从预留 surface 接入真实调度概览。页面复用 `useRuns()`、`useTaskBridgeStore`、`useAgentList()`、`useHealth()` 和设置开关，展示调度队列、Agent Profile、Execution Target readiness、模型映射/cc-switch/远控/审批策略输入；Local Edge readiness 以 Edge 在线为准，runner inventory 缺失只影响指标文字。
- Desktop：Agent Scheduling 验证已通过 `pnpm vitest run src/__tests__/SettingsPage.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/errors.test.ts src/__tests__/Toast.test.tsx`（44/44）、`python -m json.tool src/i18n/locales/{en,zh}.json`、`git diff --check -- app/desktop/src/...`；Playwright 桌面和 375px 移动端检查 `logs: []`、无 raw i18n key、无横向溢出，截图见 `app/desktop/screenshots/settings-agent-scheduling-real-data.png`、`app/desktop/screenshots/settings-agent-scheduling-real-data-mobile.png`。
- Desktop：Settings `Agent Market` 已从预留 surface 接入本地真实 Profile 与发布准备视图。页面复用 `useAgentList()`、TokenDance ID 登录状态和 Agent capability 字段，展示本地 Agent Profile 数、可发布 Profile、能力覆盖、Hub 发布状态、已安装 Profile 卡片和发布审核清单。
- Desktop：Agent Market 验证已通过 `pnpm vitest run src/__tests__/SettingsPage.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/errors.test.ts src/__tests__/Toast.test.tsx`（45/45）、`python -m json.tool src/i18n/locales/{en,zh}.json`、`git diff --check -- app/desktop/src/...`；Playwright 桌面和 375px 移动端检查 `logs: []`、无 raw i18n key、无横向溢出，真实页面读到 OpenCode / Claude Code / Codex 三个本地 Profile，截图见 `app/desktop/screenshots/settings-agent-market-real-profiles.png`、`app/desktop/screenshots/settings-agent-market-real-profiles-mobile.png`。
- Desktop：Settings `Skill Management` 已从单行路径接入项目级 Skill registry 概览。页面基于当前 `.agents/skills/*/SKILL.md` 快照展示 7 个仓库级 Skill、6/7 可审核状态、1 个含脚本 Skill、1 个 references Skill、Hub sync 边界和脚本审计入口。
- Desktop：Skill Management 验证已通过 `pnpm vitest run src/__tests__/SettingsPage.test.tsx src/__tests__/PromptInput.test.tsx src/__tests__/errors.test.ts src/__tests__/Toast.test.tsx`（46/46）、`python -m json.tool src/i18n/locales/{en,zh}.json`、`git diff --check -- app/desktop/src/...`；Playwright 桌面和 375px 移动端检查 `logs: []`、无 raw i18n key、无横向溢出，截图见 `app/desktop/screenshots/settings-skill-registry-real-data.png`、`app/desktop/screenshots/settings-skill-registry-real-data-mobile.png`。
- Desktop/Edge 注意：当前 live Edge `http://127.0.0.1:3210` health 和 agents 在线，返回 Claude Code / Codex / OpenCode；此前真实连续双 POST 到 `thread_local` 观测到两个 202。2026-05-25 已用临时 Edge `127.0.0.1:3227` + 可控慢 `powershell Start-Sleep` runner 复现真实 HTTP 路径 first 202、second 409 `active_run_exists`，且 409 body 带回首个 active `runId`；先前 3210 现象更可能是旧进程或真实 runtime 过快完成。
- Docs：gpt-5.5 xhigh 文档架构 worker 已完成文档架构审查，结论已汇总到 roadmap/STATE；`docs/inbox/` 仍保留为临时投递入口，已处理报告归档到 `docs/reference/` 或 `docs/archive/`。主文档已基本对齐 Runtime/Profile/Configuration/Execution Target、TokenDance ID、IM、多端、远控、Skill/MCP、cc-switch、安全审计等边界；剩余风险集中在 `/v1/runners` / `runner.*` 的历史兼容命名，以及 `docs/archive/client-handoff.md`、`docs/roadmaps/integration.md` 等旧独立 `runner/` 文档需要归档或改写。
- Web：~~gpt-5.5 xhigh Web worker 已在 `.worktrees/webui-desktop-port/app/web` 内补强生态控制台。~~ (worktree 已随分支清理；`app/web/README.md` 已说明 `/` 生态控制台、`/workbench-preview` 旧工作台、TokenDance 生态边界和验证命令；`EcosystemConsole` 新增身份边界、协作同步、Agent runtime、运维护栏等入口，并补响应式 lane 布局和测试。验证通过 `corepack.cmd pnpm exec vitest run src/pages/ecosystem/EcosystemConsole.test.tsx`（4/4）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`git diff --check -- app/web`。)
- Web：~~gpt-5.5 xhigh worker `McClintock` 已继续在 `.worktrees/webui-desktop-port/app/web` 内新增 `Feature readiness` 面板~~ (worktree 已随分支清理)。验证通过 `corepack.cmd pnpm exec vitest run src/pages/ecosystem/EcosystemConsole.test.tsx`（5/5）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`git diff --check -- app/web`。
- Web：~~gpt-5.5 xhigh worker `Herschel` 已继续在 `.worktrees/webui-desktop-port/app/web` 内新增移动端/平板 `Jump to surface` picker~~ (worktree 已随分支清理)。验证通过 `corepack.cmd pnpm exec vitest run src/pages/ecosystem/EcosystemConsole.test.tsx`（6/6）、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`git diff --check -- app/web`。
- Hub：`CancelTask` 已通过 `AgentInstance` 解析真实 `SessionID` 后发布 `agent.cancel`，避免把 `AgentInstanceID` 误作为 `session_id`；回归测试 `TestCancelTaskPublishesResolvedSessionID` 已覆盖。
- Hub：auth middleware 测试已适配 `AuthMiddleware(*config.Config)` 签名，当前 HEAD `19fcaa1` 已包含该修复。
- Hub：Agent 任务回调链新增服务层回归测试，覆盖 `HandleTaskStream` 生成 `client_msg_id`、走 Redis seq、发布 `message.new`，以及 `HandleTaskDone` 在 Redis 失败时走 DB fallback、写最终消息并发布 `agent.done`。
- Hub：WebSocket 慢客户端背压路径新增 `TestManagerPushToConnCountsDroppedFrames`，验证 send buffer 满时 `ws_dropped_frames_total` 递增；`writeLoop` 退出路径统一 defer close，覆盖正常结束、写失败和 panic recovery。
- Hub：`UpsertDevice` 已改为按 `device_id` 冲突更新，`devices(user_id, device_type)` 降为非唯一索引；同用户同设备类型可拥有多个物理设备，跨用户或跨类型复用同一 `device_id` 会被拒绝为客户端错误。`TestDeviceRepo_Upsert` 已覆盖同物理设备更新、同用户同类型新增第二设备、跨用户抢占拒绝。
- Hub：联系人列表和收到的好友请求已补批量查询回归测试，覆盖多条记录只走一次 `WHERE id IN` 用户查询；好友请求 sender 缺失时记录 debug 并跳过坏数据，不阻断其他请求。
- Hub：`CustomAgent` 的 jsonb 字段从“只校验 JSON 语法”收紧为结构校验：`capability_tags`/`tool_whitelist` 必须是 JSON array，`model_params` 必须是 JSON object；handler 创建/更新前预检，model hook 保存前兜底。
- Hub：P3-2 魔数常量化已完成，request/body/timeout/rate-limit/message recall/pin limit/WebSocket heartbeat/EventBus pool/metrics interval/group name length 等默认值统一收敛到 `internal/config/constants.go`；WebSocket send buffer 保持现有运行值 256。
- Hub：Hub dispatch bridge 已持久化 `taskId` -> Edge `runId` / `edge_device_id` 映射；`pending_agent_tasks.edge_run_id` 与 `edge_device_id` 分别绑定 Edge run 和具体 Desktop device，`/edge/agent-tasks/{id}/ack|stream|done|fail` 支持 `run_id`/`edge_run_id`，Desktop `useHubIntegration` 在 ack、stream、done、fail 回调中持续回传 Edge run id。
- Hub：`AH-SR-020` Edge callback device/run proof 已 repo 内缓解。在线 dispatch 和离线 pending replay 都会在推送到具体 Desktop WS conn 时记录 `edge_device_id`；route 存在但 manager/conn 不可用时回落 pending queue，不误标 dispatched；service、handler、真实 HTTP 集成和 Desktop Vitest 已覆盖错误 user/device/run id 拒绝与 run_id 转发。
- Hub：`/client/auth/login` 和 `/edge/devices/register` 已在 handler 层校验 `device_id` 为 UUID，非法值返回 `BAD_REQUEST` 且不会进入 service/repository；`AH-SR-019` 已标记为 repo 内缓解并通过临时 Postgres/Redis 的真实登录/设备注册集成验证，剩余是部署与客户端覆盖验证。
- Hub：多设备登录已对齐真实 Postgres schema。迁移 `0021_devices_allow_multiple_same_type` 将 `(user_id, device_type)` 唯一约束改为普通索引，`/client/auth/login` 支持同用户两个 desktop UUID 分别登录并刷新 token；另一个用户复用已归属 `device_id` 返回 `BAD_REQUEST`，不再冒泡为 `INTERNAL_ERROR`。
- Hub：`AH-SR-022` message pin 跨 session 泄露已 repo 内缓解。`PinMessage` 创建 pin 前要求目标 message 属于当前 session，`ListPinnedMessages` 只在当前 session 范围 hydration pinned message；service 单测和临时 Postgres/Redis 集成测试已覆盖跨 session pin 拒绝与历史坏 pin 行过滤，剩余是历史数据清理或 DB 复合约束设计。
- Hub：`AH-SR-021` attachment 共享已 repo 内缓解。新增 `message_attachments` 引用表，file message 发送时抽取并校验 UUID attachment 引用，发送者必须是 uploader 或已有会话引用授权；下载允许 uploader 或引用所在 session 的 active user member，局外人保持 `ATTACH_NOT_FOUND`。真实 Postgres/Redis 集成已覆盖 Alice 上传并发送 file message 后 Bob 下载成功、局外人下载失败。
- Hub：`AH-SR-010` Redis/cache nil 行为已 repo 内缓解。Auth/Contact/Session/Message/Agent 构造器和方法统一经 `resolve*Cache` 处理 nil 与 typed-nil cache；测试/离线路径使用 no-op/fallback cache 避免 panic，Message/Agent seq 仍走 DB fallback；生产 `App.Run` 仍保留 Redis ping fail-fast。
- Hub：`AH-SR-008` dev compose 暴露面已 repo 内缓解。`docker-compose.yml` 默认通过 `AGENTHUB_BIND_HOST=127.0.0.1` 只把 PostgreSQL、Redis、Hub API、Hub admin/metrics 绑定到本机回环；远程开发需要显式设置 `AGENTHUB_BIND_HOST=0.0.0.0`，生产 compose 保持内部网络/loopback 发布。
- Hub：`AH-SR-011` public stats 精确计数暴露已 repo 内缓解。`/api/public/stats` 保留官网所需的公开无认证入口和原字段名/数字类型，但 user/agent/message/online 计数改为下限桶，uptime 改为 `<1h`/小时/天/`30d+` 粗粒度桶。
- API：`api/openapi.yaml` 已补 `/edge/agent-tasks/{id}/ack` request body、`HubTaskAckRequest`、`/edge/devices/register` 请求体，并把 `/client/auth/login` 的 `device_id` 标为必填 UUID；YAML 解析和重复 key 检查已通过。
- Edge：event bus 慢订阅者丢弃 fanout 时累计 `DroppedCount()`，Prometheus 新增 `edge_event_bus_dropped_total`，`httpserver` 已接入真实 bus 统计。
- Edge：修复 lifecycle 测试 helper 固定 `run_test` 导致的 Windows 临时输出日志抢锁，改为 per-test 唯一 run/project/thread ID。
- Edge：`CreateProject` 已通过 `ErrProjectExists` 区分新建/已存在；API 新建返回 201 并发布 `project.created`，重复创建返回 200、保留原项目名称且不重复发布 created 事件。
- Edge：`POST /v1/runs` 已实现每 thread 一个公开 active run，命中 `queued`/`started`/`cancelling` 时返回 409 `active_run_exists` 和现有 `runId`；Store 继续允许同 thread 多 run，保留 orchestrator sub-agent 内部创建能力；executor 启动失败会把 queued run 标记为 `failed`，避免重试被永久 409 卡住。
- Edge：Run 清理已接入 `RunCleaner`/`CleanupRuns`，只清理 terminal run（`finished`/`failed`/`cancelled`），支持 24h terminal TTL 和每线程 50 条 terminal run 上限，连带删除关联 run item；FileStore 清理后持久化快照；`POST /v1/runs` 在 active-run 检查前做保守清理，不影响 `queued`/`started`/`cancelling` active run。
- Edge：Orchestrator prompt 模板转义已确认落地，`NewOrchestratorAdapter` 和 `formatAgentList` 统一通过 `escapePromptLiteral` 处理 backtick 与 `${}`；Edge P2 常量提取已收口，adapter scanner buffer 统一到 `configureAdapterScanner`，event bus 测试改用 `subscriberChannelBufferSize`。
- Edge：`/v1/health` 的 runner 检查已暴露 `total`、`available`、`unavailable`、`statuses`、`items`，无 registry、无 runner 或全离线时整体降级为 `degraded`，方便客户端和运维区分“没有 runner”和“runner 离线”。
- Edge：真实 Codex-profile smoke 已通过。临时启动 `agenthub-edge --addr 127.0.0.1:3221 --runner-profile codex` 后，run `run_22b2112afb09060a` 从 `queued`/`started` 到 `finished`，WebSocket replay 捕获 `run.agent.text_block: OK`、`run.agent.result`、`run.finished`，证明 Codex CLI -> Edge adapter -> event bus -> WS 链路可用；临时 3221 服务已关闭。
- Edge：修复真实 runtime executor 下 `/v1/runners` 误报默认 Mock Runner 的问题；adapter executor 会把 `runner_local_1` 覆盖为 `<Runtime> Runner (local)`。带补丁的临时 Codex Edge 验证 `/v1/runners` 和 `/v1/health.checks.runners` 均显示 `Codex Runner (local)`，capabilities 为 `codex/tool_calls/file_changes/multi_turn`。
- Edge：`AH-SR-005` 任意权限决策伪造已做 server 侧缓解。`/v1/permissions/decide` 现在必须消费 pending `runId/requestId`，未知请求、错 run、重复 decision 都会拒绝；EventBus observer 在 WebSocket fanout 前登记 `permission_requested`，adapter 权限事件会补齐 run/project/thread scope，OpenAPI 已把 `runId` 标为必填。仍未声称完成真正阻塞式 human-in-the-loop 审批，远程 Edge 模式还需要签名/认证决策证明。
- Edge：`AH-SR-018` raw run output flood/disk 风险已做 repo 内缓解。`ProcessExecutor` raw stdout/stderr 共享 4 MiB per-run 默认预算，超限时同时截断 temp-file 持久化和 `run.output.batch` 文本，并发布 `truncated/maxBytes/bytesWritten/message` 兼容 metadata。
- Edge：`AH-SR-018` structured adapter payload flood 风险已做 repo 内缓解。`run.agent.*` map payload 在进入 EventBus 前按默认 1 MiB 单事件 JSON payload 预算递归截断字符串字段，发布 `truncated/maxBytes/bytesBefore/message` 兼容 metadata；无法靠字符串收敛时降级为 `dropped: true` metadata-only payload，run lifecycle 不受影响。
- Edge：`AH-SR-015` REST timeout 与 WebSocket 长连接已拆开。`WriteTimeout=0` 保持 `/v1/events` WebSocket 兼容，非 WebSocket REST 请求经 30s timeout middleware 兜底，WebSocket upgrade 请求绕过该 middleware。
- Edge：`AH-SR-014` 本地调用边界已做可选 token 缓解。`--local-auth-token` / `AGENTHUB_EDGE_AUTH_TOKEN` 非空时，除 `/v1/health` 和 CORS preflight 外的 Edge API 都需要本地 token；REST 支持 `Authorization: Bearer <token>` 和 `X-AgentHub-Edge-Token`，浏览器 WebSocket 使用 `/v1/events?access_token=<token>`。默认空 token 保持本地开发兼容，Remote/Cloud/Hub relay Edge 仍需 Hub session/device proof 和审计设计。
- Client/Edge smoke：`scripts/client-smoke.ps1` 已对齐当前 Edge 架构，不再构建已删除的独立 `runner/` 目录；新增 `-EdgeAddr` 可跑隔离端口，默认用 Edge 内置 `--runner-profile agenthub-runner-mock`，并在独占 Edge 时断言当前 run 收到 `run.started`、`run.output.batch`、`run.finished` 和 mock runner 输出。
- 验证：`hub-server && go test ./internal/model ./internal/handler -run "TestCustomAgent" -count=1 -v`、`hub-server && go test ./internal/service -run "TestListContacts_BatchesFriendUserLookup|TestListFriendRequests_BatchesSenderLookupAndSkipsMissingSender" -count=1 -v`、`edge-server && go test ./internal/store -run TestStoreCreateProjectDistinguishesExistingProject -count=1 -v`、`edge-server && go test ./internal/api -run TestMuxPostProjectsExistingProjectReturnsOKWithoutCreatedEvent -count=1 -v`、`edge-server && go test ./internal/api ./internal/store ./internal/lifecycle -count=1 -v`、`edge-server && go test ./internal/api -run "TestGetHealth|TestPostRuns" -count=1 -v`、`edge-server && go test ./internal/store -run "TestStoreCleanup|TestFileStoreCleanup" -count=1 -v`、`edge-server && go test ./internal/api -run "TestPostRuns" -count=1 -v`、`edge-server && go test ./internal/store ./internal/api -count=1 -v`、`edge-server && go test ./internal/adapters -run "TestNewOrchestratorAdapter|TestDefaultOrchestratorPrompt|TestFormatAgentList|TestEscapePromptLiteral|TestOrchestratorAdapterEscapesSystemPrompt" -count=1 -v`、`edge-server && go test ./internal/adapters ./internal/events -count=1 -v` 均通过。
- 本轮新增验证：`hub-server && go test ./internal/handler ./internal/service ./internal/repository -run "TestEdgeAgentTaskAck|TestEdgeHubProtocol|TestPendingTask|TestHandleTaskAck|TestEdgeTaskLifecycle" -count=1`、`app/desktop && pnpm vitest run src/__tests__/useHubIntegration.test.ts`、`python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('api/openapi.yaml').read_text(encoding='utf-8'))"`、重复 key 检查、`edge-server && go test ./internal/httpserver ./internal/runners ./internal/api -count=1` 均通过。
- 权限修复新增验证：`edge-server && go test ./internal/api ./internal/events ./internal/adapters ./internal/lifecycle -run "TestPermission|TestPostPermission|TestMuxPermission|TestAddObserver|TestScopedEventEmitter|TestBusEventEmitter|TestBudgetAwareEmitter" -count=1 -v`、`edge-server && go test ./internal/api ./internal/events ./internal/adapters ./internal/lifecycle ./internal/httpserver -count=1`、`edge-server && go test ./... -short -count=1`、OpenAPI YAML 解析与重复 key 检查均通过；`git diff --check` 针对本次 Edge/API/doc 文件通过。
- Edge raw/structured output cap 新增验证：`edge-server && go test ./internal/adapters -run "TestPayloadLimitEmitter|TestBudgetAwareEmitter|TestScopedEventEmitter|TestBusEventEmitter" -count=1 -v`、`edge-server && go test ./internal/lifecycle -run "TestProcessExecutorTruncatesStructuredAdapterPayload|TestProcessExecutorTruncatesRawOutputAtRunBudget|TestRunOutputLimiter" -count=1 -v`、`edge-server && go test ./... -short -count=1` 均通过。
- Runtime bridge 输出回传新增验证：`edge-server && go test ./internal/lifecycle -run "TestProcessExecutor|TestSplitHubCallbackTextPreservesUTF8" -count=1`、`edge-server && go test ./tests -run "TestHubE2E_(RunCompletes|CompleteRoundTrip|CallbackFormat)" -count=1`、`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/useHubIntegration.test.ts`、`app && corepack.cmd pnpm --filter agenthub-desktop typecheck` 均通过。注意：`corepack.cmd pnpm --filter agenthub-desktop test -- src/__tests__/useHubIntegration.test.ts` 会误触发 Desktop 全量测试；当前 `edge-real.test.ts` 仍有既有 409/WS Origin 失败，不作为本轮单文件 hook 验证结果。
- Edge active-run 真实 HTTP smoke：临时构建 `edge-server/.tmp/agenthub-edge-smoke.exe` 并启动 `--addr 127.0.0.1:3227 --runner-command powershell.exe --runner-arg -NoProfile --runner-arg -Command --runner-arg "Start-Sleep -Seconds 8; Write-Output done"`；`GET /v1/health` 返回 `status=ok`/`executor=ok`，连续同 thread `POST /v1/runs` 返回 first `202`、second `409 active_run_exists`，second body 的 `runId` 等于 first `runId=run_8ec7c59058063719`；临时服务和二进制已关闭/清理。
- Hub UUID 边界新增验证：`hub-server && go test ./internal/handler -run "Test(AuthHandler_Login|DeviceHandler_Register|EdgeHubProtocol)" -count=1 -v`、`hub-server && go test ./... -short -count=1`、`edge-server && go test ./... -short -count=1`、OpenAPI YAML 解析与重复 key 检查、本轮 server/API/doc `git diff --check` 均通过；随后用临时 `docker compose` Postgres/Redis（`15432/16380`）跑通 `hub-server && go test ./tests -run "TestEdgeDevice(Register)?$" -count=1 -v`，覆盖真实 register/login/me/desktop login/`/edge/devices/register` 链路，临时容器和卷已 `docker compose down -v` 清理。
- Hub cache fallback 新增验证：`hub-server && go test ./internal/service -run "Test(ResolveCacheUsesNoopForTypedNilClient|SendMessage_NilCacheUsesDBSeqFallback|ChangePassword_NilCacheDoesNotPanic|UpdateProfile_NilCacheDoesNotPanic|AcceptFriendRequest_NilCacheDoesNotPanic|ListContacts_NilCacheMarksOffline|CreatePrivateSession_NilCacheDoesNotPanic|HandleTaskDoneNilCacheUsesDBSeqFallback)$" -count=1 -v`、`hub-server && go test ./internal/service -count=1`、`hub-server && go test ./... -short -count=1` 均通过。
- Dev compose loopback 新增验证：`docker compose config --services`、`docker compose config` 解析通过；`git diff --check` 针对 `docker-compose.yml`、`.env.example`、安全登记、roadmap、STATE 通过。
- Public stats/REST timeout 新增验证：先用 TDD 红灯确认 `TestPublicStatsBucketsCountsAndUptime` 失败在精确 `37` 未桶化、`TestRESTTimeoutMiddleware` 失败在缺少 wrapper；实现后 `hub-server && go test ./internal/handler -run TestPublicStatsBucketsCountsAndUptime -count=1`、`edge-server && go test ./internal/httpserver -run "TestRESTTimeoutMiddleware" -count=1`、`hub-server && go test ./... -short -count=1`、`edge-server && go test ./... -short -count=1` 均通过。
- Client/Edge smoke 新增验证：`app/shared && pnpm install --no-frozen-lockfile` 同步 lockfile 后，`app/desktop && pnpm build` 通过；随后 `.\scripts\client-smoke.ps1 -EdgeAddr 127.0.0.1:3228` 通过 23/23，覆盖 Edge build、shared 依赖安装、Desktop web build、Edge 启动、`/v1/health`、`/v1/runners`、`POST /v1/runs`、cancel、WebSocket 当前 run 事件和 Edge Go tests。
- Edge local auth 新增验证：`edge-server && go test ./internal/httpserver ./cmd/agenthub-edge -count=1`、`edge-server && go test ./... -short -count=1`、`hub-server && go test ./... -short -count=1`、`app/desktop && pnpm vitest run src/__tests__/edgeClient.test.ts src/__tests__/eventClient.test.ts`、`app/desktop && pnpm exec tsc --noEmit`、`.\scripts\client-smoke.ps1 -EdgeAddr 127.0.0.1:3228 -EdgeAuthToken local-smoke-token`（23/23）均通过。
- 全量短测：`hub-server && go test ./internal/config ./internal/router ./internal/middleware ./internal/service ./internal/ws -count=1`、`hub-server && go test ./... -short -count=1`、`edge-server && go test ./... -short -count=1` 均通过；本轮新增多设备登录验证：`go test ./internal/repository -run TestDeviceRepo_Upsert -count=1 -v`、`go test ./internal/service -run TestDeviceRegisterMapsOwnershipMismatchToBadRequest -count=1 -v`、真实 PG/Redis 下 `go test ./tests -run "TestLoginAllowsMultipleDesktopDevicesForSameUser|TestLoginRejectsDeviceIDOwnedByAnotherUser" -count=1 -v`；附件共享新增验证：`go test ./internal/service -run "Test(GetAttachmentByIDAllowsSessionMemberForReferencedAttachment|SendMessage_FileContent)" -count=1 -v`、`go test ./internal/repository -run "TestMessageAttachmentRepo_CreateAndAccess|TestAttachmentRepo_CreateAndGet|TestMessageRepo_(Pins|InsertAndGet)" -count=1 -v`、真实 PG/Redis 下 `go test ./tests -run TestAttachmentDownloadAllowsSessionMemberAfterFileMessage -count=1 -v`；`git diff --check` 针对本轮 server/doc 文件通过。
- 工作区：Hub Agent 回调测试、Hub WS 背压测试、Hub writeLoop close、Hub contact/custom agent 校验、Hub P3-2 常量化、Hub taskId/runId 持久化、Hub `device_id` UUID 边界、多设备登录 schema 与真实 PG/Redis 验证、Hub attachment 共享 schema/授权与真实 PG/Redis 验证、Hub cache fallback、Hub public stats bucket、dev compose loopback、Edge dropped counter、Edge lifecycle 测试隔离、Edge project duplicate 测试、Edge run 并发 API 约束、Edge run cleanup、Edge orchestrator prompt 转义、Edge 常量提取、Edge health runner 状态、Edge runtime runner 状态修复、Edge permission registry、Edge raw/structured output cap、Edge REST timeout、Edge local auth、client-smoke 当前 Edge runtime 修复、shared lockfile 同步与 `api/events.md`/`api/openapi.yaml`/`docs/roadmap.md`/本状态页/安全风险登记为已合入主线或本轮收敛的推进记录；继续工作前按当前 git 状态重新核验。

### 当前接手顺序（2026-05-26）
- Web/Mobile：2026-05-29 持续收敛到 Desktop glass baseline。新增 `app/shared/src/designTokens.ts` 作为 Web/Mobile 对齐 Desktop command-center glass 的共享 token/rule manifest，并补 Vitest 覆盖 alias、no-gradient、no-left-rail、dense-copy 与 mobile touch-target 规则；Web/Mobile CSS 文档注释和 README 改为指向该共享 manifest。Mobile Settings readiness 从稀疏 Stub/Bridge/Probe metric 改为 TokenDance ID deep link、native Hub session secure store、notification local probe 三行密集状态，继续保持无渐变卡片、无左彩条。新增截图/probe：`app/mobile/screenshots/mobile-settings-shared-glass-readiness-pw-390x844.png`、`app/mobile/screenshots/mobile-settings-shared-glass-readiness-pw-390x844.probe.json`、`app/web/screenshots/web-settings-shared-glass-shell-pw-390x844.png`、`app/web/screenshots/web-settings-shared-glass-shell-pw-390x844.probe.json`；两端 probe 均为 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`，Mobile readiness rows=3。
- Web/Mobile：2026-05-29 继续推进统一结构。`app/shared/src/surfaceMetadata.ts` 的 shared IA registry 已扩展到 Mobile，新增 `mobile.threads`、`mobile.chat`、`mobile.runs`、`mobile.account`，并从 `app/shared/src/index.ts` 导出；Mobile Account 新增 Client surfaces 区域，直接消费 `getSurfacesByPlatform("mobile")` 渲染 4 行 Threads/Chat/Runs/Account，而不是单独手写本地列表。截图/probe：`app/mobile/screenshots/mobile-settings-surface-registry-pw-390x844.png`、`app/mobile/screenshots/mobile-settings-surface-registry-pw-390x844.probe.json`；probe 为 `scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`、`registryRows=4`。
- Web/Mobile：2026-05-29 Web route context 继续收敛到 shared IA registry。`app/web/src/layouts/WebLayout.tsx` 的 `/agent-square`、`/group/:id`、`/project/:id` route context panel 已改为消费 `@agenthub/shared` `getSurfaceByWebRoute()` / `getSurfaceStatusMetadata()`，标题、说明和状态来自 `surfaceMetadata`，本地 Web-only route title/summary/source 不再是事实源；`app/shared/src/surfaceMetadata.test.ts` 补 `/agent-square` route 匹配覆盖。截图/probe：`app/web/screenshots/web-route-shared-registry-agent-square-pw-390x844.png`、`app/web/screenshots/web-route-shared-registry-group-pw-390x844.png`、`app/web/screenshots/web-route-shared-registry-project-pw-390x844.png`、`app/web/screenshots/web-route-shared-registry-pw-390x844.probe.json`；probe 覆盖三条 route，均为 `innerWidth=390`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`，并确认 Agent Square/Catalog fallback、Group Workspace/Demo fallback、Project Preview/Demo fallback 文案来自共享 registry。
- Web/Mobile：2026-05-29 Web Settings 继续收敛到 shared Desktop section registry。`app/web/src/components/SettingsPage.tsx` 的 Settings nav/header 已接 `@agenthub/shared` `getSurfaceByDesktopSectionId()` / `getSurfaceStatusMetadata()`；Execution Targets、MCP 等 Desktop-owned section 的 label/description/status 来自 `surfaceMetadata`，Keyboard Shortcuts 等 Web-only section 明确显示 `Web local section` / `Local shell`，不再和 Desktop-owned registry 混为一个事实源。补齐 Web zh/en `settings.*.description` 和 `settings.sharedDesktopSection` / `settings.webLocal.*` 文案。截图/probe：`app/web/screenshots/web-settings-shared-desktop-section-pw-390x844.png`、`app/web/screenshots/web-settings-shared-desktop-section-keyboard-pw-390x844.png`、`app/web/screenshots/web-settings-shared-desktop-section-pw-390x844.probe.json`；probe 为 `/settings` `innerWidth=390`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`，并确认 Execution Targets=`Shared Desktop section`/`Real snapshot`、MCP Servers=`Shared Desktop section`/`Interface gap`、Keyboard Shortcuts=`Web local section`/`Local shell`。
- Web/Mobile：2026-05-29 Mobile Settings surface registry 状态继续收敛到 shared status metadata。`app/mobile/src/views/SettingsView.tsx` 不再读本地 `settings.surfaceStatus.*`，改为消费 `@agenthub/shared` `getSurfaceStatusMetadata()` 并渲染 `surface.status.*.label`；`app/mobile/src/i18n.ts` 删除本地 `settings.surfaceStatus` 分支，补 shared status label/description。截图/probe：`app/mobile/screenshots/mobile-settings-shared-surface-status-pw-390x844.png`、`app/mobile/screenshots/mobile-settings-shared-surface-status-pw-390x844.probe.json`；probe 为 `innerWidth=390`、`scrollWidth=390`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`，并确认 `Real snapshot` / `Local source` 来自 shared status，旧本地 `Live` 标签不再出现。
- Web/Mobile：2026-05-29 Mobile 底栏按原生移动端 IA 调整为右下 Account。`app/mobile/src/components/BottomNav.tsx` 不再用本地 `nav.*` label，而是用 shared `getSurfaceMetadata()` 渲染 Threads/Chat/Runs/Account；Account 仍复用现有 `SettingsView.tsx` route 组件但用户可见标题、底栏 label 和 shared surface id 都是 Account，且登录/会话检查/清除会话动作移到首屏。截图/probe：`app/mobile/screenshots/mobile-bottom-nav-shared-surfaces-pw-390x844.png`、`app/mobile/screenshots/mobile-bottom-nav-shared-surfaces-pw-390x844.probe.json`；probe 为 `innerWidth=390`、`scrollWidth=390`、`navLabels=[Threads,Chat,Runs,Account]`、Account aria 来自 `surface.mobile.account.description`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`。
- Web/Mobile：2026-05-29 Web 小屏底栏同步 Account IA。`app/web/src/layouts/WebLayout.tsx` 的 390px bottom nav 从 `Workspace/Messages/Settings/Run` 调整为 `Workspace/Messages/Run/Account`：Run detail 保留为第三个执行状态入口，Account 作为右下角身份/登录入口并打开现有 Auth modal；移动端 topbar 的重复账号按钮隐藏。Account label/aria 复用 shared `getSurfaceMetadata("mobile.account")` 和 Web zh/en `surface.mobile.account.*` 文案，和 Mobile App 的右下 Account 使用同一个 shared IA source。截图/probe：`app/web/screenshots/web-mobile-account-nav-pw-390x844.png`、`app/web/screenshots/web-mobile-account-nav-pw-390x844.probe.json`；probe 为 `innerWidth=390`、`scrollWidth=390`、`navLabels=[Workspace,Messages,Run,Account]`、Account aria 来自 shared metadata、`topbarAccountButtonCount=0`、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`smallTargets=[]`、`rawI18nKeys=[]`。
- Web/Mobile：2026-05-29 Web Account 登录面板继续做手机适配。Account 右下入口打开的 AuthPage 在 390px 下不再是 Desktop 居中 modal，而是底部贴边 glass sheet：`WebLayout.module.css` 的 mobile `.modalLayer` align/end stretch，`AuthPage.module.css` 增加 390px sheet 宽度、顶部 handle、14px 顶圆角、safe-area padding、44px close/Continue/Advanced controls；Desktop 宽屏 modal 保持原样。`AuthPage.tsx` 移除硬编码 `关闭`/`高级设置`，改用 `action.close` 和新增 `auth.advancedSettings` zh/en。截图/probe：`app/web/screenshots/web-mobile-account-sheet-pw-390x844.png`、`app/web/screenshots/web-mobile-account-sheet-pw-390x844.probe.json`；probe 为 `innerWidth=390`、`scrollWidth=390`、sheet `width=390`/`bottom=0`/`radius=14px`、Continue/Close/Advanced 均 44px+、`gradientCount=0`、`leftOnlyBorderCount=0`、`leftInsetShadowCount=0`、`rawI18nKeys=[]`、英文视图无硬编码中文。
- Runtime bridge：Web→Hub task→Desktop→Local Edge→Hub typed stream/done 的可见输出闭环已落地并有回归测试，Web 直连 Edge 遗留 hook 已清理并有 `.\scripts\verify-web-hub-boundary.ps1` 防回归。下一步不要重复补普通 content 投影；优先做真实 TokenDance ID 部署态登录/设备注册 smoke、Desktop logout/reconnect 截图证据、Web 公开发布前的 BFF/HttpOnly cookie 或等价 server-owned session 方案，以及 typed stream live E2E 截图。OIDC 结构检查入口是 `.\scripts\verify-oidc-readiness.ps1`，只证明仓内 wiring/docs/storage/Hub WS upgrade auth 对齐，不替代真实部署 smoke。Web Agent/Profile 面已开始从 preview mock 收敛到 Hub `agent_profiles`；继续推进时优先把 Settings/Market/Execution Target 接到 Hub profile/target/task lifecycle。
- Web：2026-05-26 RunDetail runtime 投影已补齐。`app/web/src/utils/hubAdapters.ts` 新增 `projectRunDetail()`，从 Hub Chat blocks 派生 output/tool call/changed files；`app/web/src/layouts/WebLayout.tsx` 右侧 RunDetail 不再传空数组。验证通过 `app/web && corepack.cmd pnpm test -- src/utils/hubAdapters.test.ts`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm exec vite build`。
- Hub：2026-05-26 typed RunEvent persistence 已做最小闭环。`/edge/agent-tasks/:id/stream` 兼容旧 `content/chunk`，也接受 `event_type + payload`；Hub 写入 `agent_run_events`、发布 `agent.stream`，并继续投影 `message.new` 供现有聊天 UI 使用。`GET /web/agent-tasks/:id/events` 提供 owner-scoped replay/read。验证通过 Hub 聚焦测试、`go test ./... -short -count=1` 和 OpenAPI YAML 解析。
- Hub/Web/Desktop：2026-05-26 Web typed RunEvent consumption 已做仓内闭环。Desktop bridge 不再把 tool/file/runtime payload 只作为普通 content 回传，而是对 `run.agent.*` / `run.output.batch` 调 Hub typed stream；Hub app 订阅 `agent.stream` 并推送到 session WebSocket；Web 新增 `AgentRunEvent` client 类型和 `listTaskRunEvents()`，`WebLayout` 合并 `agent.stream` 与 `GET /web/agent-tasks/{id}/events` replay，RunDetail 优先消费 typed events。`hubAdapters` 覆盖 Codex `files[]` file_change、stdout batch chunks、tool call/result 投影。验证通过 `hub-server && go test ./internal/app -run TestStartEventSubscriptionsPushesAgentStreamToSession -count=1 -v`、`app/web && corepack.cmd pnpm test -- src/utils/hubAdapters.test.ts`、`app/web && corepack.cmd pnpm typecheck`、`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/useHubIntegration.test.ts`、`app/desktop && corepack.cmd pnpm typecheck`。
- Hub/Web/Desktop/Edge：2026-05-26 Agent Profile runtime config bridge 验证通过 `hub-server && go test ./internal/service -run "TestDispatchTaskIncludesPrompt|TestMergeModelParamsLetsDispatchOverrideProfileDefaults" -count=1`、`edge-server && go test ./internal/api -run "TestPostRunsPassesRuntimeProfileConfigToExecutor|TestPostRunsBindsProjectAndThread" -count=1`、`edge-server && go test ./internal/lifecycle ./internal/adapters -run "TestClaudeCodeBuildCommandArgs|TestCodex|TestOpenCodeBuildCommandArgs" -count=1`、`app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/useHubIntegration.test.ts`、`app/web && corepack.cmd pnpm test -- src/api/agentQueries.test.ts src/utils/hubAdapters.test.ts`、Web/Desktop typecheck 和 OpenAPI YAML 解析。
- Hub/TokenDance ID：2026-05-26 OIDC dev setup 示例已对齐动态 loopback 规则。`hub-server/.env.example`、`scripts/setup-tokendance-oidc.{ps1,sh}` 和 `scripts/seed-tokendance-client.sql` 不再写 `http://127.0.0.1:PORT_IDX/callback` / `agenthub://callback`，改为注册无端口 `http://127.0.0.1/callback` 和 Web dev callback，并输出 `AGENTHUB_TOKENDANCE_ID_ALLOWED_REDIRECT_URIS`。
- Web/Desktop：2026-05-26 Settings 账号页 OIDC 状态已移除旧 `td_code_verifier` / `td_state` 判断。Web 使用当前 `agenthub_oidc_pkce_pending` session payload 区分一次浏览器 PKCE 往返提示；Desktop 不再从 storage 推断 PKCE，因为 Tauri OIDC state/verifier 保持在系统浏览器 + 本机回调路径中。验证通过 `app/desktop && corepack.cmd pnpm exec vitest run src/__tests__/SettingsPage.test.tsx`、Web/Desktop typecheck 和 `git diff --check`。
- Hub：2026-05-26 Execution Target owner boundary 已收紧。`GET /web/execution-targets/:id` 和 `POST /web/execution-targets/:id/ping` 现在都要求目标 `owner_id` 等于当前 Hub user，避免任意 Hub Web 会话凭 UUID 读取或标记其他用户的执行目标；聚焦验证通过 `hub-server && go test ./internal/service ./internal/handler -run "TestExecutionTarget" -count=1`。
- Hub/Edge：2026-05-26 Execution Target workspace policy 地基已推进。Hub target model/migration/API 增加 `workspace_allowlist`、`trust_level`、`health_state` 和 policy enum 校验；Edge `agenthub-edge` 增加 `--workspace-allowlist` / `AGENTHUB_WORKSPACE_ALLOWLIST`，配置后拒绝 allowlist 外或 symlink 逃逸的 `/v1/runs.workDir`，且拒绝路径不会创建 run 或启动 Runtime。验证通过 Hub/Edge 聚焦测试、两个 Go 短测、OpenAPI YAML 解析和 `git diff --check`。注意这不是 Remote/Cloud target 完成证据，只是 registered target/workspace policy 的本地执行护栏。
- Web/Desktop：2026-05-26 Execution Target Settings 已接 Hub 真实 inventory。两端 `hubClient` 和 `executionTargetQueries` 读取 owner-scoped `/web/execution-targets`，Settings 展示 Hub target 总数/在线数、health breakdown、类型计数、登录/加载/空/错误态和逐 target ping；Web 仍通过 Hub session 读取，不新增浏览器到 Local Edge 的调用。该进展只证明 target inventory 和 health 可见，不代表 Web/Hub task 已按 `target_id` 调度到远程或云执行目标。验证通过 Web `hubClient/agentQueries/executionTargetQueries` focused Vitest、Web typecheck、Desktop `hubClient/executionTargetQueries/SettingsPage` focused Vitest、Desktop typecheck、Web-Hub boundary、JSON 解析和 `git diff --check`。
- Hub/Web/Desktop：2026-05-26 `target_id` task contract 已推进。`/web/agent-tasks` 接受可选 owner-scoped Execution Target id，Hub 校验 owner/deleted/type 后写入 `pending_agent_tasks.target_id` 并在 `agent.dispatch` payload 透传；Web/Desktop Hub client 类型和 OpenAPI/events 文档同步。当前仍按现有 inviter desktop route 派发，下一步 `feat/hub-edge-target-routing` 才做 target.device_id route、离线队列隔离和禁止 fallback。
- Hub：2026-05-26 `feat/hub-edge-target-routing` 已完成仓内最小 target-bound desktop dispatch。`/web/agent-tasks target_id` 会解析 Execution Target 绑定的 owner desktop `device_id`，预写 `pending_agent_tasks.edge_device_id`，dispatch 只查 `desktop:<device_id>` exact route；目标 device 离线、route 不存在或 conn/device 不匹配时进入 target/device 专属 Redis queue，Desktop reconnect 只 replay 同一 device 的 target queue，legacy 无 target 任务仍走原用户队列。验证通过 Hub cache/ws/service/app focused tests 和 `hub-server && go test ./... -short -count=1`。这仍不是 Remote SSH/Tailscale/Cloud Edge 完成证据。
- Docs：2026-05-26 8 个部署场景口径已收紧。当前不是 8/8：Desktop 本地离线已完成，Desktop 本地在线和 Web 中继当前 Desktop 是最小闭环；Desktop/Web 到远程 Desktop 或 Cloud Edge 的直接/中继场景仍属 P3/P4，依赖 registered Edge target、workspace allowlist、relay routing 和远程审批。
- Docs：`bytedance.md` 功能映射已归档到 `docs/reference/cross-comparison/11-bytedance-feature-map.md`。该文档只作为 gap matrix 和竞品方向参考，不是 Trump/旧 worktree 完成证据；下一批顺序仍是部署态 TokenDance ID smoke、Runtime UI parity、多 Agent group smoke、Artifact lifecycle，以及 Home 站点到 AgentHub Web/Hub session 的低风险深链。
- Docs：Cherry Studio 参考已新增到 `docs/reference/projects/cherry-studio/`，入口已挂到 `docs/reference/README.md`。可采纳点是 operational Home、Settings row/group primitives、typed message/tool blocks、composer scopes、artifact preview、SDK env blocklist、permission hook、SSE abort/timeout 和 Channel/Scheduler 边界；明确不采纳 renderer-as-SSOT、provider secret 持久化、Web 直连本地 Runtime 或第三方 provider 直登。AionUi 继续只作为 action-first Home、team composition、runtime auto-detection、scheduling/approval ergonomics 参考。
- Docs：AgentTeam 竞品深度研究已新增到 `docs/reference/cross-comparison/13-agentteam-competitive-roadmap.md` 并同步主架构/产品/实现/roadmap 口径。当前只能说 Hub group/session、Agent Profile、Desktop bridge 和 Edge local sub-agent prototype 是基础；产品级 `AgentTeam`、`TeamRun`、`TeamTask`、`TeamEvent`、可恢复 `TeamRunState`、typed `CoordinatorRouteDecision`、delegation guardrails、双真实 Runtime Profile 群组 E2E、聚合 transcript 和冲突处理仍未完成。下一批建议拆 `feat/agentteam-contract`、`feat/teamrun-state-router`、`feat/teamrun-local-smoke`；路线保持 IM-native，ReactFlow/DAG 只作为 TeamRunState 可视化或模板编辑器，不做 canvas-first 产品转向。
- Docs：产品方向竞品研究已新增到 `docs/reference/cross-comparison/14-product-direction-competitive-roadmap.md`。长期定位是 local-first multi-runtime Agent command center + Hub-governed collaboration fabric + Target network；四根柱子是 Runtime Workbench、AgentTeam Collaboration、Target Network、Agent Platform。Runtime 生态方向要从“支持几个 CLI”升级为 runtime control plane：Runtime capability、`ModelSpec/ModelRoute`、cc-switch `ProviderBinding`、Tooling Registry 和每次 run 的 `RunConfigSnapshot`。8 场景缺口按 P2A identity/session evidence、P2B enterprise foundation、P3A registered Remote Edge、P3B Cloud Edge lease、P4 Team Platform 管理。下一批不要重复做静态 Home/preview surface，优先推进 `feat/operational-home-console`、`feat/runtime-event-blocks-ui`、`feat/artifact-lifecycle`、`feat/run-config-snapshot`、`feat/platform-profile-market`、Target dispatch 三段任务和 Remote/Cloud productization 四段任务。
- Runtime readiness：`.\scripts\verify-runtime-readiness.ps1` 是 AgentAdapter/Runtime wiring 的结构 gate，只证明仓内真实调用链和 UI/docs 边界对齐；Codex、Claude Code、OpenCode live smoke 已在 2026-05-26 通过。下一步需要补 Desktop/Web 截图证据、Hub task typed stream live E2E 和真正阻塞式审批；尤其 Claude Code 阻塞式审批不要用当前 permission event 展示替代。
- Desktop：Settings `Tasks` 已接 `/v1/runs` 与 Hub task bridge，重复 terminal run event 的 `COMPLETED -> COMPLETED` warning 已清理；下一优先级是继续把 IM 群聊、Agent 调度、Agent Market、Skill/MCP、模型映射、cc-switch 从预留 surface 接到真实 Hub/Edge API，并补 runStore/TanStack Query active run 列表同步链。
- Desktop：Settings `Agent Scheduling` 已接本地 run、Hub bridge task、Agent Profile、Edge health 与策略开关；下一优先级建议继续把 Agent Market、Skill/MCP、模型映射、cc-switch、远控/审计从预留 surface 接到真实 Hub/Edge/API 或本机配置源，并把 runStore/TanStack Query active run 列表同步链补齐。
- Desktop：Settings `Agent Market` 已接本地 Agent Profile 和 capability 字段；下一优先级建议继续把 Skill/MCP、模型映射、cc-switch、远控/审计接到真实 Hub/Edge/API 或本机配置源，并补 Profile 发布/安装的 Hub API 契约。
- Desktop：Settings `Skill Management` 已接项目级 `.agents/skills` registry 快照；下一优先级建议继续把 MCP、模型映射、cc-switch、远控/审计接到真实 Edge capability、Hub API 或本机配置源。
- Worktree 建议：下一轮优先拆 `feat/hub-edge-target-routing`（按 `target_id -> device_id` 派发、pending queue 隔离、禁止 fallback）、`feat/runtime-event-blocks-ui`（typed blocks/tool group/artifact projection）、`feat/operational-home-console`（两端 Home 展示 active runs、pending approvals、target health、TokenDance ID session state）和 `feat/runtime-typed-control-callbacks`（normalized callback/control event）。Execution Target 不要一口吃成远程/云执行；所有任务都必须保持 Web Hub-only、Hub-issued session 授权和 secret-free 证据边界。
- Edge：active-run 真实 HTTP smoke 已用可控慢 runner 收口；后续若 3210 真实 runtime 仍出现双 202，应重点确认当前进程版本和 runtime 是否在第二个 POST 前已完成。
- Edge：raw output cap 和 structured adapter payload cap 已 repo 内缓解；后续用 live runtime 做截断 metadata smoke，确认真实 Codex/Claude/OpenCode adapter 事件在客户端可读。
- Edge：下一步若继续权限链路，要做真正阻塞式审批和远程 Edge 决策认证；当前修复只关闭 REST 任意 requestId 伪造和重复消费。
- Docs：处理文档架构审查的最小建议：先在 API/架构文档标注 Runner 兼容命名，再归档或改写旧 `docs/archive/client-handoff.md`、`docs/roadmaps/integration.md`。
- Docs：Codex follow-up 已完成，确认最小补丁应优先改 `docs/architecture/implementation-guide.md` 的旧 client 文档入口，并在 API docs 标注 `/v1/runners` / `runner.*` 是历史兼容命名。
- Web：~~继续保持 `D:\Code\TokenDance\AgentHub\.worktrees\webui-desktop-port` 独立~~ worktree 及 `feat/webui-desktop-port` 分支已于 2026-05-25 删除，产出已合入 `dev/delicious233`。
- 后端：保留当前 Hub/Edge 并行改动，不回退；Hub `device_id` UUID 边界、多设备登录 schema、Hub cache fallback、Hub public stats bucket、dev compose loopback、Edge REST timeout、Edge local auth、Edge active-run smoke 和当前 `client-smoke` 23/23 已收口，后续优先做部署态 Hub 登录/设备注册验证、runner degraded/offline 与 Hub task/IM/scheduling API 的客户端消费；若继续安全队列，下一批可处理 `AH-SR-016/017` 部署态 CORS/admin 暴露验证，或开始 Remote/Cloud Edge 的 Hub session/device proof 设计。

### 本轮提交（2026-05-24）
- `cd26e2c` — Claude Session 2026-05-25 交接报告 + ui-screenshot skill；包含 Hub CancelTask/session_id 修复与 auth middleware 测试适配
- `e03c407` — merge master 冲突解决
- `adc829d` — CI/配置修复（go.mod、Dockerfile、CI workflow、docker-compose）
- `d299f1c` — 清理死代码 useAgents.ts
- `5e04e76` — P0-1 状态架构重构：TanStack Query + Zod + runStore 纯客户端化
- `3faa348` — STATE.md + roadmap 更新
- `4cd8551` — i18n 中文化收官
- `1f50b17` — hubAuth getState snapshot 稳定化

### Desktop P0 验收清单
- [x] P0-1: TanStack Query + Zod + RunState 状态机 + selector 优化
- [x] P0-2: 非受控输入 + 草稿持久化 + 循环检测 + 文件去重缓存
- [x] P0-3: WebSocket 心跳 + 离线队列 + Transport 抽象
- [x] P0-4: 虚拟滚动 + App.tsx 568→343 行

### 已知问题（预存，非阻塞）(持续监控)
- 5 个 shared/ui 测试文件无法加载（pnpm 跨包虚拟存储）
- AuthPage 4 个测试失败
- hubClient getState snapshot 测试失败
- `AH-SR-012` 的当前树清理已完成：已删除跟踪中的 `app/desktop/stats.html`、Edge ad hoc coverage 文件和 Hub test upload blobs，并补 `.gitignore` 防止重新生成后误提交；旧提交历史里的 blob 是否重写清理仍需维护者单独协调。

## 模型分配

| 别名 | 实际模型 | 上下文 | 角色 |
|---|---|---|---|
| **opus** | DeepSeek-V4-Pro | 1M | 主 Agent 架构/审查 |
| **sonnet** | Kimi-K2.6 | 256k | 前端/多模态 |
| **haiku** | GLM-5.1 | 200k | Go 后端编码 |

## 项目规则

- `AGENTS.md` — 共享开发规范
- `docs/governance/branch-governance.md` — 分支策略
- `docs/governance/document-standards.md` — 文档规范
- `docs/roadmap.md` — 全局路线图（唯一事实源）
- `.ops/deployment-record.md` — 部署记录（本地，不入仓库）

## Subagent 接口

### 后端 subagent（haiku/GLM-5.1）
- 范围：`edge-server/` 或 `hub-server/`，不碰 `app/desktop/`
- 提交格式：`type(scope): 中文摘要`
- 验证：`go build ./... && go test ./... -short -count=1`

### 前端 subagent（sonnet/Kimi-K2.6）
- 范围：`app/desktop/` 或 `app/web/`，不碰 Go 代码
- 共享 UI 组件放 `app/shared/src/ui/`（从 `@shared/ui` 导入）
- 新组件必须：测试 + Storybook story + barrel export
- 样式用 CSS Modules + OKLCH 变量（禁止硬编码颜色）
- 验证：`pnpm tsc --noEmit && pnpm test`

### 主 Agent（opus/DeepSeek-V4-Pro）
- 设计决策、审查输出、编辑核心文件
- 分发 subagent、交叉审查
- 更新 roadmap 和文档

## 当前阻塞 / 已知问题

- api.hub.vectorcontrol.tech 无 SSL（HTTP only）— （待排期）
- 登录已修复但需验证（migration 0017 + UUIDv7 修复后需重建容器）— （低优先级）
- 服务器磁盘 29GB 总量偏小，需定期清理 Docker 镜像 — （运维任务）

## 本地开发

```powershell
# Edge Server（必需，先启动）
cd edge-server && go build -o agenthub-edge.exe ./cmd/agenthub-edge && .\agenthub-edge.exe --store-file test_store.json

# Desktop（Tauri 原生窗口）
cd app/desktop && pnpm tauri dev

# Hub Server — 不需要本地跑！Desktop 直接连生产 Hub
# Hub URL: http://api.hub.vectorcontrol.tech
```

## 接手文档

| 文档 | 位置 | 面向 |
|---|---|---|
| 前端接手指南 | `.ops/frontend-handoff.md` | 前端 agent |
| 后端接口审计 | `.ops/backend-audit.md` | 后端 agent |
| 运维手册 | `.ops/deployment.md` | 运维 |
