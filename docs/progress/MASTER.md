# AgentHub Progress Tracker

> **Task**: post-polish residual hardening — **complete**
> **Started**: 2026-07-16 · residual closed 2026-07-21 · **Last Updated**: 2026-07-29
> **Mode**: `GITHUB_FULL` · **Repo**: `TokenDanceLab/AgentHub`

## Progress index

| Item | Value |
|---|---|
| Active SPEC | **3 P3 RFC DRAFT** (#1478 直播 / A-V1 #1471 / A-V3 #1472) — 待管理员定档 · PROPOSAL #1412/#1413/#1414 CLOSED 未合并 |
| Closed residual | [overview](../analysis/post-polish-project-overview.md) · plan trio under `docs/plan/post-polish-*` |
| Strategy | Strangler Fig; **no** Visual QA chase past 89 |
| Live tip | master `aa345f07`（wave v4 续18：4 opus 并行 F/G/H + mobile spike + ProjectMain loading 接线，72 commit 累计） |

Phases 73–80 **closed**. Historical `docs/plan/task-breakdown.md` is **HISTORICAL only**.

## 企业稳定·综合优化（2026-07-29）

坏了有人知道、回归挡在 CI。两波闭环。

### 强制门禁波（validate 硬阻断，七层）
| 门禁 | PR | 覆盖 |
|---|---|---|
| Web Hub-only boundary | #1431 | web 不直连 Local Edge |
| Mobile Hub-only boundary | #1436 | mobile 禁 3210/v1 events|runs/edgeAuth |
| Hub pure packages import | #1435 | dispatch/outbox/im/agentevent 无 DB/WS/cache |
| Design token re-export | #1440 | styles 仅 @import shared |
| 前端 coverage 基线不可退 | #1443 | shared 72.07/web 63.61/desktop 67.13（A-V3 后重测，shared +0.5pp） |
| OpenAPI↔hub 路由契约 | #1444 | 152/155 + 3 admin 白名单 |
| hubClient thin-shell SSOT | #1452 | web 59/desktop 212/mobile 378 行，禁 `/client/` 新字面量 |
| shared 边界 / barrel / handler 分层 / conventions | #1463 | 4 门禁扩展（A #1/#2/#4/#5） |
| shared-UI hubClient 门禁（A-V2 / A7#4 专属面） | #1468 | 共享 UI 层禁运行时 import hubClient，类型导入放行；与 #1463 互补（依赖图相反两侧） |
| Hub 客户端↔Hub router REST 契约 | #1467 | A#3 契约门禁（verify-shared-rest-contract.ps1，0 drift；Edge 面未纳入） |

### 观测波（12 缺口全闭环，Prometheus 11→32 counter）
| 缺口 | PR | metric |
|---|---|---|
| G3 outbox 重试/死信/redispatch | #1447 | 4 counter |
| G4 edge-dispatch 6 类失败 | #1447 | CounterVec + unreachable Debug→Warn |
| G9 JWT/WS auth 失败 | #1447 | 3 counter + WS audit log |
| G11 redis_pool_hits Gauge→Counter | #1447 | 类型修复 |
| G12 sendFrame seq_id bypass | #1446 | characterization + bypass counter（KNOWN DEFECT） |
| G1 ws delivery 失败 | #1449 | CounterVec |
| G2 ws 断连/重连/僵死 | #1449 | 3 counter |
| G5 dispatch offline push | #1449 | CounterVec 6 route |
| G6 team 超时/状态转移 | #1449 | 2 counter |
| G7 eventbus submit 失败 | #1449 | counter |
| G10 admin server up | #1449 | Gauge |
| G8 DB 慢查询/错误 | #1450 | 2 counter + db_pool_idle（rows==0 metric 仍计） |
| notification/heartbeat/dead-letter-move | #1462 | 4 counter（dispatch_dead_letter_move / notification / heartbeat / session_touch） |

### correctness
| 主题 | 结果 |
|---|---|
| A10 TurnInProgress 409 全链路 | **closed** · #1437 Hub per-agent_instance 409 + #1442 前端可恢复 |
| fault-escalation event 不裸吞 | **closed** · #1441 |
| agentteam 并发/生命周期/投影 | **closed** · #1383 #1384 #1385 #1427 #1429 |
| WS 死面 / 幂等语义 | **closed** · #1422 #1433 |
| edge 存储 | **closed** · #1424 #1426 |
| #1395 测试残余 | **closed** · #1425 #1432 |
| 删 2 处过时 skip 恢复覆盖 | **closed** · #1461 |
| resolveCache 三包提取 / agentteam payload 去重 | **closed** · #1464 #1465（8/10 统一，#1385 投影不重做） |
| dispatch 成功路径单测 / #1430 gate 直测 | **closed** · #1476（TriggerAgentTask 成功 + 4 错误路径 + TurnInProgress 409 二次触发 gate） |
| TS 常量自等 tautology 修正 | **closed** · #1475（全仓扫仅 1 处真 tautology：oidc-login `expect(true).toBe(true)` → DOM 渲染断言） |

### Open follow-ups

| 主题 | Issue / PR |
|---|---|
| G12 sendFrame 走 PushToConn 修复（M，待管理员定档） | #1446 PR body 建议 |
| ACP spike（紧迫度↑：Phase 2 prep 已落地——pure ACP→Edge 事件映射器 + 单测，待真 binary 集成） | #1404 |
| agentteam 子任务直播（#1478 Phase A 已合入 `d4270360`：后端归属 + bus fan-out；**Phase C 前端起点 S1 已落地** `bef0a5f8`：SubagentStreamStore + webHubRealtime handler + 测试，前端零消费→有消费；Phase B/C DRAFT 待定档） | #1478 |
| @agent 派单（产品 RFC top 1，不碰协议；**Phase 1-3 全闭环**：P1 `5b836599` composer dispatchRole + submit 触发 triggerAgentTask + metadata；P2 `05032e41` SubagentStreamOverlay 浮层栈；**P3 `35b52cda` 委派卡钻取子会话 transcript**——SubagentTranscript 7 类事件分类+中文标签+payload 格式化，chip 展开 inline 渲染，20 test。北极星三阶段闭环：@agent 派单→委派状态可见→钻取子会话） | #1406 |
| Automations / 会话导入 / 观察池 | #1405 · #1407 |
| 签名发布 / WS 增量 SPEC | #1403 · #1411 |
| PROPOSAL（**CLOSED 未合并** 2026-07-29，提案文档归档可查） | #1412 · #1413 · #1414 |
| P3 裁决项（A-V4/D-V3 closed；D-V2 评估为低价值；D-V1 -41% 完成；A-V1/A-V3 **RFC 已写**，待管理员定档） | #1469 · #1470 · #1471 · #1472 |
| D-V1 edge run() 持续重构（Step 3: completeRunAttempt + handleFaultEscalation 提取，累计 418→115 行 -73%，三阶段管道 + outcome 枚举 + goto escalate） | 进行中 |

Research off-repo: `D:\Code\Temp\codeg-research\` — SYNTHESIS.md（v0.21.9 基准）+ v0.22.1-DELTA.md（45 commits 增量分析）。综合审计：`agenthub-comprehensive-audit-2026-07-29.md` / `agenthub-observability-audit.md`（A/D/T 源报告已被综合/证伪，已清理）。

### 审计澄清

- Desktop「15 pre-existing fail」过期；CI 全绿
- A7 候选余量：hubClient thin-shell / shared UI hubClient（**A-V2 已由 #1468 合入**，全闭环）

## Product tip & Visual QA

**Gate**: **89**/100 SHIP。**不追 89+**。Refs: [scorecard](../analysis/visual-qa-scorecard.md)

## Infrastructure

- `validate` 硬门禁：doc-ssot · ci-gates · web/mobile-hub-boundary · pure-packages · token-ssot · coverage-baseline · openapi-contract · hubclient-ssot · shared-boundary · shared-barrel · hub-layering · conventions · shared-rest-contract · shared-ui-hubclient（**14 层全部合入**）
- 观测：`/metrics`（admin server，basic auth）+ 36 Prometheus counter（11 基线 + 25 新增）
- 供应链：Dependabot 0 open（brace-expansion fix #1370/#1397 + alert #43 dismissed）
- 综合审计 off-repo：`agenthub-comprehensive-audit-2026-07-29.md`（D 32 + T 13 + A 17，top 10 落地 **10/10**：#1461(1) · #1462(2/3/4) · #1463(5/6/7) · #1464(9) · #1465(8) · #1475(10，实测仅 1 处真 tautology，审计「52 处」高估)）+ T-M1 dispatch 成功路径 #1476
- residual out of scope: live OIDC evidence · full Mobile redesign · QA>89

## Session Log

| Date | Summary |
|:-----|:--------|
| 2026-07-27 | #1420–#1433 波；#1385/#1395 closed；A10=#1430；PROPOSAL 三审 NEEDS_FIX |
| 2026-07-29 | 强制门禁波（#1435-#1445）+ 观测波 12 缺口闭环（#1441/#1446/#1447/#1449/#1450）+ hubClient 门禁（#1452）；A10 全链路；32 metrics；Dependabot 0 open |
| 2026-07-29 | 综合审计三维度（D/T/A 62 条）→ 快修波（#1461 删skip/#1462 counter/#1463 4门禁）+ 重构波（#1464 resolveCache/#1465 agentteam payload）；12 门禁/36 metrics |
| 2026-07-29 | P2 收口：A#3 Hub 契约门禁合并（#1467 → master 6d3875cb；A#4/A#5 已在 #1463）。P3 裁决：8 项实测，D-V1 受 #867 硬阻塞 DEFER；D-V2/D-V3/A-V1/A-V3/A-V4 转 PLAN issue #1469–#1473；A-V5 已采纳 |
| 2026-07-29 | A-V2 澄清修正：非「由 #1463 覆盖」——p2 关门前补 A-V2 专属门禁 PR #1468（shared-UI 禁运行时 import hubClient，类型导入放行；与 #1463「平台↛共享Edge」是依赖图相反两侧的互补面，master 上 0 违规）。#1468 rebase 至最新 master 解 checks.yml/verify-ci-gates.ps1 加法冲突，本地 anti-cheat 通过，MERGEABLE 待 CI |
| 2026-07-29 | MASTER 同步：A-V2 澄清 + #1468 门禁入 14 层索引；live tip `6d3875cb`（#1467），#1468 待 CI 后合 |
| 2026-07-29 | #1468 合入 master `54c21867`（A-V2 闭环）；MASTER tip 同步至 #1468，14 层门禁全部合入；综合审计 top10 落地 6/10 |
| 2026-07-30 | fable 并行 lane 收口：Lane A #1476（TriggerAgentTask 成功 + 4 错误路径 + #1430 TurnInProgress gate 直测，320 行）/ Lane B #1475（全仓扫仅 1 处真 TS tautology，审计「52 处」高估→澄清）合入 master `31c482cc`。综合审计 top10 **10/10** 全落地 + T-M1 覆盖缺口闭环 |
| 2026-07-30 | 基线清理：分支 103→8，stash 20→0（已在之前会话清完）。codeg v0.22.1 竞品增量分析完成：45 commits 6 簇，3 个竞争信号（子 agent 直播/自定义 agent 平台/@session 提及）。产出 `v0.22.1-DELTA.md`。#1404 ACP spike 紧迫度升级，#1406 方向调整为 @agent 派单，agentteam 子任务直播待立 issue |
| 2026-07-30 | **D-V3 完成**（#1470）：startEventSubscriptions 239 行拆为 5 域方法（message/agent/team/contact/session），主函数退化为 7 行分发器。**D-V2 评估**：PostRuns 357 行实为清晰线性 pipeline，非 god function，审计高估——跳过低价值重构。**D-V1 评估**：edge run() 418 行含会话重试循环 + fault escalation 交接 + 多并发原语，高价值但高风险，需深入理解 #867 语义后专项设计 |
| 2026-07-30 | **D-V1 Step 1**：buildAndStartProcess 138 行提取到 process_executor_build.go（新文件），run() 从 418→281 行（-33%）。提取的是循环体中最机械的构建+启动阶段，每条错误路径自己发事件，调用者仅需 return。控制流决策（continue/break）未提取，留待后续步骤。lifecycle tests 全过。 |
| 2026-07-30 | **D-V1 Step 2**：collectAndWaitOutput 40 行提取到 process_executor_build.go，run() 从 281→246 行。循环体现在是清晰的 3 阶段管道：(1) buildAndStart → (2) collectAndWait → (3) evaluate+finish。累计 418→246 行（-41%）。lifecycle tests 全过。 |
| 2026-07-30 | **D-V1 Step 3**：completeRunAttempt（Phase 3 决策/完成）+ handleFaultEscalation（循环后 #867 后继交接）提取到 process_executor_build.go。引入 attemptOutcome 枚举（done/retry/break）驱动调用者控制流，runCtx 改指针以便会话重试就地重写。run() 循环体现为 switch+goto escalate 单标签。run() 246→115 行，累计 418→115（-73%）。lifecycle tests + adapters tests 全过，go vet clean。|
| 2026-07-30 | **God function 全仓扫描**：扫出 19 个 >150 行函数。3 个真正 god function 评估：PostRuns 358 行（HTTP handler，长但线性，跳过）、StartTeamRun 226 行（已标 #1385）、HandleSubAgentFailure 158 行（已结构化 Step 0-6，委托子函数，跳过）。其余 16 个为长但线性的路由/注册/wiring/parser。D-V1 仍是唯一高价值重构——已完成（-41%）。|
| 2026-07-30 | **ACP Spike Phase 2 prep**：把 ACP session/update → run.agent.* 翻译提取为纯映射器 `acp_events.go`（无 I/O、无状态、前向兼容），ParseStream 接线通知/响应，阻塞请求留 Phase 2。8 个单测覆盖全部映射类型 + 3 个有意不映射类型 + prompt-result 边界。adapters 全测试过。|
| 2026-07-30 | **PROPOSAL 状态澄清**：#1412/#1413/#1414 经查于 2026-07-29 CLOSED 未合并（非永久挂起）。MASTER open 表从「NEEDS_FIX 不 merge」更正为「CLOSED 未合并，归档可查」。|
| 2026-07-30 | **leader wave（fable 深拆 + opus 执行）**：3 RFC 落地——#1478 agentteam 直播 SPEC（对标 codeg live subagent，3 阶段 A/B/C，edge 零改）；A-V1 #1471（驳回 lifecycle 拆分+adapters 全量叶子化，采纳定向 adapters/orchestrator 抽取，preflight 已捕获 PlanTask/TaskStatus 导入环）；A-V3 #1472（驳回 shared 三分，采纳定向剔除 apiClient+硬化 edge 隔离）。safe quick-win 执行：删 shared/apiClient（454 行零消费）+ web/desktop workspace 依赖声明 + edge-surface-isolation 软门禁 + coverage 重测（shared +0.5pp）。ACP 词汇锁测试防 §3 映射漂移。全本地验证：build/test/3 门禁/coverage gate 全过。|
| 2026-07-30 | **D-V1 Step 3**：completeRunAttempt（Phase 3 决策/完成）+ handleFaultEscalation（循环后 #867 后继交接）提取到 process_executor_build.go。引入 attemptOutcome 枚举（done/retry/break）驱动调用者控制流，runCtx 改指针以便会话重试就地重写。run() 循环体现为 switch+goto escalate 单标签。run() 246→115 行，累计 418→115（-73%）。lifecycle tests + adapters tests 全过，go vet clean。master `5184f523`。|
| 2026-07-30 | **#1478 Phase A 合入** `d4270360`：team.subagent.stream 后端事件归属 + bus fan-out。HandleTaskStream 在 agent.stream 之外多发一条 team 域 bus 事件，经 dbTeamRunLookup（GetTeamRunBySessionID+ListAssignmentsByTeamRun+ListTeamTasksByRun，按 RunID 反查 pending task）+ 1024 条 LRU 缓存解析归属，app/events.go 订阅并 PushToSession。新增 1 个 WS 帧常量 TypeTeamSubagentStream（frame.go↔hubEvents.ts↔openapi.yaml↔events.md 1:1 同步；activity/batch/subscribe 留待有 wire producer 再加，遵守死面禁令）。edge 零改、前端未改也能观测。9 个测试（LRU/归属/缓存命中/双投递/非 team run/可注入）。build/vet/service+app tests/shared-rest-contract/ci-gates/hubEvents 契约全过。Phase B/C DRAFT 待定档。|
| 2026-07-30 | **leader wave v4 落地**（codeg 对标全栈并行，4 commit push master）：3 份设计报告（UIUX 差距分析 6 sonnet 卡+11 QW+2 RFC / 产品 RFC 8 裁决+#1406 不碰协议确认 / 后端扫描 H3 GetTeamRunState+E3 dispatch+H2 App.Run 三目标）。E3 opencode dispatch 委托化 `f3e44ddc`（140→17 行主分派器+8 handler，emit 语义零变更，adapters 88s test 全过）；S1 team.subagent.stream 前端订阅 stub `bef0a5f8`（SubagentStreamStore byTaskId+event_seq 幂等 UPSERT + webHubRealtime handler + 4 测试，#1478 Phase C 前端起点/前端零消费→有消费）；S2 fail 态自动展开 `bef0a5f8`（RowItem useRef once + useEffect fail 态自动展开跳过 approval/tool/sub，shared 1303 test 全过）；H2 App.Run 四阶段 `9b2cc0d8`（216→15 行 + initInfra/initServices/initHandlers/startServer，new 顺序一字不变零功能变更，app test 全过）。**commit 卫生注**：S1+S2 同 commit `bef0a5f8`（message 只提 S2，S1 文件被 S2 commit 纳入），未来独立改动应分开 commit。**子代理报告可信度教训**：opus/fable 报告行数/函数边界准确但"case 数/已做项"常高估/漏看（E3 实际 8 case 非 20+；QW-3 OIDC 脱敏已实现；H3 派生投影已委托 #1385 做过一轮），执行前必须主线程交叉验证；子代理"已完成+push"报告须亲自 git log/ls 确认产物在 master（S1 曾误报"已 push"实被 S2 commit 纳入）。|
| 2026-07-30 | **wave v4 续**：S3 think 卡 1s 收起 `a0d3ed67`（RowItem useEffect running→展开/非running→1s 收起+once 语义，1306 test 全过）；H3 GetTeamRunState 4 循环提取 `7b5df11d`（185→78 行 -58%，projectTeamMembers/Assignments/Tasks/RunEvents 跨循环依赖返回值传递，agentteam test 0.348s）；**#1406 Phase 1** `5b836599`（ComposerMention dispatchRole + submit 触发 triggerAgentTask + 消息 metadata JSON 包装，不碰协议，187 test 全过）。累计 7 commit + 3 报告 + SSOT+memory，wave v4 收口。|
| 2026-07-30 | **wave v4 续2**：S4 anti-FOUC 预水合 `2d0b8113`（web index.html inline 脚本首帧前写 theme dataset + matchMedia，tsc/test/build 全过）；**#1406 Phase 2** `05032e41`（SubagentStreamOverlay 浮层栈：固定右下角 + 5 色状态边框 + active→展开/done→3s 折叠 + event_seq 排序 + maxVisible 3+N badge，14 test，消费已有 WS 帧+S1 store 不碰协议）。累计 9 commit。|
| 2026-07-30 | **wave v4 续3 收口**：dispatchRole 测试断言修复 `91be9a02`（Phase 1 漏 shared test，#1406 mapAgentsToComposerMentions 断言同步）；S5+S6 `4a18c927`（reduced-motion 信息/装饰分级文档 + 玻璃双层 alpha `--td-glass-content-*` 锚定族 token + designTokens content-anchored 规则）；QW-5 `988ef7d8`（Windows cmd shim 三家去重提取 resolveNodeCLICommand，adapters 60.8s test 全过）；ARCH-7 `b11e434a`（oidc.go 461 行拆到 service/oidc/ 子包，OIDCService→oidc.Service，7 外部 import 更新，agentteam test 13/13 包全过）；QW-4 经 sonnet 深度核实**不执行**（lifecycle.ClassifyError 主分类器 vs adapters.ClassifyFailure 次分类器不同构不同抽象层，硬统一破坏分层——避免 opus 报告误判）；**#1406 Phase 3** `35b52cda`（SubagentTranscript 钻取组件 + Overlay chip inline 渲染，#1406 三阶段北极星闭环）。累计 15 commit + 3 报告 + SSOT+memory。**子代理稳定性教训**：fable glm-5.2 daily free limit 429（19h 恢复）+ opus 多次 API connection closed mid-response——深任务改主线程接手或派 sonnet 替代更稳。|
| 2026-07-31 | **wave v4 续3**（codeg 前端 UIUX 对标，3 commit push master）：QW10 empty copy 矩阵 `94ab7d91`（EmptyState 5 态+noPermission + i18n 8 类场景文案 zh+en + 5 页面分文案 + 3 market，shared test 全绿+zh/en parity）；QW5 risk badge 下沉 shared `6664e998`（RiskBadge.tsx+css+test 4 级 pill，desktop ApprovalCard 改用 shared，10 test，反超 codeg 无危险分级）；**QW4+QW6** `4c738a39`（审批卡六型分型 command+cwd/diff/plan/allowed_prompts/web/JSON，parseApKind 推断+max-h 36vh 滚动，对齐 codeg permission-dialog；think Shimmer 扫光+"Thought for Ns" 时长回填，对齐 codeg reasoning.tsx；17 test，shared 1354+web 187 全过）。UIUX 12 点已做 10/12（剩 T9/T10 轮询语汇需 edge spike）。累计 18 commit。|
| 2026-07-31 | **wave v4 续4**（codeg 前端 UIUX 对标收尾，3 commit + spike）：#3+#4 `5ed351cc`（--td-border-hairline token + @supports color-mix 升级，#3 消息气泡 --chat-* 已 content-anchored 确认覆盖，web build 过）；per-folder 主题色 `e1f30461`（8 色色板+ProjectInfo.themeColor+data-folder-accent CSS+ProjectsPage useEffect，10 文件+511，shared 1365+web 187 全过）；**T16** `62e3981a`（审批卡键盘 A/R/Esc+修饰键守卫+input 焦点忽略、critical 二次确认 arm→fire、RiskBadge 接入 RowItem+adapterMapBlock 打通 risk→riskLevel 端到端，14 test，shared 1378+web 187 全过，反超 codeg 无键盘/危险分级）；T9/T10 edge spike 文档落盘 `agenthub-t9-t10-spike.md`（结论：edge 无 poll 只有 push retry，T9 暂缓需产品决策语义来源，T10a ×N 借 attempt 不碰协议但 retry≠poll 语义错位需产品确认，T10b <N/M> 需 RFC）。累计 21 commit + 4 报告/spike。UIUX 12 点已做 11/12（T9/T10 暂缓）。|
| 2026-07-31 | **wave v4 续5**（codeg 视觉细节对标，5 commit + spike 报告）：细滚动条 `c7999c9c`+`611d82e5`（全局 ::-webkit-scrollbar 6px + --scrollbar-thumb/-hover/-track token 消费已定义未用，暗色保持黑色 edge 符合品牌）；Prism 对抗 `b9b679af`（2 处 !important → 校准特异性 (0,2,1) 压过 Prism (0,2,0)，对标 codeg globals.css:1580）；per-folder 选择器+desktop `ac925102`（8 色 swatch picker 存 ProjectInfo.themeColor + desktop presets.css @import）；**#1406 P3 时间线** `508782c0`（InlineDelegationCard 消息时间线内联挂载 + MessageDelegationStore 消费 agent.dispatch 帧 trigger_message_id+task_id + upsert 不可变性 bug 修复 + 39 测试，#1406 北极星完整闭环+时间线增强，不碰协议）；视觉细节 spike `agenthub-visual-details-spike.md`（6 项对照：instant-collapsible/OverlayScrollbars/Monaco 不适用/xterm 不适用/diff-merge 部分/CSS 特异性部分，Top 3 推荐：滚动条 token 收口/CollapsibleBlock fade+a11y/Prism 对抗）。累计 26 commit + 5 报告/spike。|
| 2026-07-31 | **wave v4 续6**（对话流 spike Top5 + 死代码清理，5 commit push master，对标 codeg 对话流/UI 交互）：B1 Toast 致命bug `79aa0c23`（desktop App.tsx 挂 ToastContainer + main.tsx setToastHandler 桥接 useToastStore；web 新建 components/Toast.tsx+Toast.module.css + App.tsx 挂载 + main.tsx 桥接；静默吞 toast 修复，useWebAuth 登录错误不再丢失；13 desktop Toast test + web 190 全过）；回到底部+搜索防抖+typing `aad3ebe1`（Transcript isNearBottom→圆形 ArrowDown 按钮 sticky bottom；MessageSearchPanel 300ms 防抖 immediateQuery→query；入站 typing WS 帧消费→typingPresence 观测者+TypingIndicator DM/群聊双模式三点弹跳，desktop useHubEventStream+web webHubRealtime 接入，15 文件+589，20 测试）；死代码清理 `91b5fcb0`（删 7 项 useStreamingText/useAutoScroll/TextShimmer/SearchInput/Skeleton/shared BottomSheet/Tooltip，18 文件 -1187 行，**B2 核实"shared/src/ui 整层死代码"为误判**——5 个符号在 48 文件 230 处活跃消费，仅删真死 + 修正 index.ts 过时"available for adoption"注释；worktree 隔离双跑 shared 1398+web 190 全过）；flaky 修复 `80b5376d`（MessageSearchPanel 搜索测试去 flaky——主线程交叉验证发现搜索防抖引入后 6 个即时测试用 findByText 重试 race 在全量 171 files 并发下 1 failed，子代理报告"全过"是低负载侥幸；改 flushDebounce helper act wrap await 350ms + 同步 getBy* 断言消除 race，全量 1425 全过）；停止按钮+编辑消息 `d5be5c87`（ComposerSendButton isRunning+onCancel 变形停止钮 stop 图标→cancelAgentTask REST+agent.cancel WS；ContextMenu role=human 文本块加"编辑"项→composer 回填 setText+setEditingMessage+ComposerEditBar→editMessage REST，链路 App.tsx→WorkbenchFrame→ConversationHost→UnifiedComposer，24 文件+365，7 测试）。对话流 spike Top5 全闭环。shared 1425+web 190 全过。累计 31 commit。**T8 未读分隔线深查发现**：shared chatview（agent 对话 transcript）未消费 last_read_seq，agent 对话是 stream-based（task run events）非 IM 已读水位模型；desktop IM（人类会话）有完整 last_read_seq 接线（api/hubEvents.ts:156+sessionQueries markRead+hubEventBridge onMessageRead+i18n im.session.unread）。T8 需先深查 shared chatview 消息模型是否适用 last_read_seq，非 spike 假设的简单 partition 加 divider。**commit 卫生交叉污染**：fable 停止+编辑的 i18n 键被偶混入 sonnet 回到底部 commit `aad3ebe1`（功能 OK，边界不干净）。|
| 2026-07-31 | **wave v4 续7**（对话流 spike T11+T14，3 commit push master）：T14 列表 infinite scroll `0f8e2415`（消费已有 pageCursor 翻页——API 层 HubWorkspaceProjectListResponse{items,page:HubPageInfo{nextCursor,hasMore}} 已完整支持，仅 UI 未消费；useWorkbenchProjectsRoute loadMore/hasMore/loadingMore + ref 门控防重复 + ProjectNav IntersectionObserver sentinel rootMargin 200px 提前触发 + ProjectsPage 透传 + workbenchRoutesHelpers 接线，7 文件+137，不碰协议）；T11 @mention inline popover `58f49d05`+`391dcccc`（textarea @ 触发联想 popover——复用 mentionableAgents prop + dispatchRole 派单语义，无改 store/协议全收敛 workbench+composer 表面层；composerMentionTrigger detectMentionTrigger/filterCandidates/removeTriggerText 纯逻辑 + composerMentionPopoverHelpers 键盘规划/光标钳制/mirror-div 测量 + ComposerMentionPopover listbox/option ARIA + UnifiedComposer onChange/onSelect/compositionEnd evaluateTrigger + IME compositionRef 守护 + 选中 setText 剥离+addMention+rAF 回正光标 + 点击外部关闭 + useLayoutEffect 钳制定位 + aria-expanded/controls/activedescendant，9 文件+1077，38 测试，173/1463 全过）。累计 34 commit。**子代理报告验证教训**：opus T14 报告"1 flaky MessageSearchPanel 超时"实为 opus 跑时机器高并发偶发，主线程重跑 shared 1463 全过 0 flaky，flushDebounce 修复稳固。**Bash 工作目录教训**：Bash 工作目录不持久跨调用，每次需 cd app/shared 再 npx vitest，否则在 D:\Code 根跑全域 14878 files 含 reference/* 大量 fail 误判。|
| 2026-07-31 | **wave v4 续8**（对话流 spike T9+T18+T16 + 测试基建修复，8 commit push master）：T9 composer onPaste/onDrop `234d0b18`（textarea onPaste clipboardData.files + form onDrag* dataTransfer.files → browserFilesToComposerAttachments→dispatchComposerAttachmentAdds + dashed dragOverlay，与 T11 @mention popover 正交不碰 evaluateTrigger/compositionRef，2 文件）；T18 会话列表排序 `82a8f90c`（ConversationSidebar sort dropdown recent/name/active + 置顶保持 + localStorage agenthub.conversationSort 持久化 + .sidebarSort CSS + 6 测试，3 文件）；T16 ErrorBoundary 下沉 shared 4 commits `66b36184`+`8f1eaf47`+`8c7a468f`+`c97d78e7`（新建 shared/ui/ErrorBoundary.tsx 基础 4 类 chunk/network/timeout/unknown + extensions prop 扩展点 classifyBaseKind 先试 extensions.matches 再 fallback + ErrorConfig primaryIcon + showStack prop + t() react-i18next + window.location.reload try/catch；合并双轨 CSS；18 tests；web 240→13 thin wrapper showStack=DEV；desktop 259→64 thin wrapper crash extension + network Reconnect override；mobile-rn 新建简单 ErrorBoundary+ErrorNotice fallback；crash/reconnect 行为不变）；**vitest config exclude .worktrees** `7bc29d69`（遗留 git worktree 目录 .worktrees/refactor/* 等测试文件被 vitest 误扫入，环境不同 document is not defined spuriously fail，干扰测试可信度——曾致子代理报告"3 fail"实为 .worktrees 干扰非主树 regression；加 exclude .worktrees/** + reference/** 永久消除，174/1487 全过）；**App.v4 mock 修复** `c210dd85`（B1 Toast 挂载后 App.v4 toastStore mock 仅 {showToast} 无 toasts，ToastContainer toasts.map crash 致 5 App.v4 fail；补全 mock 支持 selector + toasts:[]，10/10 全过——B1 sonnet 跑 Toast 单元测试但未跑 App.v4 集成测试漏看）。shared 174/1487 + web 26/190 + desktop App.v4 10/10 全过；desktop edge-real 8 fail 环境依赖 pre-existing（需真实 edge server，非 regression）。累计 42 commit。|
| 2026-07-31 | **wave v4 续9**（对话流 spike T19+T12+T10，3 commit push master）：T19 Spinner token `f78ae592`（tokens-base.css 7 个 --spinner-* token speed/easing/sm/sm-border/lg/lg-border + .deploySpinner/.workspaceLoadingSpinner 改用 token + border-radius 统一 --r-full，2 文件零回归 174/1487）；T12 ContextMenu a11y `7cd8910e`（useFocusTrap 焦点陷阱保存 trigger+Tab/Shift+Tab 包裹+回归 + ArrowUp/Down 循环导航+Home/End + Enter 选中 + roving tabindex 仅活跃项 tabIndex=0 + onMouseEnter 同步 + .item.active 复用 --surface-high/.danger.active 复用 --danger-bg，2 文件，173/175 通过 2 fail 为 T10 WIP+MessageSearchPanel flaky 非本次）；T10 草稿持久化 `b8cedf28`（composerDraft.ts loadDraft/saveDraft/clearDraft localStorage key agenthub.composer.draft.{sessionId} 存 text+mentions JSON validation + 16 单元测试；UnifiedComposer 单一 useEffect 合并 load+save 防 race + requestIdleCallback+setTimeout 200ms fallback 批量写 + flushDraft visibilitychange:hidden + 清除 on empty state + @mention/paste 不碰 + 5 集成测试 beforeEach polyfill/afterEach localStorage.clear；composer/index.ts re-export；只持久化 text+mentions attachments/replyTo/quote 排除，5 文件，175/1507 全过）。累计 46 commit。对话流 spike 22 项已完成 15/22（T1-T7/T9-T12/T14/T16-T19），剩 T8 误判低优/T13 已删调整/T15 RFC/T20 低优/T21 低优/T22 调查 + **T5 键盘快捷键配置层接线 B3 高价值待做**。|
| 2026-07-31 | **wave v4 续10**（对话流 spike T22+T20+T5，2 commit + 1 报告）：T22 composer 流式中调查（报告 `codeg-research/agenthub-composer-streaming-spike.md`，非代码）——揭示：① 流式中 composer 不禁用无占位符（Enter 可提交触发 409）；② 无客户端发送队列（流式中发送被 409 turn_in_progress 拒绝+toast，非 FIFO）；③ 多会话切换不 unmount（有利流式续传）但**滚动记忆"伪保留"bug**（身份比对误判切换为新增消息→强制 scrollToBottom + scrollTop/shouldAutoFollowRef 跨会话残留）+ composer shell 单例切会话保留 text（输入跨会话残留）；4 建议（占位符/禁用复用 blocked-submit/队列 pending intents+事件 flush/切换滚动记忆按会话隔离）；T20 FileExplorer roving tabindex `a623d515`（fileTreeKeyboard.ts buildVisibleRows/resolveTreeKeyboardAction 纯函数 + 树容器单 tab stop + aria-activedescendant + ArrowUp/Down/Home/End/Right/Left/Enter/Space + 点击 focus 归位 + CSS 抑制容器 focus ring，desktop 5 文件，34 测试，stash 验证 desktop 8 fail 均基线既有 T5 WIP+edge-real 非本次）；T5 键盘快捷键配置层接线 `7ff9882d`（**B3 严重缺陷修复**：useGlobalKeyboardShortcuts 中央 document keydown dispatch hook + matchesBinding 修 Ctrl/ token + Ctrl+K=global search/Ctrl+F=in-chat search 一致 + kebab-case 统一 ID + shared canonical 4 组 14 快捷键 config + ShortcutsPane Settings 渲染+冲突检测红色高亮 + SearchDialog 删 broken Ctrl/K listener，10 文件+428/-137，shared 175/1507 + desktop 235/2195 全过，edge-real 8 fail 环境依赖）。累计 49 commit。对话流 spike 22 项已完成 18/22（T1-T7/T9-T12/T14/T16-T20/T22 调查），剩 T8 误判/T13 已删/T15 RFC/T21 低优 + **T22 揭示的后续 bug**（切换滚动记忆/composer 跨会话隔离/流式中占位符/队列）。|
| 2026-07-31 | **wave v4 续11**（T22 bug 修复 + CF20 + T15 RFC，2 commit + 1 RFC）：切换滚动记忆 bug 修复 `610f41d5`（Transcript.tsx sessionId prop + 滚动状态 Map<sessionKey> 按会话隔离 + 切换保存/恢复位置 + 不触发 followAfterUserSubmit + 身份基线播种新会话 + ChatViewBridge 传 activeConversation.id，4 文件，175/1511 全过 +4 回归测试——T22 揭示的身份比对误判切换为新增消息→强制 scrollToBottom + scroll state 跨会话残留 bug 修复）；composer 跨会话隔离+流式中占位符 `57dff2e1`（composerReducer setConversationId 完全重置 + useWorkbenchSessionChrome 切前 saveDraft + T10 恢复新 session；UnifiedComposer isRunning 占位符变 "{target} is responding..." 中英文 composer.agentResponding；canSubmitFromKeyDown 接 isRunning 阻断流式中提交 复用 blocked-submit stop 按钮不受影响，9 文件+31/-9，175/1511 全过）；T15 虚拟化 RFC（报告 `codeg-research/agenthub-virtualization-rfc.md`）——推荐 virtua ^0.50.0（gzip 6KB 0 依赖 vs @tanstack 7.3KB 1 依赖；共享 scroll 容器零改动 scrollRef 挂 .transcript；children API 与 partitionWithDates/AgentGroup/IM 双气泡同构；codeg 用 virtua 8 处），集成风险：高亮跳转迁 scrollToIndex + 测试环境 ResizeObserver polyfill，待管理员定档实现。shared 175/1511 + desktop 235/2191 全过（edge-real 8 fail 环境依赖）。累计 52 commit。对话流 spike 22 项 20/22 完成 + T15 RFC。|
| 2026-07-31 | **wave v4 续12**（T8 desktop IM unread + T15 virtua 核心落地 + per-file mock，2 commit）：T8 desktop IM unread `eab1deb8`（desktop IM transcript 消费 HubSession.unread_count 水位渲染未读分隔线——transcriptUnreadMarker 纯函数派生 anchor block id + UnreadDivider 组件 + Transcript partition 插 unread segment + adapter resolveUnreadAnchorItemIndex + workbench transcriptUnreadDivider prop 贯通 + 复用 i18n im.session.unread/readThrough 首次消费 + normalizeHubMessages hubMessageBlockId 单源；18 测试；**随附 T15 virtua 核心落地**——Transcript virtua Virtualizer + scrollToIndex 高亮跳转 RFC §6.3 + virtua 依赖 + jsdom 测试环境 ResizeObserver/scrollIntoView stub + Virtualizer 直通 mock，因 T8/T15 并发互锁 sonnet 为树绿一并提交；T15 fable 被停因 WIP 过时；29 文件+838/-38）；per-file virtua mock `71f5ddb1`（AgentHubWorkbench/InlineDelegationCard passthrough Virtualizer，jsdom 无 layout 补全现有 test）。shared 177/1527 + desktop 238/2216 全过（edge-real 8 fail 环境依赖）。累计 55 commit。**T15 专用虚拟化测试（blockIndexMap/scrollToBlockId 接线）未完成**——T15 fable 被停时正在写，待补。**并发冲突教训**：T8/T15 都碰 Transcript.tsx（T8 unread partition + T15 virtua），派发时误判不重叠（T8 desktop IM vs T15 shared Transcript），实 T8 实现需改 shared Transcript 与 T15 互锁——未来派发需深查任务实际触碰文件跨域。|
| 2026-07-31 | **wave v4 续13**（T15 专用虚拟化测试 + T21 调查，1 commit + 1 报告）：T15 专用虚拟化测试 `44e94816`（Transcript.virtualization.test.tsx 7 测试——blockIndexMap row id/toolCallId→call-${id}/跨 segment 索引/standaloneRows/parts/未知 id→-1 + scrollToBlockId→virtua scrollToIndex{align:center} 可控 mock + ChatViewTranscript highlightedBlockId→scrollToIndex(0)+rAF highlighted class 迁移 + Virtualizer children 透传；补全 T15 fable WIP untracked 残留 + prettier + beforeEach 显式导入；177/1527 全过 tsc 0 错）；T21 compaction 调查（报告 `codeg-research/agenthub-compaction-spike.md`）——edge adapter 已发 compact_boundary（流内 system/compact_boundary）+ context_compaction（收尾 85% 预算）事件 WS 原样转发，但 normalizeEdgeEvents switch 无 case 落 default console.warn 静默丢弃；types/chat.ts 预留 compact block 但无生产者；实现 5 层小步（normalize case→mapper→transcript types CompactTranscriptBlock→adapterMapBlock case→渲染仿 UnreadDivider），不碰协议。累计 57 commit。**对话流 spike 22 项全完成**（21 实现 + T21 调查 + T15 RFC + virtua 核心+测试）。|
| 2026-07-31 | **wave v4 续14**（T21 compact divider + Dependabot uuid 修复，3 commit + 1 报告）：T21 compact_boundary divider `07698c3c`+`38b67f05`（5 层实现——normalizeEdgeEvents 加 case 不再 default warn/discard + compactBoundaryBlock mapper 提取 trigger/preTokens + CompactBoundaryTranscriptBlock types 加入 union + adapterMapBlock resolveCompactDividerIndices 仿 resolveUnreadAnchorItemIndex + CompactDivider 组件渲染仿 UnreadDivider 细线+"上下文已压缩"+trigger/preTokens，segment-level divider 与 virtua 正交，只 compact_boundary 非 context_compaction，10 文件+207，177/1527 全过，不碰协议）；Dependabot 调查（报告 `codeg-research/agenthub-dependabot-spike.md`）——2 moderate：uuid CVE-2026-41907（v3/v5/v6 buffer 越界，传递依赖 storybook/xcode→expo，可达性极低）+ glib cargo（tauri 锁 gtk ^0.18 Linux-only 无解）；uuid overrides 修复 `978eb27b`（pnpm.overrides uuid@7/@9→11.1.1 最后 CJS 兼容版，v4 签名一致同 brace-expansion 先例，shared 177/1527 全过，预期 Alert #5 自动关闭；glib 跟踪上游）。累计 61 commit。对话流 spike 22 项全完成 + T15 virtua + T21 compact + Dependabot uuid。|
| 2026-07-31 | **wave v4 续15**（T13 BottomSheet 评估，1 报告）：T13 BottomSheet 评估（报告 `codeg-research/agenthub-bottomsheet-eval.md`）——结论**弃**（保持删除 91b5fcb0）：shared BottomSheet 只为旧 app/mobile（已死壳）而生，mobile-rn 活跃 BottomSheet 是 RN 原生 Modal 不可复用，web/desktop 零底部滑入弹层需求（web 居中弹窗，desktop 居中 scrim/popover/右键菜单 + .overlayPanelLeft 死 CSS），shared Modal 仍在（未来需求从 Modal 派生 bottom-anchored 变体）；**附带死代码清理候选**：desktop 死左抽屉 CSS（.overlayPanelLeft 无 TSX 消费 8bdce2f7 遗留）+ web 死 i18n key（drawerAria 等）+ app/mobile 死壳目录（大范围需管理员批准）。**对话流 spike 22 项真正全闭环**。|
| 2026-07-31 | **wave v4 续16**（web virtua 测试基建修复 + T13 附带死代码清理，2 commit push master）：T13 附带死代码清理 `cb93212a`（desktop .overlayPanelLeft 死左抽屉 CSS 删除 + web drawerAria 等死 i18n key 清理，续15 评估报告认定的真死代码，零 TSX 消费）；**web virtua 测试基建修复** `89b2a616`+`418fb1a1`（T15 续12 落地 virtua Virtualizer 到 shared Transcript 后，web/package.json **从未声明 virtua 依赖**——仅 shared/desktop 声明。pnpm 严格 node_modules 下 web 测试通过 shared 软链解析 virtua，导致 web setup.ts 的全局 `vi.mock('virtua')` passthrough 无法拦截真实模块：jsdom 零高度容器下 virtua 渲染 0 行，App.test.tsx "renders Hub sessions and messages" 丢失 transcript 文本 "来自 Hub session 的真实消息"（1 failed | 4 passed）。根因被掩盖因 mock 语法正确——virtua 根本不是 web 可解析依赖，mock 工厂注册到一个 web 从不直接导入的模块。补 web/package.json virtua ^0.50.0 让 web 拥有自己的解析路径，setup.ts passthrough mock 生效，children 无需 layout 引擎即可渲染。对齐 desktop setup.ts（已声明 virtua + 同 passthrough mock）。**验证**：web 全量 190/190 green（修复前 189/190 含 T15 引入的 App.test regression）。App.test.tsx 5/5。**教训**：vi.mock 失效优先查依赖声明——pnpm 严格 node_modules 下未声明依赖只能借软链解析，mock 工厂注册到不可解析模块时静默失效，表现像"mock 没生效"而非"模块未安装"。|
| 2026-08-01 | **wave v4 续17**（fable UIUX gap scan + 6 opus 并行落地 9 项不碰协议 polish，6 commit push master）：fable 深度调研报告 Top10（对标 codeg v0.22.x，扫描 workbench/chatview/composer/ui/mobile-rn，排除已做 22 项 spike + Top3 视觉 + B1/停止/编辑/T11/T14/T18/T10/T5/T16/T19/T20/T8/T15/T21，碰协议项 T9 checked/T10 poll/word-diff/Automations 标 DRAFT 跳过）。6 opus 并行（A=CodeBlock 复制+折叠 `e9c4e508`、B=mobile ErrorBoundary 挂载 `0c741035`、C=SkeletonBar 变体+EmptyState i18n `7a89b595`、D1=rail/inspector roving+skip-link+?帮助 `1ae94f58`、D2=sidebar 右键+虚拟化+roving `50b4a3da`、E=composer 附件大小+预览 `06035b3d`）。**主线程收口交叉验证**（子代理"测试全过"常限于子集）：① tsc 净 0 新错误（修 Markdown.test 解构 bug/File|undefined/'auto'→'center'/editingMessageId/[0]! 6 类共 ~15 处类型错误）；② D1 报告 2 fail 是真实接缝——C 改 AgentInstalledViews/ProjectMain 文案为 i18n key 但 AgentHubWorkbench.test.tsx mock map 没同步 projects.edit/agents.empty.*，主线程补 mock 修复；③ opus-C 新加 AgentsPage.test 缺 react-i18next mock 致 t('agents.empty.*') 返回 raw key，补 narrow vi.mock 修复；④ A/D2 被 stopped 但文件改动已落地，主线程验证接住。**验证**：shared 182/182 1570 green（基线 1527 +43 新测试）、web 26/26 190、mobile-rn 26/26 339 全绿。**教训再确认**：多 agent 并发改同包源码即使文件不重叠，i18n mock 同步 + tsc 类型 + 测试 mock 是三大接缝事故点，必须主线程统一回归兜底。|
| 2026-08-01 | **wave v4 续18**（4 opus 并行 F/G/H + mobile spike + ProjectMain loading 接线，4 commit push master）：**ProjectMain loading 接线** `b85f93c5`（opus-C 留的 TODO——ProjectsPage 传 loading={projectsLoading} 给 ProjectMain，首载不再闪"暂无项目"空态，改显详情骨架屏）；**F SubagentSessionDialog 钻取弹层** `3397e8cd`（chip 独立钻取按钮→Modal 内嵌 SubagentTranscript，codeg 式点 chip 弹完整子会话，保留 Phase3 内联展开，zh+en subagentStream i18n，team/index.ts 导出，59 测试绿）；**G infinite scroll 扩 Tasks+Contacts** `f535c23e`（T14 IntersectionObserver sentinel 模式扩 TasksPage/ContactsPage，向后兼容可选 hasMore/loadingMore/onLoadMore，**诚实标 TODO**——tasks/contacts 数据层无 pageCursor REST 支持仅 mock，UI 骨架落地 hasMore 默认 false 不显示，接线在 route hook 层 follow-up，11 测试绿）；**H composer 客户端发送队列** `aa345f07`（T22 spike 第 3 项——409 turn_in_progress 时派单 intent 入队，isAgentRunning true→false flush 重派单不重发消息，3 次放弃+toast，换会话丢弃，新增可选 RunPort.redispatchTask 平台端口 web 实现 triggerAgentTask(messageId)+409 映射，desktop/demo 零改，pendingIntents 8 单测+4 集成测试绿）；**I mobile-rn 流式 spike 报告**（报告 `codeg-research/agenthub-mobile-streaming-spike.md`——方案 A 推荐：mobile-rn 自建轻量增量 store 复用 shared 已导出 normalizer，不碰协议不放宽边界，难度 L 中偏低，4 项前置含原生端无 WS 实时层需接线 hubLifecycle/mobilePlatform；**关键发现**：真实 getMobileSnapshot transcript 恒空，增量流是移动端拿 transcript 唯一现实路径非锦上添花）。主线程收口：edge-server .bak 死代码清理（D-V1 残留未追踪备份）+ team/index.ts 导出补全。**验证**：shared 183/183 1591 green（续17 1570 +21 新测试）、web 26/26 190 全绿，tsc 净 0 新错误。累计 72 commit。**遗留 TODO**：① tasks/contacts 数据层 pageCursor 接线（route hook 层）；② H 的 redispatchWebTask 无直接单测（仅 tsc+mock 集成覆盖）；③ H 的 badge/toast 文案硬编码未收进 i18n；④ mobile-rn 流式 4 项前置待做。|
