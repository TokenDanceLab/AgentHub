# AgentHub 交接说明

更新日期：2026-06-09  
当前事实源：`origin/master` / `origin/dev/delicious233`，不要以本地主工作树 dirty 状态判断进度。

## 当前基线

- `dev/delicious233` 已通过 PR #297 合入 `master`。
- `master` 最新合并提交：`b7e9c1a4 Merge pull request #297 from TokenDanceLab/dev/delicious233`。
- 合入的 dev 基线：`fc0f0628`。
- PR #297：`chore: promote dev baseline for AgentHub 0.3.0 RC`，状态 `MERGED`。
- 手工 Release Readiness dry run `27209763756` 已完成并通过。
- 当前没有打开的 PR。
- 当前打开 issue 只剩：
  - #126 `Separate Desktop Hub client methods from web-only routes`
  - #64 `docs: 清理公开文档中的 live ops 细节`

本地主工作树 `D:\Code\TokenDance\AgentHub` 落后远端且有大量历史 dirty 文件。后续事实判断、合并、打包、发布都应先 `git fetch origin`，并以远端 `master` / `dev/delicious233` 为准。

## 已进入主线的主要能力

- Desktop/Web shared v4 Workbench 主壳。
- Hub/Edge 后端合同硬化：AgentProfile、ExecutionTarget、Hub Projects/workspaces P1。
- Hub 单任务 approval / artifact 合同和 Web 消费路径。
- IM/@Agent 主链、typed transcript、RightInspector、runtime/provider icons。
- Edge CLI adapter command shape、ProcessExecutor、SQLite/SDK fixture 与 no-secret readiness gate。
- Desktop exact target bridge、Tauri/sidecar smoke、release preflight 修复。
- Mobile Expo/RN 主线已进入 `app/mobile-rn`，旧 `app/mobile` Tauri 线不再作为活跃 mobile 主线。
- Mobile native media/storage capability gates 已推入 dev，并随 PR #297 基线进入 master。

这些能力很多仍是 fixture、observed、no-spend 或 dry-run 证据。不要把它们表述为真实登录、真实模型调用、签名发布或生产可用。

## 未完成的关键闭环

最高优先级是可演示真实产品链路：

1. TokenDanceID 真实登录到 Hub session。
2. Desktop 启动 Tauri，注册 Local Edge / target。
3. Web 通过 Hub 选择 Desktop target。
4. Hub 下发任务到 Edge。
5. Edge 调用本地 CLI adapter，至少跑通 safe/no-spend 或受控真实 CLI。
6. 运行事件、approval、artifact、transcript 回传 Hub。
7. Web/Desktop 展示可录屏闭环。
8. 输出脱敏 observed manifest。

当前仍未完成或不能声称完成：

- 真实 TokenDanceID disposable account 登录闭环。
- 真实 CLI/model/API 消费闭环。
- Web 远程控制 Desktop/Edge 的完整端到端验收。
- Windows 签名安装包、macOS 签名/公证、Android/iOS release tag 或 store release。
- Artifact content/apply/discard/revert 的安全写入闭环。
- Agent Builder 从配置到运行的可见完整产品闭环。
- Agent 间私聊、群聊、项目群、群内 @Agent、orchestrator 编排的完整后端/前端联动。

## 当前排队工作

优先吸收或收口：

- README 开源首页重组：`docs/readme-open-source-home`，提交 `88cec2b9 docs(readme): 重组开源首页`，只改 `README.md`，尚未合入 dev。
- Desktop CLI readiness UI：`desktop-cli-readiness-ui-20260609` worktree 有未提交改动，范围在 `app/desktop`，内容是设置页展示 codex/claude/opencode discovery 状态和 no-spend 边界。
- Real login 相关 worktree 仍需收口真实证据和阻塞项。
- Web Projects read-through、Artifact Workspace、Agent Builder visible loop、Edge SQLite durable store、A2A group/orchestrator 多条线需要逐条审查是否已被 dev 吸收，不能整枝乱合。

低优先级或需谨慎：

- 大量旧 `codex/p0-*`、`codex/p1-*`、backend/mobile 历史 worktree 仍在本地。先查 commit 是否已被 `origin/master` 或 `origin/dev/delicious233` 包含，再删除。
- `origin/dev/johnny`、`origin/feat/backend-edge-hub` 属旧大分叉，只能按功能摘补丁。
- mobile 已有专门负责人，主线程只做版本协调和主线 gate，不抢 mobile 内部实现。

## 分支与工作区规则

- `master`：当前稳定发布基线。
- `dev/delicious233`：开发主线。
- 本地主工作树：仅作历史现场，不作为事实源，不建议直接 pull/merge。
- 新开发：从最新 `origin/dev/delicious233` 创建隔离 worktree。
- 文档小改可以直接落主工作树，但不要混入代码脏改；提交前必须按路径精确 staging。
- 禁止 `git add .`。
- 禁止 force push 和 destructive cleanup。
- 删除 worktree 前先确认：
  - `git status --short` 干净；
  - 分支提交已被 `origin/dev/delicious233` 或 `origin/master` 包含；
  - 没有 subagent 仍在使用。

## 48 小时优先级

### P0：真实可用链路

- 真实登录：TokenDanceID -> Hub session -> Web/Desktop session state。
- Desktop/Tauri：可启动、本地 Edge 可注册、设置页能看见 CLI discovery 状态。
- Web remote：Web 选择 target、发任务、看 transcript/approval/artifact。
- Edge CLI：safe/no-spend CLI readiness 和受控真实 CLI 路径拆清楚。
- Evidence：输出脱敏 manifest 和可录屏 demo。

### P1：发布基线

- README 重组为开源仓库首页。
- Windows unsigned package dry evidence 固化。
- Android dev build/device proof 继续补齐。
- Release notes 和 tag 策略确认后再打 tag。
- master 已同步 dev，下一次 tag 前先确认 CI 和 release workflow。

### P2：产品加分线

- Agent Builder：AgentProfile、runtime、model、tools、MCP、avatar、skills、memory、market preset。
- Agent 群聊：私聊、项目群、群内 @、agent-to-agent message、orchestrator。
- Artifact Workspace：topic/version/preview/download/export，后续再做 apply/revert。
- LobeHub icon：runtime/model/tool logo 统一专业化。
- Target health：Hub/Web/Desktop 同步健康状态和绑定状态。

## 交接给三个负责人的建议

### Trump：前端、体验、Mobile 协调

- 收口 Web/Desktop visible demo：首页不要做空壳，直接让 IM/@Agent、Runs、RightInspector、Artifact 能看见真实/fixture 状态。
- 在 Desktop 设置页完成 CLI readiness UI，显示 codex/claude/opencode discovery、版本、可用性、no-spend 状态。
- 启动 Web/Desktop 本地 dev server 做 UI 细调和截图检查。
- Mobile 归 Trump 协调，mobile 负责人继续 `app/mobile-rn`，主线只要求 gate 和 release 证据。
- 同步 AgentHub 产品图标引用：AgentHub 本体用 `agenthub-*` 蓝底白条资产，不再新增旧 `token-dance-*` 泛名引用。

### Johnny：后端、Hub/Edge、真实链路

- 负责 Hub session、target binding、Edge dispatch、CLI adapter、artifact/approval/event replay 的真实链路。
- 不再大包合旧 backend 分支，只按功能摘补丁。
- 优先解决 #126：Desktop Hub client methods 与 web-only routes 分离。
- 补真实登录和 remote-control E2E 所需的最小服务端证据。

### 总控：分支、发布、证据

- 维护 `dev/delicious233` 和 `master` 节奏。
- 收口 README、handoff、release notes、tag 决策。
- 审查 stale branches/worktrees/issues/PRs。
- 分配 subagent 并要求每条线输出：写集、commit、验证命令、是否真实运行、是否可合并。

## 下一步命令建议

```powershell
git -C D:\Code\TokenDance\AgentHub fetch origin --prune --tags
git -C D:\Code\TokenDance\AgentHub rev-parse origin/master
git -C D:\Code\TokenDance\AgentHub rev-parse origin/dev/delicious233
gh pr list --repo TokenDanceLab/AgentHub --state open
gh issue list --repo TokenDanceLab/AgentHub --state open
```

发布前不要直接打 tag。先确认：

```powershell
gh run list --repo TokenDanceLab/AgentHub --branch master --limit 10
gh workflow run release-readiness.yml --repo TokenDanceLab/AgentHub --ref master
```

Tag 候选可用 `v0.3.0-rc.7` 或下一个 rc 号。打 tag 会触发 release workflow，必须在 Windows/Android 证据明确后执行。

## 当前口径

对外可以说：

- AgentHub 已把 dev 基线推进到 master。
- Desktop/Web/Hub/Edge/Mobile 的主要骨架已合流。
- 当前进入真实登录、真实本地 CLI 控制、Web 远程控制 Desktop、release packaging 的最后闭环阶段。

不要说：

- 已经生产可用。
- 已经完成真实模型调用。
- 已经完成签名安装包或商店发布。
- Web/Mobile 已完整远程控制真实 Desktop CLI。

