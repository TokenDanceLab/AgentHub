name: Release

on:
  push:
    tags: ['v*']

env:
  GO_VERSION: "1.25"
  NODE_VERSION: "22"
  PNPM_VERSION: "10"

jobs:
  # ── Go: Edge Server + Hub Server ─────────────
  build-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Create dist directory
        run: mkdir -p dist

      - name: Build Edge Server (Linux)
        working-directory: edge-server
        env:
          GOOS: linux
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-edge-linux-amd64 ./cmd/agenthub-edge/

      - name: Build Edge Server (Windows)
        working-directory: edge-server
        env:
          GOOS: windows
          GOARCH: amd64
        run: go build -ldflags="-s -w" -o ../dist/agenthub-edge-windows-amd64.exe ./cmd/agenthub-edge/

      - name: Build Hub Server (Linux)
        working-directory: hub-server
        env:
          GOOS: linux
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-hub-linux-amd64 ./cmd/server-hub/

      - name: Build Edge Server (macOS amd64)
        working-directory: edge-server
        env:
          GOOS: darwin
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-edge-darwin-amd64 ./cmd/agenthub-edge/

      - name: Build Edge Server (macOS arm64)
        working-directory: edge-server
        env:
          GOOS: darwin
          GOARCH: arm64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-edge-darwin-arm64 ./cmd/agenthub-edge/

      - name: Build Hub Server (macOS amd64)
        working-directory: hub-server
        env:
          GOOS: darwin
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-hub-darwin-amd64 ./cmd/server-hub/

      - name: Build Hub Server (macOS arm64)
        working-directory: hub-server
        env:
          GOOS: darwin
          GOARCH: arm64
          CGO_ENABLED: "0"
        run: go build -ldflags="-s -w" -o ../dist/agenthub-hub-darwin-arm64 ./cmd/server-hub/

      - name: Upload Go artifacts
        uses: actions/upload-artifact@v4
        with:
          name: go-binaries
          path: dist/

  # ── Tauri Desktop ────────────────────────────
  build-desktop:
    needs: build-go
    runs-on: windows-latest
    env:
      TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: app
        run: pnpm install --frozen-lockfile

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Download Go binaries for sidecar
        uses: actions/download-artifact@v4
        with:
          name: go-binaries
          path: go-binaries

      - name: Prepare edge-server sidecar binary
        shell: pwsh
        run: |
          New-Item -ItemType Directory "app\desktop\src-tauri\binaries" -Force
          Copy-Item "go-binaries\agenthub-edge-windows-amd64.exe" "app\desktop\src-tauri\binaries\agenthub-edge-x86_64-pc-windows-msvc.exe"

      - name: Build Tauri (NSIS)
        working-directory: app/desktop
        run: pnpm tauri build

      - name: Create dist directory
        shell: pwsh
        run: New-Item -ItemType Directory dist -Force

      - name: Collect NSIS installer
        shell: pwsh
        run: |
          $ver = "${{ github.ref_name }}" -replace '^v',''
          $nsis = Get-ChildItem "app\desktop\src-tauri\target\release\bundle\nsis" -Filter "*setup.exe" | Select-Object -First 1
          Copy-Item $nsis.FullName "dist\AgentHub_${ver}_x64-setup.exe"
          # Collect updater metadata if present
          $updater = Get-ChildItem "app\desktop\src-tauri\target\release\bundle" -Filter "latest.json" -Recurse | Select-Object -First 1
          if ($updater) { Copy-Item $updater.FullName "dist\latest.json" }
          $sig = Get-ChildItem "app\desktop\src-tauri\target\release\bundle" -Filter "*.sig" -Recurse | Select-Object -First 1
          if ($sig) { Copy-Item $sig.FullName "dist\AgentHub_${ver}_x64-setup.exe.sig" }

      - name: Create portable zip
        shell: pwsh
        run: |
          $ver = "${{ github.ref_name }}" -replace '^v',''
          $exe = Get-ChildItem "app\desktop\src-tauri\target\release" -Filter "agenthub-desktop.exe" | Select-Object -First 1
          $portableDir = "dist\AgentHub-portable"
          New-Item -ItemType Directory $portableDir -Force
          Copy-Item $exe.FullName "$portableDir\AgentHub.exe"
          Copy-Item "go-binaries\agenthub-edge-windows-amd64.exe" "$portableDir\agenthub-edge.exe"
          @"
          AgentHub ${{ github.ref_name }} — Portable Edition

          No installation required. Double-click AgentHub.exe to run.
          Requires: Windows 10/11 (WebView2 built-in).

          Full documentation: https://github.com/TokenDanceLab/AgentHub
          "@ | Out-File "$portableDir\README.txt" -Encoding UTF8
          Compress-Archive -Path "$portableDir\*" -DestinationPath "dist\AgentHub_${ver}_x64-portable.zip" -Force

      - name: Upload Desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-artifacts
          path: dist/

  # ── Tauri Desktop (macOS) ─────────────────────
  build-desktop-macos:
    needs: build-go
    runs-on: macos-latest
    env:
      TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: app
        run: pnpm install --frozen-lockfile

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Download Go binaries for sidecar
        uses: actions/download-artifact@v4
        with:
          name: go-binaries
          path: go-binaries

      - name: Prepare edge-server sidecar binary
        run: |
          mkdir -p app/desktop/src-tauri/binaries
          cp go-binaries/agenthub-edge-darwin-arm64 app/desktop/src-tauri/binaries/agenthub-edge-aarch64-apple-darwin

      - name: Build Tauri (DMG)
        working-directory: app/desktop
        run: pnpm tauri build

      - name: Create dist directory
        run: mkdir -p dist

      - name: Collect DMG
        run: |
          ver="${{ github.ref_name }}"
          ver="${ver#v}"
          dmg=$(find app/desktop/src-tauri/target/release/bundle -name "*.dmg" -o -name "*.app" | head -1)
          if [ -z "$dmg" ]; then
            echo "ERROR: No DMG or APP found in bundle directory"
            find app/desktop/src-tauri/target/release/bundle -type f 2>/dev/null || echo "bundle dir not found"
            exit 1
          fi
          cp "$dmg" "dist/AgentHub_${ver}_aarch64.dmg"

      - name: Upload macOS Desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-macos-artifacts
          path: dist/

  # ── GitHub Release ───────────────────────────
  release:
    needs: [build-go, build-desktop, build-desktop-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Go artifacts
        uses: actions/download-artifact@v4
        with:
          name: go-binaries
          path: dist

      - name: Download Desktop artifacts
        uses: actions/download-artifact@v4
        with:
          name: desktop-artifacts
          path: dist

      - name: Download macOS Desktop artifacts
        uses: actions/download-artifact@v4
        with:
          name: desktop-macos-artifacts
          path: dist

      - name: List assets
        run: ls -la dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          files: dist/*
          draft: false
          prerelease: false
          generate_release_notes: true
