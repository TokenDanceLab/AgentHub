#!/bin/bash
# AgentHub prepare-commit-msg hook — inject template
# Only for interactive commits (no -m, no -F, no merge, no amend).

case "$2" in
  "")
    # Interactive commit — inject template if file is empty
    if [ ! -s "$1" ]; then
      cat > "$1" << 'TEMPLATE'
# type(scope): summary
#
# Allowed types:
#   init     — project initialization
#   feat     — new feature
#   fix      — bug fix
#   docs     — documentation only
#   refactor — code restructuring (no feature/fix)
#   chore    — build, deps, tooling
#   test     — testing
#   perf     — performance improvement
#   ci       — CI/CD changes
#   revert   — revert previous commit
#
# Scope is optional (lowercase, alphanumeric + . _ -).
# Summary: lowercase English, no trailing period, max 72 chars.
# Leave one blank line between subject and body.
#
# Examples:
#   docs: add architecture diagrams
#   feat(edge): implement websocket hub
#   fix(runner): handle subprocess timeout

TEMPLATE
    fi
    ;;
esac
